/**
 * Migration 021's strengthened recovery qualification, kept outside the
 * historically applied SQL so that immutable migration bytes can be restored.
 */
export const MIGRATION_021_STRONG_RECOVERY_QUERY = "WITH qualified_candidate AS (\n  SELECT job.job_id, lease.admission_id, lease.complete_at\n  FROM jobs AS job\n  JOIN cleanup_leases AS lease\n    ON lease.job_id = job.job_id\n   AND lease.status = 'handed_back'\n  JOIN job_events AS completion\n    ON completion.job_id = job.job_id\n   AND completion.event_type = 'cleanup_complete'\n   AND completion.at = lease.complete_at\n   AND completion.state = lease.stale_state\n  JOIN job_events AS handback\n    ON handback.job_id = job.job_id\n   AND handback.event_type = 'recovery'\n   AND handback.at = lease.handback_at\n   AND handback.state = 'interrupted'\n  WHERE job.state = 'interrupted'\n    AND job.queue_state = 'complete'\n    AND job.terminal_at IS NOT NULL\n    AND job.runner_unit IS NOT NULL\n    AND job.runner_started_at IS NOT NULL\n    AND job.runner_finished_at IS NULL\n    AND job.runner_lease_owner IS NOT NULL\n    AND job.runner_lease_expires_at IS NOT NULL\n    AND job.cleanup_admission_id IS NULL\n    AND job.cleanup_fence_generation IS NULL\n    AND job.cleanup_fence_token_hash IS NULL\n    AND job.cleanup_blocker_code IS NULL\n    AND job.cleanup_blocker_json IS NULL\n    AND job.container_id IS NULL\n    AND job.container_name IS NULL\n    AND job.container_image_digest IS NULL\n    AND job.container_label_job_id IS NULL\n    AND job.container_label_manifest_sha IS NULL\n    AND job.container_labels_json IS NULL\n    AND job.container_mount_json IS NULL\n    AND job.container_env_json IS NULL\n    AND job.container_security_json IS NULL\n    AND job.container_inspection_json IS NULL\n    AND job.container_created_at IS NULL\n    AND job.container_started_at IS NULL\n    AND job.container_stopped_at IS NULL\n    AND job.container_removed_at IS NULL\n    AND job.container_cleanup_outcome IS NULL\n\n    -- One handed-back generation is the complete admission boundary. Expired\n    -- predecessors are allowed, but another handed-back generation is not.\n    AND (\n      SELECT COUNT(*)\n      FROM cleanup_leases AS handed_back_lease\n      WHERE handed_back_lease.job_id = job.job_id\n        AND handed_back_lease.status = 'handed_back'\n    ) = 1\n    AND NOT EXISTS (\n      SELECT 1\n      FROM cleanup_leases AS active_lease\n      WHERE active_lease.job_id = job.job_id\n        AND active_lease.status IN ('admitted', 'claimed', 'failed', 'blocking')\n    )\n\n    -- Bind the terminal row to the immutable handed-back lease and its sealed\n    -- evidence identity. Canonical timestamps make lexical chronology exact.\n    AND job.dispatched_at IS NOT NULL\n    AND strftime('%Y-%m-%dT%H:%M:%fZ', job.dispatched_at) = job.dispatched_at\n    AND lease.claim_at IS NOT NULL\n    AND lease.complete_at IS NOT NULL\n    AND lease.handback_at IS NOT NULL\n    AND strftime('%Y-%m-%dT%H:%M:%fZ', lease.admitted_at) = lease.admitted_at\n    AND lease.handback_at = job.terminal_at\n    AND strftime('%Y-%m-%dT%H:%M:%fZ', job.terminal_at) = job.terminal_at\n    AND strftime('%Y-%m-%dT%H:%M:%fZ', job.runner_started_at) = job.runner_started_at\n    AND strftime('%Y-%m-%dT%H:%M:%fZ', job.runner_lease_expires_at) = job.runner_lease_expires_at\n    AND strftime('%Y-%m-%dT%H:%M:%fZ', lease.claim_at) = lease.claim_at\n    AND strftime('%Y-%m-%dT%H:%M:%fZ', lease.complete_at) = lease.complete_at\n    AND strftime('%Y-%m-%dT%H:%M:%fZ', lease.handback_at) = lease.handback_at\n    AND strftime('%Y-%m-%dT%H:%M:%fZ', lease.expires_at) = lease.expires_at\n    AND job.dispatched_at <= job.runner_started_at\n    AND job.runner_started_at <= job.runner_lease_expires_at\n    AND job.runner_lease_expires_at < lease.admitted_at\n    AND lease.admitted_at <= lease.claim_at\n    AND lease.claim_at <= lease.complete_at\n    AND lease.complete_at <= lease.handback_at\n    AND lease.complete_at < lease.expires_at\n    AND lease.blocker_code IS NULL\n    AND lease.blocker_json IS NULL\n    AND lease.completion_evidence_path = 'jobs/' || job.job_id || '/evidence/cleanup/' || lease.admission_id || '.complete.json'\n    AND length(lease.completion_evidence_sha256) = 64\n    AND lease.completion_evidence_sha256 NOT GLOB '*[^0-9a-f]*'\n    AND lease.stale_runner_unit = job.runner_unit\n    AND lease.stale_runner_owner = job.runner_lease_owner\n    AND lease.stale_runner_lease_expires_at = job.runner_lease_expires_at\n    AND lease.stale_state IN ('starting', 'preflight', 'source', 'release_gates', 'frontend', 'target_setup', 'feeds', 'config', 'building', 'verifying', 'publishing', 'cancel_requested')\n    AND lease.stale_container_id IS NOT NULL\n    AND lease.stale_container_name IS NOT NULL\n    AND json_valid(lease.stale_container_labels_json) = 1\n\n    -- The admission snapshot is immutable after claim. Require its runner and\n    -- exact container identity to agree with every stale_* field on the lease.\n    AND json_valid(lease.proof_json) = 1\n    AND json_type(lease.proof_json) = 'object'\n    AND (SELECT COUNT(*) FROM json_each(lease.proof_json)) = 6\n    AND NOT EXISTS (\n      SELECT 1\n      FROM json_each(lease.proof_json)\n      WHERE key NOT IN ('runner', 'state', 'container', 'staging', 'logs', 'blocker')\n    )\n    AND json_type(lease.proof_json, '$.runner') = 'object'\n    AND (SELECT COUNT(*) FROM json_each(json_extract(lease.proof_json, '$.runner'))) = 5\n    AND NOT EXISTS (\n      SELECT 1\n      FROM json_each(json_extract(lease.proof_json, '$.runner'))\n      WHERE key NOT IN ('unit', 'owner', 'leaseExpiresAt', 'inactiveAt', 'observedAt')\n    )\n    AND json_type(lease.proof_json, '$.container') = 'object'\n    AND (SELECT COUNT(*) FROM json_each(json_extract(lease.proof_json, '$.container'))) = 7\n    AND NOT EXISTS (\n      SELECT 1\n      FROM json_each(json_extract(lease.proof_json, '$.container'))\n      WHERE key NOT IN ('kind', 'id', 'name', 'imageDigest', 'labels', 'globalLabelResult', 'observedAt')\n    )\n    AND json_type(lease.proof_json, '$.staging') = 'object'\n    AND (\n      (\n        json_extract(lease.proof_json, '$.staging.kind') = 'absent'\n        AND (SELECT COUNT(*) FROM json_each(json_extract(lease.proof_json, '$.staging'))) = 2\n        AND NOT EXISTS (\n          SELECT 1\n          FROM json_each(json_extract(lease.proof_json, '$.staging'))\n          WHERE key NOT IN ('kind', 'path')\n        )\n        AND json_type(lease.proof_json, '$.staging.path') = 'null'\n        AND job.artifact_staging_path IS NULL\n        AND job.artifact_quarantine_path IS NULL\n      )\n      OR (\n        json_extract(lease.proof_json, '$.staging.kind') = 'present'\n        AND (SELECT COUNT(*) FROM json_each(json_extract(lease.proof_json, '$.staging'))) = 4\n        AND NOT EXISTS (\n          SELECT 1\n          FROM json_each(json_extract(lease.proof_json, '$.staging'))\n          WHERE key NOT IN ('kind', 'path', 'sha256', 'size')\n        )\n        AND json_type(lease.proof_json, '$.staging.path') = 'text'\n        AND json_extract(lease.proof_json, '$.staging.path') = 'staging/' || job.job_id\n        AND json_extract(lease.proof_json, '$.staging.path') NOT GLOB '*\\\\*'\n        AND json_extract(lease.proof_json, '$.staging.path') NOT GLOB '*..*'\n        AND job.artifact_staging_path IS NULL\n        AND job.artifact_quarantine_path = 'quarantine/' || job.job_id\n        AND job.publish_state = 'quarantined'\n        AND (\n          json_type(lease.proof_json, '$.staging.sha256') = 'null'\n          OR (\n            json_type(lease.proof_json, '$.staging.sha256') = 'text'\n            AND length(json_extract(lease.proof_json, '$.staging.sha256')) = 64\n            AND json_extract(lease.proof_json, '$.staging.sha256') NOT GLOB '*[^0-9a-f]*'\n          )\n        )\n        AND (\n          json_type(lease.proof_json, '$.staging.size') = 'null'\n          OR (\n            json_type(lease.proof_json, '$.staging.size') = 'integer'\n            AND json_extract(lease.proof_json, '$.staging.size') >= 0\n          )\n        )\n        AND (\n          (\n            json_type(lease.proof_json, '$.staging.sha256') = 'text'\n            AND json_type(lease.proof_json, '$.staging.size') = 'integer'\n            AND json_extract(lease.proof_json, '$.staging.sha256') = job.artifact_sha256\n            AND json_extract(lease.proof_json, '$.staging.size') = job.artifact_size\n          )\n          OR (\n            job.artifact_sha256 IS NULL\n            AND job.artifact_size IS NULL\n            AND job.artifact_mtime IS NULL\n            AND job.checksum_path IS NULL\n            AND job.checksum_sha256 IS NULL\n            AND job.manifest_path IS NULL\n            AND job.manifest_sha256 IS NULL\n            AND job.verification_path IS NULL\n            AND job.verification_sha256 IS NULL\n          )\n        )\n      )\n      OR (\n        json_extract(lease.proof_json, '$.staging.kind') = 'physical-present'\n        AND (SELECT COUNT(*) FROM json_each(json_extract(lease.proof_json, '$.staging'))) = 5\n        AND NOT EXISTS (\n          SELECT 1\n          FROM json_each(json_extract(lease.proof_json, '$.staging'))\n          WHERE key NOT IN ('kind', 'path', 'sha256', 'size', 'observedAt')\n        )\n        AND json_type(lease.proof_json, '$.staging.path') = 'text'\n        AND json_extract(lease.proof_json, '$.staging.path') = 'staging/' || job.job_id\n        AND json_extract(lease.proof_json, '$.staging.path') NOT GLOB '*\\\\*'\n        AND json_extract(lease.proof_json, '$.staging.path') NOT GLOB '*..*'\n        AND json_type(lease.proof_json, '$.staging.sha256') = 'null'\n        AND json_type(lease.proof_json, '$.staging.size') = 'null'\n        AND strftime('%Y-%m-%dT%H:%M:%fZ', json_extract(lease.proof_json, '$.staging.observedAt')) = json_extract(lease.proof_json, '$.staging.observedAt')\n        AND job.artifact_staging_path IS NULL\n        AND job.artifact_quarantine_path = 'quarantine/' || job.job_id\n        AND job.publish_state = 'quarantined'\n        AND job.artifact_sha256 IS NULL\n        AND job.artifact_size IS NULL\n        AND job.artifact_mtime IS NULL\n        AND job.checksum_path IS NULL\n        AND job.checksum_sha256 IS NULL\n        AND job.manifest_path IS NULL\n        AND job.manifest_sha256 IS NULL\n        AND job.verification_path IS NULL\n        AND job.verification_sha256 IS NULL\n      )\n    )\n    AND json_type(lease.proof_json, '$.logs') = 'object'\n    AND (SELECT COUNT(*) FROM json_each(json_extract(lease.proof_json, '$.logs'))) = 4\n    AND NOT EXISTS (\n      SELECT 1\n      FROM json_each(json_extract(lease.proof_json, '$.logs'))\n      WHERE key NOT IN ('runner', 'docker', 'verifiedAt', 'generationIdentity')\n    )\n    AND json_extract(lease.proof_json, '$.logs.runner') IN ('absent', 'sealed')\n    AND json_extract(lease.proof_json, '$.logs.docker') IN ('absent', 'sealed')\n    AND strftime('%Y-%m-%dT%H:%M:%fZ', json_extract(lease.proof_json, '$.logs.verifiedAt')) = json_extract(lease.proof_json, '$.logs.verifiedAt')\n    AND json_extract(lease.proof_json, '$.logs.verifiedAt') <= lease.admitted_at\n    AND json_type(lease.proof_json, '$.logs.generationIdentity') = 'object'\n    AND (SELECT COUNT(*) FROM json_each(json_extract(lease.proof_json, '$.logs.generationIdentity'))) = 2\n    AND NOT EXISTS (\n      SELECT 1\n      FROM json_each(json_extract(lease.proof_json, '$.logs.generationIdentity'))\n      WHERE key NOT IN ('runner', 'docker') OR json_type(value) IS NOT 'array' OR json_array_length(value) > 128\n    )\n    AND json_array_length(lease.proof_json, '$.logs.generationIdentity.runner')\n      + json_array_length(lease.proof_json, '$.logs.generationIdentity.docker') <= 128\n    AND (\n      SELECT COUNT(*)\n      FROM job_log_generations AS persisted\n      WHERE persisted.job_id = job.job_id\n    ) <= 128\n    AND (\n      SELECT COUNT(*)\n      FROM job_events AS persisted_event\n      WHERE persisted_event.job_id = job.job_id\n        AND persisted_event.stream IS NOT NULL\n    ) <= 8192\n    AND NOT EXISTS (\n      SELECT 1\n      FROM json_each(json_extract(lease.proof_json, '$.logs.generationIdentity.runner')) AS generation\n      WHERE json_type(generation.value) IS NOT 'object'\n         OR (SELECT COUNT(*) FROM json_each(generation.value)) <> 3\n         OR EXISTS (SELECT 1 FROM json_each(generation.value) WHERE key NOT IN ('generation', 'path', 'startedAt'))\n         OR json_type(generation.value, '$.generation') IS NOT 'integer'\n         OR json_extract(generation.value, '$.generation') < 0\n         OR CAST(generation.key AS INTEGER) <> json_extract(generation.value, '$.generation')\n         OR json_type(generation.value, '$.path') IS NOT 'text'\n         OR json_extract(generation.value, '$.path') NOT GLOB 'logs/*'\n         OR json_extract(generation.value, '$.path') GLOB '*\\\\*'\n         OR json_extract(generation.value, '$.path') GLOB '*..*'\n         OR strftime('%Y-%m-%dT%H:%M:%fZ', json_extract(generation.value, '$.startedAt')) IS NOT json_extract(generation.value, '$.startedAt')\n         OR json_extract(generation.value, '$.startedAt') > json_extract(lease.proof_json, '$.logs.verifiedAt')\n    )\n    AND NOT EXISTS (\n      SELECT 1\n      FROM json_each(json_extract(lease.proof_json, '$.logs.generationIdentity.docker')) AS generation\n      WHERE json_type(generation.value) IS NOT 'object'\n         OR (SELECT COUNT(*) FROM json_each(generation.value)) <> 3\n         OR EXISTS (SELECT 1 FROM json_each(generation.value) WHERE key NOT IN ('generation', 'path', 'startedAt'))\n         OR json_type(generation.value, '$.generation') IS NOT 'integer'\n         OR json_extract(generation.value, '$.generation') < 0\n         OR CAST(generation.key AS INTEGER) <> json_extract(generation.value, '$.generation')\n         OR json_type(generation.value, '$.path') IS NOT 'text'\n         OR json_extract(generation.value, '$.path') NOT GLOB 'logs/*'\n         OR json_extract(generation.value, '$.path') GLOB '*\\\\*'\n         OR json_extract(generation.value, '$.path') GLOB '*..*'\n         OR strftime('%Y-%m-%dT%H:%M:%fZ', json_extract(generation.value, '$.startedAt')) IS NOT json_extract(generation.value, '$.startedAt')\n         OR json_extract(generation.value, '$.startedAt') > json_extract(lease.proof_json, '$.logs.verifiedAt')\n    )\n\n    -- A sealed/absent state is a claim about the durable generation table, not\n    -- merely about the JSON array. Bind both directions and require the exact\n    -- immutable generation identity, sealed metadata, and contiguous byte\n    -- coverage that runtime cleanup uses before accepting a sealed stream.\n    AND (\n      (\n        json_extract(lease.proof_json, '$.logs.runner') = 'absent'\n        AND json_array_length(lease.proof_json, '$.logs.generationIdentity.runner') = 0\n        AND NOT EXISTS (\n          SELECT 1 FROM job_log_generations AS persisted\n          WHERE persisted.job_id = job.job_id AND persisted.stream = 'runner'\n        )\n      )\n      OR (\n        json_extract(lease.proof_json, '$.logs.runner') = 'sealed'\n        AND json_array_length(lease.proof_json, '$.logs.generationIdentity.runner') > 0\n        AND (SELECT COUNT(*) FROM job_log_generations AS persisted WHERE persisted.job_id = job.job_id AND persisted.stream = 'runner') = json_array_length(lease.proof_json, '$.logs.generationIdentity.runner')\n      )\n    )\n    AND (\n      (\n        json_extract(lease.proof_json, '$.logs.docker') = 'absent'\n        AND json_array_length(lease.proof_json, '$.logs.generationIdentity.docker') = 0\n        AND NOT EXISTS (\n          SELECT 1 FROM job_log_generations AS persisted\n          WHERE persisted.job_id = job.job_id AND persisted.stream = 'docker'\n        )\n      )\n      OR (\n        json_extract(lease.proof_json, '$.logs.docker') = 'sealed'\n        AND json_array_length(lease.proof_json, '$.logs.generationIdentity.docker') > 0\n        AND (SELECT COUNT(*) FROM job_log_generations AS persisted WHERE persisted.job_id = job.job_id AND persisted.stream = 'docker') = json_array_length(lease.proof_json, '$.logs.generationIdentity.docker')\n      )\n    )\n    AND (\n      (json_extract(lease.proof_json, '$.logs.runner') = 'absent'\n       AND json_extract(lease.proof_json, '$.logs.docker') = 'absent')\n      OR (json_extract(lease.proof_json, '$.logs.runner') = 'sealed'\n       AND json_extract(lease.proof_json, '$.logs.docker') = 'sealed')\n    )\n    AND NOT EXISTS (\n      SELECT 1\n      FROM json_each(json_extract(lease.proof_json, '$.logs.generationIdentity.runner')) AS identity\n      WHERE NOT EXISTS (\n        SELECT 1\n        FROM job_log_generations AS persisted\n        WHERE persisted.job_id = job.job_id\n          AND persisted.stream = 'runner'\n          AND persisted.generation = json_extract(identity.value, '$.generation')\n          AND persisted.path = json_extract(identity.value, '$.path')\n          AND persisted.started_at = json_extract(identity.value, '$.startedAt')\n      )\n    )\n    AND NOT EXISTS (\n      SELECT 1\n      FROM job_log_generations AS persisted\n      WHERE persisted.job_id = job.job_id\n        AND persisted.stream = 'runner'\n        AND NOT EXISTS (\n          SELECT 1\n          FROM json_each(json_extract(lease.proof_json, '$.logs.generationIdentity.runner')) AS identity\n          WHERE persisted.generation = json_extract(identity.value, '$.generation')\n            AND persisted.path = json_extract(identity.value, '$.path')\n            AND persisted.started_at = json_extract(identity.value, '$.startedAt')\n        )\n    )\n    AND NOT EXISTS (\n      SELECT 1\n      FROM json_each(json_extract(lease.proof_json, '$.logs.generationIdentity.docker')) AS identity\n      WHERE NOT EXISTS (\n        SELECT 1\n        FROM job_log_generations AS persisted\n        WHERE persisted.job_id = job.job_id\n          AND persisted.stream = 'docker'\n          AND persisted.generation = json_extract(identity.value, '$.generation')\n          AND persisted.path = json_extract(identity.value, '$.path')\n          AND persisted.started_at = json_extract(identity.value, '$.startedAt')\n      )\n    )\n    AND NOT EXISTS (\n      SELECT 1\n      FROM job_log_generations AS persisted\n      WHERE persisted.job_id = job.job_id\n        AND persisted.stream = 'docker'\n        AND NOT EXISTS (\n          SELECT 1\n          FROM json_each(json_extract(lease.proof_json, '$.logs.generationIdentity.docker')) AS identity\n          WHERE persisted.generation = json_extract(identity.value, '$.generation')\n            AND persisted.path = json_extract(identity.value, '$.path')\n            AND persisted.started_at = json_extract(identity.value, '$.startedAt')\n        )\n    )\n    AND NOT EXISTS (\n      SELECT 1\n      FROM job_log_generations AS persisted\n      WHERE persisted.job_id = job.job_id\n        AND persisted.stream IN ('runner', 'docker')\n        AND (\n          typeof(persisted.generation) <> 'integer'\n          OR persisted.generation < 0\n          OR typeof(persisted.size_bytes) <> 'integer'\n          OR persisted.size_bytes < 0\n          OR persisted.size_bytes > 9007199254740991\n          OR persisted.sealed_at IS NULL\n          OR strftime('%Y-%m-%dT%H:%M:%fZ', persisted.started_at) IS NOT persisted.started_at\n          OR strftime('%Y-%m-%dT%H:%M:%fZ', persisted.sealed_at) IS NOT persisted.sealed_at\n          OR persisted.sealed_at < persisted.started_at\n          OR persisted.sealed_at > json_extract(lease.proof_json, '$.logs.verifiedAt')\n          OR length(persisted.sha256) <> 64\n          OR persisted.sha256 GLOB '*[^0-9a-f]*'\n        )\n    )\n    AND NOT EXISTS (\n      SELECT 1\n      FROM job_log_generations AS persisted\n      WHERE persisted.job_id = job.job_id\n        AND persisted.stream = 'runner'\n        AND (\n          (persisted.size_bytes = 0 AND EXISTS (\n            SELECT 1 FROM job_events AS event\n            WHERE event.job_id = job.job_id AND event.stream = 'runner' AND event.file_generation = persisted.generation\n          ))\n          OR (persisted.size_bytes > 0 AND (\n            NOT EXISTS (\n              SELECT 1 FROM job_events AS event\n              WHERE event.job_id = job.job_id AND event.stream = 'runner' AND event.file_generation = persisted.generation\n            )\n            OR (SELECT SUM(event.byte_length) FROM job_events AS event WHERE event.job_id = job.job_id AND event.stream = 'runner' AND event.file_generation = persisted.generation) <> persisted.size_bytes\n            OR (SELECT MIN(event.byte_offset) FROM job_events AS event WHERE event.job_id = job.job_id AND event.stream = 'runner' AND event.file_generation = persisted.generation) <> 0\n            OR (SELECT MAX(event.byte_offset + event.byte_length) FROM job_events AS event WHERE event.job_id = job.job_id AND event.stream = 'runner' AND event.file_generation = persisted.generation) <> persisted.size_bytes\n            OR EXISTS (\n              SELECT 1 FROM job_events AS event\n              WHERE event.job_id = job.job_id AND event.stream = 'runner' AND event.file_generation = persisted.generation\n                AND (\n                  typeof(event.seq) <> 'integer' OR event.seq >= completion.seq\n                  OR typeof(event.byte_offset) <> 'integer' OR event.byte_offset < 0\n                  OR typeof(event.byte_length) <> 'integer' OR event.byte_length <= 0\n                  OR event.byte_offset + event.byte_length > persisted.size_bytes\n                  OR event.partial IS NULL OR event.partial NOT IN (0, 1)\n                  OR event.event_type NOT IN ('log', 'log_orphan_tail', 'log-truncated')\n                  OR strftime('%Y-%m-%dT%H:%M:%fZ', event.at) IS NOT event.at\n                  OR event.at < persisted.started_at OR event.at > persisted.sealed_at\n                )\n            )\n            OR EXISTS (\n              SELECT 1 FROM job_events AS event\n              WHERE event.job_id = job.job_id AND event.stream = 'runner' AND event.file_generation = persisted.generation\n              GROUP BY event.byte_offset\n              HAVING COUNT(*) > 1\n            )\n            OR EXISTS (\n              SELECT 1 FROM job_events AS event\n              WHERE event.job_id = job.job_id AND event.stream = 'runner' AND event.file_generation = persisted.generation\n                AND event.byte_offset <> COALESCE((SELECT SUM(previous.byte_length) FROM job_events AS previous WHERE previous.job_id = event.job_id AND previous.stream = event.stream AND previous.file_generation = event.file_generation AND previous.byte_offset < event.byte_offset), 0)\n            )\n          ))\n        )\n    )\n    AND NOT EXISTS (\n      SELECT 1\n      FROM job_log_generations AS persisted\n      WHERE persisted.job_id = job.job_id\n        AND persisted.stream = 'docker'\n        AND (\n          (persisted.size_bytes = 0 AND EXISTS (\n            SELECT 1 FROM job_events AS event\n            WHERE event.job_id = job.job_id AND event.stream = 'docker' AND event.file_generation = persisted.generation\n          ))\n          OR (persisted.size_bytes > 0 AND (\n            NOT EXISTS (\n              SELECT 1 FROM job_events AS event\n              WHERE event.job_id = job.job_id AND event.stream = 'docker' AND event.file_generation = persisted.generation\n            )\n            OR (SELECT SUM(event.byte_length) FROM job_events AS event WHERE event.job_id = job.job_id AND event.stream = 'docker' AND event.file_generation = persisted.generation) <> persisted.size_bytes\n            OR (SELECT MIN(event.byte_offset) FROM job_events AS event WHERE event.job_id = job.job_id AND event.stream = 'docker' AND event.file_generation = persisted.generation) <> 0\n            OR (SELECT MAX(event.byte_offset + event.byte_length) FROM job_events AS event WHERE event.job_id = job.job_id AND event.stream = 'docker' AND event.file_generation = persisted.generation) <> persisted.size_bytes\n            OR EXISTS (\n              SELECT 1 FROM job_events AS event\n              WHERE event.job_id = job.job_id AND event.stream = 'docker' AND event.file_generation = persisted.generation\n                AND (\n                  typeof(event.seq) <> 'integer' OR event.seq >= completion.seq\n                  OR typeof(event.byte_offset) <> 'integer' OR event.byte_offset < 0\n                  OR typeof(event.byte_length) <> 'integer' OR event.byte_length <= 0\n                  OR event.byte_offset + event.byte_length > persisted.size_bytes\n                  OR event.partial IS NULL OR event.partial NOT IN (0, 1)\n                  OR event.event_type NOT IN ('log', 'log_orphan_tail', 'log-truncated')\n                  OR strftime('%Y-%m-%dT%H:%M:%fZ', event.at) IS NOT event.at\n                  OR event.at < persisted.started_at OR event.at > persisted.sealed_at\n                )\n            )\n            OR EXISTS (\n              SELECT 1 FROM job_events AS event\n              WHERE event.job_id = job.job_id AND event.stream = 'docker' AND event.file_generation = persisted.generation\n              GROUP BY event.byte_offset\n              HAVING COUNT(*) > 1\n            )\n            OR EXISTS (\n              SELECT 1 FROM job_events AS event\n              WHERE event.job_id = job.job_id AND event.stream = 'docker' AND event.file_generation = persisted.generation\n                AND event.byte_offset <> COALESCE((SELECT SUM(previous.byte_length) FROM job_events AS previous WHERE previous.job_id = event.job_id AND previous.stream = event.stream AND previous.file_generation = event.file_generation AND previous.byte_offset < event.byte_offset), 0)\n            )\n          ))\n        )\n    )\n    AND json_extract(completion.payload_json, '$.postcondition.logs.runner') = json_extract(lease.proof_json, '$.logs.runner')\n    AND json_extract(completion.payload_json, '$.postcondition.logs.docker') = json_extract(lease.proof_json, '$.logs.docker')\n    AND json_extract(lease.proof_json, '$.state') = lease.stale_state\n    AND json_extract(lease.proof_json, '$.runner.unit') = lease.stale_runner_unit\n    AND json_extract(lease.proof_json, '$.runner.owner') = lease.stale_runner_owner\n    AND json_extract(lease.proof_json, '$.runner.leaseExpiresAt') = lease.stale_runner_lease_expires_at\n    AND json_extract(lease.proof_json, '$.container.kind') = 'present'\n    AND json_extract(lease.proof_json, '$.blocker') = 'container'\n    AND json_extract(lease.proof_json, '$.container.id') = lease.stale_container_id\n    AND json_extract(lease.proof_json, '$.container.name') = lease.stale_container_name\n    AND length(json_extract(lease.proof_json, '$.container.imageDigest')) = 64\n    AND json_extract(lease.proof_json, '$.container.imageDigest') NOT GLOB '*[^0-9a-f]*'\n    AND json_extract(lease.proof_json, '$.container.labels.\"org.osi.image-builder.job-id\"') = job.job_id\n    AND json_extract(lease.proof_json, '$.container.labels.\"org.osi.image-builder.manifest-sha\"') = job.target_manifest_sha256\n    AND json_extract(lease.stale_container_labels_json, '$.\"org.osi.image-builder.job-id\"') = job.job_id\n    AND json_extract(lease.stale_container_labels_json, '$.\"org.osi.image-builder.manifest-sha\"') = job.target_manifest_sha256\n    AND (SELECT COUNT(*) FROM json_each(json_extract(lease.proof_json, '$.container.labels'))) = 2\n    AND (SELECT COUNT(*) FROM json_each(lease.stale_container_labels_json)) = 2\n    AND json_extract(lease.proof_json, '$.container.globalLabelResult') IN ('single-exact-match', 'no-match')\n    AND strftime('%Y-%m-%dT%H:%M:%fZ', json_extract(lease.proof_json, '$.container.observedAt')) = json_extract(lease.proof_json, '$.container.observedAt')\n    AND json_extract(lease.proof_json, '$.container.observedAt') <= lease.admitted_at\n\n    -- Bind runner inactivity to both the immutable snapshot and completion\n    -- event, with the canonical chronology requested by the hand-back proof.\n    AND strftime('%Y-%m-%dT%H:%M:%fZ', json_extract(lease.proof_json, '$.runner.inactiveAt')) = json_extract(lease.proof_json, '$.runner.inactiveAt')\n    AND strftime('%Y-%m-%dT%H:%M:%fZ', json_extract(lease.proof_json, '$.runner.observedAt')) = json_extract(lease.proof_json, '$.runner.observedAt')\n    AND lease.stale_runner_lease_expires_at <= json_extract(lease.proof_json, '$.runner.inactiveAt')\n    AND json_extract(lease.proof_json, '$.runner.inactiveAt') <= json_extract(lease.proof_json, '$.runner.observedAt')\n    AND json_extract(lease.proof_json, '$.runner.observedAt') <= lease.admitted_at\n    AND json_extract(lease.proof_json, '$.runner.observedAt') <= lease.complete_at\n    AND (\n      json_extract(lease.proof_json, '$.staging.kind') <> 'physical-present'\n      OR json_extract(lease.proof_json, '$.staging.observedAt') <= lease.admitted_at\n    )\n    AND json_valid(completion.payload_json) = 1\n    AND json_type(completion.payload_json) = 'object'\n    AND (SELECT COUNT(*) FROM json_each(completion.payload_json)) = 3\n    AND NOT EXISTS (\n      SELECT 1\n      FROM json_each(completion.payload_json)\n      WHERE key NOT IN ('admissionId', 'evidencePath', 'postcondition')\n    )\n    AND json_type(completion.payload_json, '$.postcondition') = 'object'\n    AND (SELECT COUNT(*) FROM json_each(json_extract(completion.payload_json, '$.postcondition'))) = 7\n    AND NOT EXISTS (\n      SELECT 1\n      FROM json_each(json_extract(completion.payload_json, '$.postcondition'))\n      WHERE key NOT IN ('runner', 'state', 'container', 'staging', 'logs', 'egress', 'blocker')\n    )\n    AND json_type(completion.payload_json, '$.postcondition.runner') = 'object'\n    AND (SELECT COUNT(*) FROM json_each(json_extract(completion.payload_json, '$.postcondition.runner'))) = 5\n    AND NOT EXISTS (\n      SELECT 1\n      FROM json_each(json_extract(completion.payload_json, '$.postcondition.runner'))\n      WHERE key NOT IN ('unit', 'owner', 'leaseExpiresAt', 'inactiveAt', 'observedAt')\n    )\n    AND json_type(completion.payload_json, '$.postcondition.container') = 'object'\n    AND (\n      (\n        json_extract(completion.payload_json, '$.postcondition.container.kind') = 'already-absent'\n        AND (SELECT COUNT(*) FROM json_each(json_extract(completion.payload_json, '$.postcondition.container'))) = 9\n        AND NOT EXISTS (\n          SELECT 1\n          FROM json_each(json_extract(completion.payload_json, '$.postcondition.container'))\n          WHERE key NOT IN ('kind', 'id', 'name', 'imageDigest', 'labels', 'exactIdAbsent', 'dockerAction', 'globalLabelResult', 'observedAt')\n        )\n        AND json_extract(completion.payload_json, '$.postcondition.container.dockerAction') = 'none'\n      )\n      OR (\n        json_extract(completion.payload_json, '$.postcondition.container.kind') = 'removed'\n        AND (SELECT COUNT(*) FROM json_each(json_extract(completion.payload_json, '$.postcondition.container'))) = 10\n        AND NOT EXISTS (\n          SELECT 1\n          FROM json_each(json_extract(completion.payload_json, '$.postcondition.container'))\n          WHERE key NOT IN ('kind', 'id', 'name', 'imageDigest', 'labels', 'exactIdAbsent', 'globalLabelResult', 'stoppedAt', 'removedAt', 'observedAt')\n        )\n        AND strftime('%Y-%m-%dT%H:%M:%fZ', json_extract(completion.payload_json, '$.postcondition.container.stoppedAt')) = json_extract(completion.payload_json, '$.postcondition.container.stoppedAt')\n        AND strftime('%Y-%m-%dT%H:%M:%fZ', json_extract(completion.payload_json, '$.postcondition.container.removedAt')) = json_extract(completion.payload_json, '$.postcondition.container.removedAt')\n        AND json_extract(lease.proof_json, '$.container.observedAt') <= json_extract(completion.payload_json, '$.postcondition.container.stoppedAt')\n        AND json_extract(completion.payload_json, '$.postcondition.container.stoppedAt') <= json_extract(completion.payload_json, '$.postcondition.container.removedAt')\n        AND json_extract(completion.payload_json, '$.postcondition.container.removedAt') <= json_extract(completion.payload_json, '$.postcondition.container.observedAt')\n      )\n    )\n    AND json_type(completion.payload_json, '$.postcondition.logs') = 'object'\n    AND (SELECT COUNT(*) FROM json_each(json_extract(completion.payload_json, '$.postcondition.logs'))) = 3\n    AND NOT EXISTS (\n      SELECT 1\n      FROM json_each(json_extract(completion.payload_json, '$.postcondition.logs'))\n      WHERE key NOT IN ('runner', 'docker', 'verifiedAt')\n    )\n    AND json_extract(completion.payload_json, '$.postcondition.logs.runner') IN ('absent', 'sealed')\n    AND json_extract(completion.payload_json, '$.postcondition.logs.docker') IN ('absent', 'sealed')\n    AND strftime('%Y-%m-%dT%H:%M:%fZ', json_extract(completion.payload_json, '$.postcondition.logs.verifiedAt')) = json_extract(completion.payload_json, '$.postcondition.logs.verifiedAt')\n    AND json_extract(completion.payload_json, '$.postcondition.logs.verifiedAt') <= lease.complete_at\n    AND json_type(completion.payload_json, '$.postcondition.staging') = 'object'\n    AND (\n      (\n        json_extract(completion.payload_json, '$.postcondition.staging.kind') = 'absent'\n        AND (SELECT COUNT(*) FROM json_each(json_extract(completion.payload_json, '$.postcondition.staging'))) = 5\n        AND NOT EXISTS (\n          SELECT 1\n          FROM json_each(json_extract(completion.payload_json, '$.postcondition.staging'))\n          WHERE key NOT IN ('kind', 'path', 'sourcePath', 'sourceAbsent', 'verifiedAt')\n        )\n        AND json_extract(completion.payload_json, '$.postcondition.staging.path') IS NULL\n        AND json_extract(completion.payload_json, '$.postcondition.staging.sourcePath') = 'staging/' || job.job_id\n        AND json_extract(completion.payload_json, '$.postcondition.staging.sourceAbsent') = 1\n      )\n      OR (\n        json_extract(completion.payload_json, '$.postcondition.staging.kind') = 'quarantined'\n        AND (SELECT COUNT(*) FROM json_each(json_extract(completion.payload_json, '$.postcondition.staging'))) = 8\n        AND NOT EXISTS (\n          SELECT 1\n          FROM json_each(json_extract(completion.payload_json, '$.postcondition.staging'))\n          WHERE key NOT IN ('kind', 'sourcePath', 'destinationPath', 'sourceAbsent', 'destinationPresent', 'sha256', 'size', 'verifiedAt')\n        )\n        AND json_extract(completion.payload_json, '$.postcondition.staging.sourcePath') = 'staging/' || job.job_id\n        AND json_extract(completion.payload_json, '$.postcondition.staging.destinationPath') = 'quarantine/' || job.job_id\n        AND json_extract(completion.payload_json, '$.postcondition.staging.sourceAbsent') = 1\n        AND json_extract(completion.payload_json, '$.postcondition.staging.destinationPresent') = 1\n        AND (json_type(completion.payload_json, '$.postcondition.staging.sha256') = 'null' OR (json_type(completion.payload_json, '$.postcondition.staging.sha256') = 'text' AND length(json_extract(completion.payload_json, '$.postcondition.staging.sha256')) = 64 AND json_extract(completion.payload_json, '$.postcondition.staging.sha256') NOT GLOB '*[^0-9a-f]*'))\n        AND (json_type(completion.payload_json, '$.postcondition.staging.size') = 'null' OR (json_type(completion.payload_json, '$.postcondition.staging.size') = 'integer' AND json_extract(completion.payload_json, '$.postcondition.staging.size') >= 0))\n      )\n    )\n    AND strftime('%Y-%m-%dT%H:%M:%fZ', json_extract(completion.payload_json, '$.postcondition.staging.verifiedAt')) = json_extract(completion.payload_json, '$.postcondition.staging.verifiedAt')\n    AND json_extract(completion.payload_json, '$.postcondition.staging.verifiedAt') <= lease.complete_at\n    AND (\n      (\n        json_extract(lease.proof_json, '$.staging.kind') = 'absent'\n        AND json_extract(completion.payload_json, '$.postcondition.staging.kind') = 'absent'\n        AND job.artifact_staging_path IS NULL\n        AND job.artifact_quarantine_path IS NULL\n      )\n      OR (\n        json_extract(lease.proof_json, '$.staging.kind') = 'present'\n        AND json_extract(completion.payload_json, '$.postcondition.staging.kind') = 'quarantined'\n        AND json_extract(completion.payload_json, '$.postcondition.staging.sourcePath') = json_extract(lease.proof_json, '$.staging.path')\n        AND json_extract(completion.payload_json, '$.postcondition.staging.sha256') IS json_extract(lease.proof_json, '$.staging.sha256')\n        AND json_extract(completion.payload_json, '$.postcondition.staging.size') IS json_extract(lease.proof_json, '$.staging.size')\n        AND job.artifact_staging_path IS NULL\n        AND job.artifact_quarantine_path = json_extract(completion.payload_json, '$.postcondition.staging.destinationPath')\n        AND job.publish_state = 'quarantined'\n      )\n      OR (\n        json_extract(lease.proof_json, '$.staging.kind') = 'physical-present'\n        AND json_extract(completion.payload_json, '$.postcondition.staging.kind') = 'quarantined'\n        AND json_extract(completion.payload_json, '$.postcondition.staging.sourcePath') = json_extract(lease.proof_json, '$.staging.path')\n        AND json_type(completion.payload_json, '$.postcondition.staging.sha256') = 'null'\n        AND json_type(completion.payload_json, '$.postcondition.staging.size') = 'null'\n        AND job.artifact_staging_path IS NULL\n        AND job.artifact_quarantine_path = json_extract(completion.payload_json, '$.postcondition.staging.destinationPath')\n        AND job.publish_state = 'quarantined'\n      )\n    )\n    AND json_extract(completion.payload_json, '$.admissionId') = lease.admission_id\n    AND json_extract(completion.payload_json, '$.evidencePath') = lease.completion_evidence_path\n    AND json_extract(completion.payload_json, '$.postcondition.blocker') = 'none'\n    AND json_extract(completion.payload_json, '$.postcondition.state') = lease.stale_state\n    AND json_extract(completion.payload_json, '$.postcondition.runner.unit') = lease.stale_runner_unit\n    AND json_extract(completion.payload_json, '$.postcondition.runner.owner') = lease.stale_runner_owner\n    AND json_extract(completion.payload_json, '$.postcondition.runner.leaseExpiresAt') = lease.stale_runner_lease_expires_at\n    AND json_extract(completion.payload_json, '$.postcondition.runner.inactiveAt') = json_extract(lease.proof_json, '$.runner.inactiveAt')\n    AND json_extract(completion.payload_json, '$.postcondition.runner.observedAt') = json_extract(lease.proof_json, '$.runner.observedAt')\n    AND strftime('%Y-%m-%dT%H:%M:%fZ', json_extract(completion.payload_json, '$.postcondition.runner.inactiveAt')) = json_extract(completion.payload_json, '$.postcondition.runner.inactiveAt')\n    AND strftime('%Y-%m-%dT%H:%M:%fZ', json_extract(completion.payload_json, '$.postcondition.runner.observedAt')) = json_extract(completion.payload_json, '$.postcondition.runner.observedAt')\n    AND json_extract(completion.payload_json, '$.postcondition.runner.inactiveAt') <= json_extract(completion.payload_json, '$.postcondition.runner.observedAt')\n    AND json_extract(completion.payload_json, '$.postcondition.runner.observedAt') <= lease.complete_at\n\n    -- Bind the completion observation to the exact snapshot container. Both\n    -- removal outcomes prove exact-ID and global-label absence.\n    AND json_extract(completion.payload_json, '$.postcondition.container.id') = lease.stale_container_id\n    AND json_extract(completion.payload_json, '$.postcondition.container.name') = lease.stale_container_name\n    AND json_extract(completion.payload_json, '$.postcondition.container.imageDigest') = json_extract(lease.proof_json, '$.container.imageDigest')\n    AND json_extract(completion.payload_json, '$.postcondition.container.labels.\"org.osi.image-builder.job-id\"') = job.job_id\n    AND json_extract(completion.payload_json, '$.postcondition.container.labels.\"org.osi.image-builder.manifest-sha\"') = job.target_manifest_sha256\n    AND (SELECT COUNT(*) FROM json_each(json_extract(completion.payload_json, '$.postcondition.container.labels'))) = 2\n    AND json_extract(completion.payload_json, '$.postcondition.container.exactIdAbsent') = 1\n    AND json_extract(completion.payload_json, '$.postcondition.container.globalLabelResult') = 'no-match'\n    AND strftime('%Y-%m-%dT%H:%M:%fZ', json_extract(lease.proof_json, '$.container.observedAt')) = json_extract(lease.proof_json, '$.container.observedAt')\n    AND strftime('%Y-%m-%dT%H:%M:%fZ', json_extract(completion.payload_json, '$.postcondition.container.observedAt')) = json_extract(completion.payload_json, '$.postcondition.container.observedAt')\n    AND json_extract(lease.proof_json, '$.container.observedAt') <= json_extract(completion.payload_json, '$.postcondition.container.observedAt')\n    AND json_extract(completion.payload_json, '$.postcondition.container.observedAt') <= lease.complete_at\n\n    -- Dependency-egress resources are a separate ownership domain. Missing or\n    -- partial proof is not equivalent to absence.\n    AND json_type(completion.payload_json, '$.postcondition.egress') = 'object'\n    AND (SELECT COUNT(*) FROM json_each(json_extract(completion.payload_json, '$.postcondition.egress'))) = 4\n    AND NOT EXISTS (\n      SELECT 1\n      FROM json_each(json_extract(completion.payload_json, '$.postcondition.egress'))\n      WHERE key NOT IN ('persistedDocker', 'discoveredDocker', 'credentials', 'globalLabelResult')\n    )\n    AND json_type(completion.payload_json, '$.postcondition.egress.persistedDocker') = 'null'\n    AND json_type(completion.payload_json, '$.postcondition.egress.discoveredDocker') = 'array'\n    AND json_array_length(completion.payload_json, '$.postcondition.egress.discoveredDocker') = 0\n    AND json_type(completion.payload_json, '$.postcondition.egress.credentials') = 'array'\n    AND json_array_length(completion.payload_json, '$.postcondition.egress.credentials') = 0\n    AND json_extract(completion.payload_json, '$.postcondition.egress.globalLabelResult') = 'no-match'\n\n    -- Hand-back is the terminal transition for this exact admission.\n    AND completion.seq < handback.seq\n    AND json_valid(handback.payload_json) = 1\n    AND json_extract(handback.payload_json, '$.admissionId') = lease.admission_id\n    AND json_extract(handback.payload_json, '$.state') = 'interrupted'\n),\nsingle_candidate AS (\n  SELECT job_id, MIN(admission_id) AS admission_id, MIN(complete_at) AS complete_at\n  FROM qualified_candidate\n  GROUP BY job_id\n  HAVING COUNT(*) = 1\n)\nSELECT job_id, admission_id, complete_at FROM single_candidate;";

export function migration021PostRecoveryQuery(): string {
  return MIGRATION_021_STRONG_RECOVERY_QUERY
    .replace("AND job.runner_finished_at IS NULL", "AND job.runner_finished_at = lease.complete_at")
    .replace("AND job.runner_lease_owner IS NOT NULL", "AND job.runner_lease_owner IS NULL")
    .replace("AND job.runner_lease_expires_at IS NOT NULL", "AND lease.stale_runner_lease_expires_at IS NOT NULL")
    .replace("AND strftime('%Y-%m-%dT%H:%M:%fZ', job.runner_lease_expires_at) = job.runner_lease_expires_at", "AND strftime('%Y-%m-%dT%H:%M:%fZ', lease.stale_runner_lease_expires_at) = lease.stale_runner_lease_expires_at")
    .replace("AND job.runner_started_at <= job.runner_lease_expires_at", "AND job.runner_started_at <= lease.stale_runner_lease_expires_at")
    .replace("AND job.runner_lease_expires_at < lease.admitted_at", "AND lease.stale_runner_lease_expires_at < lease.admitted_at")
    .replace("AND lease.stale_runner_owner = job.runner_lease_owner", "AND lease.stale_runner_owner IS NOT NULL")
    .replace("AND lease.stale_runner_lease_expires_at = job.runner_lease_expires_at", "AND lease.stale_runner_lease_expires_at IS NOT NULL");
}
