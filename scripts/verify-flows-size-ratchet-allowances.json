{
  "_comment": "Explicit growth allowances. Consumed-or-deleted: remove entry when offset by extraction.",
  "node_allowances": {
    "scoped-weather-zone-assign-router": {
      "delta": 1595,
      "reason": "Wave 3 zone/weather/calibration sync port (AgroLink 6800a421, fix(sync): scoped weather zone edits mirror to weather_station_zone_state (E1)): measured with verify-flows-size-ratchet's nodeSizes over both maintained profiles (byte-identical bcm2712/bcm2709): origin/main 4382 -> HEAD 5977 = +1595. The router previously ran its own BEGIN IMMEDIATE transaction over weather_station_zones directly, bypassing osi-device-commands/weather.js entirely, so it never touched weather_station_zone_state -- WEATHER_STATION_ZONES_REPLACED never published for a scoped GUI edit, and a later cloud REPLACE_WEATHER_STATION_ZONES at the un-bumped base version would pass its conflict check and silently discard the local edit. Added a get() helper and a priorZoneState read, then mirrors weather_station_zone_state inside the existing transaction: a first assignment inserts the state row at sync_version=0 so the AI defaults trigger's own UPDATE bumps it to 1; a subsequent edit increments the existing row directly. This node's now-unused new_node_ceilings entry (max_chars 4382, from the earlier scoped-access port commit E6) was removed in the same commit: verify-flows-size-ratchet.js's new_node_ceilings only governs a node absent at --base-ref, and this node already exists at origin/main, so rule 1 (node_allowances delta) is the mechanism that actually gates it -- the stale ceiling entry was dead weight and collided with this one on the node-key-uniqueness check."
    },
    "auth-register-func": {
      "delta": 251,
      "reason": "Wave 3 scoped-access port (AgroLink 4eb05522, feat(auth): complete scoped access phase A): measured on port/wave3-edge-scope with verify-flows-size-ratchet's nodeSizes over both maintained profiles (byte-identical bcm2712/bcm2709): origin/main 644 -> HEAD 895 = +251. Credentials now travel on msg._authwork instead of flow context (flow.set('register_username'/'register_password')), so a concurrent register/login on the same flow instance can no longer clobber each other's in-flight credentials -- the comment cross-referencing auth-login-func documents why."
    },
    "auth-login-func": {
      "delta": 54,
      "reason": "Wave 3 scoped-access port (AgroLink 4eb05522, feat(auth): complete scoped access phase A): measured on port/wave3-edge-scope with verify-flows-size-ratchet's nodeSizes over both maintained profiles (byte-identical bcm2712/bcm2709): origin/main 472 -> HEAD 526 = +54. Same msg._authwork fix as auth-register-func: credentials move off flow context onto msg."
    },
    "auth-process-result": {
      "delta": 1957,
      "reason": "Supersedes the previous entry (810). port/wave3-auth-flaggate (fix(auth): flag-gate the shared scope resolver across all auth nodes + hermetic flag-off verifier, follow-up to PR #200 / commit 2c4fa9a5f): measured with verify-flows-size-ratchet's nodeSizes over both maintained profiles (byte-identical bcm2712/bcm2709): origin/main 5695 -> HEAD 7652 = +1957. getAuthSecret() previously called osiLib.require('scope') unconditionally, violating the default-off contract (a lost osi-lib helper file would 500 every auth endpoint on a flag-off gateway). Fixed per the history-api-router-fn/zone-env-fn reference pattern: getAuthSecret() now checks a freshly-recomputed OSI_SCOPED_ACCESS flag first (recomputed locally rather than closing over any outer scopedOn, avoiding TDZ risk uniformly across every node regardless of its own structure) and only resolves osiLib inside the flag-on branch; the flag-off branch inlines the exact pre-refactor secret-resolution logic (same file paths, same ENOENT-quiet read/generate/write behavior), byte-equivalent to the pre-port route."
    },
    "auth-db-insert": {
      "delta": 1167,
      "reason": "Wave 3 scoped-access port (AgroLink 4eb05522, feat(auth): complete scoped access phase A): measured on port/wave3-edge-scope with verify-flows-size-ratchet's nodeSizes over both maintained profiles (byte-identical bcm2712/bcm2709): origin/main 1114 -> HEAD 2281 = +1167. Scoped mode (OSI_SCOPED_ACCESS=1) makes public self-registration a single atomic conditional INSERT ... SELECT ... WHERE NOT EXISTS (SELECT 1 FROM users WHERE role = 'admin') that both creates the very first account as admin and closes public registration thereafter (403 'Public registration is closed' once an admin exists), trusting only the INSERT's own row-change count (not a follow-up SELECT, which a same-username loser could misread as its own success); the run() helper now resolves that changes count; the close() catch was converted to a visible node.warn."
    },
    "sync-bootstrap-build": {
      "delta": 2483,
      "reason": "Supersedes the previous entry (5859, live identity restart sentinel (Option C Slice 1) provenance carried forward). Wave 3 durable-history-batch + installation-identity port (AgroLink 77d3c52a, renumbered migrations 0052-0053, port/wave3-edge-durable): the prior 5859 was already fully baked into origin/main's own measured size for this node (its base moved with it), so this pin is re-measured fresh rather than stacked. measured with verify-flows-size-ratchet's nodeSizes over both maintained profiles (byte-identical bcm2712/bcm2709): origin/main 39985 -> HEAD 42468 = +2483. This node now reads/creates/merges the local installation_identity singleton via osi-lib's installation helper before building the bootstrap payload, and advertises installationUuid/recoveryState/recoveryOperationUuid on gatewayIdentity plus the installation_recovery_v1 sync capability."
    },
    "sync-force-build": {
      "delta": 5786,
      "reason": "live identity restart sentinel (Option C Slice 1) (main, 2026-08) + valve-control growth: at merge 48c8ab47 this node measured byte-identical to origin/main (52929 chars) so main's identity-sentinel growth is carried by the origin/main baseline itself and the net allowance was 0. Re-measured on integration/valve-focused-portback-20260831 with verify-flows-size-ratchet's nodeSizes over both maintained profiles (byte-identical bcm2712/bcm2709): origin/main 52929 -> HEAD 58715 = +5786. feat/valve-control's copy is still byte-identical to origin/main's (52929), so the entire +5786 is this port-back's cherry-picked valve work, attributed per pick: valve_schedules in the bootstrap/force snapshot (+820), UPSERT_VALVE_SETTINGS applier + snapshot (+684), VALVE_ACTUATION_ARCHIVED terminal emission + backfill (+3147), P4-E1 review fixes (status derivation, backfill emit, LWW ties) (+1158), P5-3 i18n retranslation (-11), branding scrub (-12). Sums to +5786 exactly. [re-measured on port/sdi12-edge: origin/main 53121 -> HEAD 58907 = +5786, all of it the pre-existing feat/valve-control baseline; this port did not touch the node]"
    },
    "strega-reconciliation-monitor": {
      "delta": 5528,
      "reason": "FW-T1 fix wave: gate state derivation on 'uplink && uplink.recorded_at' so a never-uplinked valve's LEFT JOIN NULL row is not mistaken for a real observation (phantom OBSERVED_COMPLETE/OBSERVED_RUNNING) (+22); FW-T1b Gap A: query also selects d.updated_at AS state_updated_at, and the state-derivation block additionally gates on Date.parse(state_updated_at) >= Date.parse(commanded_at) (both Number.isFinite, epoch-millis compare) so a stale current_state paired with a fresh Class-A uplink is not mistaken for a real observation (+381); FW-T1c: RECONCILIATION_GRACE_SEC widened 300 -> 1800 (a full Class-A 600s uplink cycle plus margin on both the OPEN-delivery and CLOSE-confirmation legs) so a genuine run confirming 600-1500s after expected_close_at no longer times out to the litres-excluded STALE_NO_OBSERVATION (+1). FW-T1c R1 (review be3346a8): the late-promotion path (STALE_NO_OBSERVATION rescan + 6h window guard, +391) was reverted per review ruling - it re-opened unbounded phantom-litres exposure (MAJOR-1, any later valve activity within 6h revived a stale row regardless of which run it belonged to) and collided with osi-valve-control's runObserveTick dedupe model (MAJOR-2, race-dependent duplicate rows). The node is now byte-for-byte the a88b88dd (FW-T1b) version plus only the grace constant change; delta shrunk from 794 to 404, exact vs origin/main. Valve control Phase B Task 6 (+3809): the active-expectations SELECT LEFT JOINs devices for user_id/irrigation_zone_id (canLog guard, mirrors workers.js's runOnceTick); the OBSERVED_COMPLETE transition for trigger IN (on_valve_schedule, unexplained) now INSERTs an irrigation_events row (observed-span duration, event_uuid omitted so the sync trigger mints it, reason distinct from one_time_open) in the SAME transaction as the state UPDATE, so a crash cannot leave a completed actuation with no event or vice versa; ONCE runs are excluded (already logged at fire time by workers.js, avoiding a double count); the pre-existing empty catch on the close() call was also converted to a visible node.warn (touched-node silent-catch ratchet). cloud full-parity Task P3-E1 (+671, exact vs origin/main): loads osiLib once per tick and, inside the same per-expectation transaction as the state UPDATE, best-effort emits a VALVE_RUNTIME_CHANGED sync event via osi-valve-control's new emitRuntimeChanged() -- this is the code seam for the reconciliation-transition half of the ValveRuntime derived-state resource (the other half is the write-strega-expectation node, for new active_actuation rows). cloud full-parity Task P4-E1 (+650, exact vs origin/main): beside that same emitRuntimeChanged call, best-effort emits VALVE_ACTUATION_ARCHIVED via osi-valve-control's new emitActuationArchived() -- unconditional (it no-ops on its own for the non-terminal OBSERVED_RUNNING transition), so this is the terminal-transition half of the ValveActuation archive resource (the other half is cancel.js, for the CANCELLED transition). [re-measured on port/sdi12-edge: origin/main 2876 -> HEAD 8404 = +5528, all of it the pre-existing feat/valve-control baseline; this port did not touch the node]"
    },
    "merge-device-data": {
      "delta": 4372,
      "reason": "Measured on port/sdi12-edge with verify-flows-size-ratchet's nodeSizes over both maintained profiles (byte-identical bcm2712/bcm2709): origin/main 10392 -> branch base a38c9db6 10392 -> HEAD 14764 = +4372, all +4372 of it this SDI-12 port. SDI-12 contribution: the device payload gains the SDI-12 probe/layout fields and the bounded recipe-deployment projection."
    },
    "cs-reg-cloud-ack-fn": {
      "delta": 742,
      "reason": "Supersedes the prior entry (695). Wave 3 scoped-access port, gap-fill commit 6ffc7a07d (AgroLink, \"fix: fence cloud device claims in flag-off mode\", missing from the pick list -- discovered when its own N1 test (byte-exact ACK payload check) required forwarding the claim-fence code field): cs-reg-cloud-fn reverted the TYPE_CONFLICT/ALREADY_CLAIMED claim-fence gate from scoped-only back to unconditional (both flag modes), matching its pre-commit-70 unconditional behavior; separately, cs-reg-cloud-ack-fn gained an explicit if (ack.code) payload.code = String(ack.code); passthrough alongside the existing grpcStatus line -- this branch deliberately keeps grpcStatus for ChirpStack provisioning failures (rejecting AgroLink upstream commit fe6f33fe0's grpcStatus->code rename) as its own separate field from the claim-fence's own code (TYPE_CONFLICT/ALREADY_CLAIMED/UNKNOWN_PRINCIPAL), so this line is additive, not a rename; it fixes a latent bug where claim-fence failure codes were silently dropped from the outbound ACK. Re-measured fresh with verify-flows-size-ratchet nodeSizes (func.length) over both maintained profiles, exact delta vs origin/main."
    },
    "cs-reg-cloud-fn": {
      "delta": 5391,
      "reason": "Supersedes the prior entry (4548). Wave 3 scoped-access port, pick-list commit 71 (AgroLink d4364f6725, \"fix: resolve register claims by stable user identity\"): REGISTER_DEVICE now resolves the claiming principal by the stable cloud user UUID (params.userUuid/user_uuid or cmd.userUuid/user_uuid) first, falling back to cloud_user_id only for older commands that lack it, and fails closed with a new UNKNOWN_PRINCIPAL claim-fence code (this branch additionally calls close() on that path, matching the established convention every other early return in this function already follows -- a considered addition beyond AgroLink's own diff, which left the DB handle open on this path). Re-measured fresh with verify-flows-size-ratchet nodeSizes (func.length) over both maintained profiles, exact delta vs origin/main."
    },
    "post-devices-insert": {
      "delta": 1562,
      "reason": "Measured on port/sdi12-edge with verify-flows-size-ratchet's nodeSizes over both maintained profiles (byte-identical bcm2712/bcm2709): origin/main 4558 -> branch base a38c9db6 5832 -> HEAD 6120 = +1562, +1274 the pre-existing feat/valve-control baseline and +288 this SDI-12 port. SDI-12 contribution: the local register path persists chirpstack_app_id on both the claim/update and create branches. Supersedes the previous entry: STREGA Gen2 device profile Task 4: select CHIRPSTACK_PROFILE_STREGA_GEN2 for GEN2 STREGA registrations and stamp msg.deviceRegistration.stregaGeneration for cs-register-device-fn (+557); R1 review fix (MAJOR-2): resolve the effective generation from stored (LEFT JOIN valve_settings via check-existing-device) OR requested state, not the request field alone -- ensureDeviceProvisioned (Task 2) actively repoints ChirpStack to match the resolved profile, so consulting only the request field would demote an already-promoted GEN2 valve's ChirpStack profile back to Gen1 on a generation-omitted re-registration (+717) (exact delta vs origin/main)"
    },
    "sync-init-fn": {
      "delta": 1396,
      "reason": "Measured on port/sdi12-edge with verify-flows-size-ratchet's nodeSizes over both maintained profiles (byte-identical bcm2712/bcm2709): origin/main 74217 -> branch base a38c9db6 74217 -> HEAD 75613 = +1396, all +1396 of it this SDI-12 port. SDI-12 contribution: boot-owned literals track the seed: DRAGINO_SDI12 in the devices CHECK, the five SDI-12 columns in DEVICES_NEW_DDL/DEVICES_COPY_SQL, and sdi12_probe_profile/sdi12_value_count in trg_sync_devices_outbox_au. Supersedes the previous entry: issue #10 trigger rewrites pass NEW.sync_version + AU gates; issue #4 gatewaySql escape fix; issue #5 chameleon_enabled added to trg_sync_devices_outbox_au WHEN clause + payload; issue #16 (+420) boot rewrites of trg_dp_device_data_outbox_ai + trg_dp_chameleon_readings_outbox_ai regain the seed's WHEN-clause gateway-EUI resolvability guard (trigger-body parity adjudication, Pattern A)"
    },
    "strega-process-fn": {
      "delta": 1378,
      "reason": "STREGA Gen2 device profile Task 1: accept the Gen2 vendor decoder's `Actuator` field as an alias for `Valve` (docs/hardware/strega-codecs/ChirpStack-JS-CODEC-Decoder-STREGA-Gen2-CS4.17-and-up emits Actuator, not Valve) via a valveRaw fallback, used in place of decodedObject.Valve at both read sites (+152); Task 6 fix round R1 (MAJOR-2): getProfileKind's stregaProfiles also matches CHIRPSTACK_PROFILE_STREGA_GEN2, so a valve already re-pointed to the Gen2 profile is still recognised as STREGA_VALVE instead of falling through to null (+64); final review fix (MAJOR-5): the empty-object fallback decoder was Gen1-only, so a Gen2 valve hitting that branch got its bytes mis-decoded and written to device_data as though it were Gen1 telemetry -- refuse the fallback decode for a Gen2 profile instead (isGen2StregaProfile helper + a guarded decodedObject expression + a node.warn on the refused path), a wrong valve state being worse than a missing one (+1162, exact delta vs origin/main) [re-measured on port/sdi12-edge: origin/main 5776 -> HEAD 7154 = +1378, all of it the pre-existing feat/valve-control baseline; this port did not touch the node]"
    },
    "post-zone-insert": {
      "delta": 1290,
      "reason": "FW-T5: default a missing zone timezone to the app_settings gateway_timezone (table-missing-safe), instead of silently relying on the irrigation_zones.timezone column DEFAULT 'UTC'; adds an async osiDb read/close and error-wrapping around the existing INSERT build (exact delta vs origin/main) [re-measured on port/sdi12-edge: origin/main 1080 -> HEAD 2370 = +1290, all of it the pre-existing feat/valve-control baseline; this port did not touch the node]"
    },
    "get-devices-query": {
      "delta": 2331,
      "reason": "Supersedes the prior entry (2244). Wave 3 scoped-access port, pick-list commit 57 (AgroLink 06c94d1b2, \"fix(scope): keep cloud-assigned devices visible in device lists\"): the scoped-mode LIFECYCLE whereClause widens from 'd.user_id IS NOT NULL' to '(d.user_id IS NOT NULL OR d.irrigation_zone_id IS NOT NULL)' -- REGISTER_DEVICE's INSERT OR IGNORE can leave a cloud-introduced device with user_id NULL but a resolved zone, and an owner-only lifecycle filter would hide it from every list while its ACK said SUCCESS (+87, exact vs the prior entry). origin/main 2701 -> HEAD 5032 = 2331, exact delta vs origin/main. Re-measured fresh with verify-flows-size-ratchet nodeSizes (func.length) over both maintained profiles. Prior provenance retained: commit E4 (adapted from AgroLink 85dba0b4d, \"fix(scope): deny disabled accounts before building the device list query\") calls scope.assertEnabledAccount() before building the weather-device OR-branch WHERE clause and adds a try/catch converting the thrown 403 into a proper response; and the AgroLink 29ff9d08 (feat(api): enforce scope on zone and device reads) zone-membership/weather-device scoping, both fully subsumed in the current query alongside the SDI-12 joins/columns and the bound-$userId hardening."
    },
    "write-strega-expectation": {
      "delta": 9124,
      "reason": "origin/main -> HEAD delta 9124. Wave 3 scoped-access port, commit X1 (adapted from AgroLink 37dc1a0b2, \"fix(actuation): don't treat transient scope-helper infra errors as denials\"): rethrows a caught scope error with no .statusCode (a transient infra failure like SQLITE_BUSY, not a deliberate scope decision) to the generic outer catch instead of R4's queueCommandAck path, so the command stays non-terminal/retryable instead of being permanently denied for a legitimate actor. Re-measured fresh with verify-flows-size-ratchet nodeSizes (func.length). Supersedes prior entry: origin/main -> HEAD delta 8432. Wave 3 scoped-access port, commit R4 (adapted from AgroLink 9a3624fb0, \"fix(actuation): queue a terminal ack when an actor is present but denied\", ported out-of-pick-list-order as a necessary R1/E3 follow-up the earlier classification missed): wraps assertFreshDeviceAccess/canMutate in a try/catch that queues a terminal REJECTED_PERMANENT/scope_denied ack instead of letting the throw propagate to the generic outer catch, which left a scope-denied cloud command non-terminal (redelivered until expiry instead of told no immediately). Re-measured fresh with verify-flows-size-ratchet nodeSizes (func.length). Supersedes prior entry: origin/main -> HEAD delta 6750. Wave 3 scoped-access port, commit R2 (adapted from AgroLink 76592c3a4, \"fix(actuation): exempt genuine scheduler dispatch from the E3 actor requirement\", ported out-of-pick-list-order as a necessary R1/E3 follow-up the earlier classification missed): the irrigation scheduler dispatches OPEN_FOR_DURATION with no actor (not a user action); without this exemption every automated irrigation decision was silently dropped once OSI_SCOPED_ACCESS=1. Exempts strictly on message-level msg._systemActuation === true (cannot be smuggled via payload/req.body). Re-measured fresh with verify-flows-size-ratchet nodeSizes (func.length). Supersedes prior entry: origin/main -> HEAD delta 5709. Wave 3 scoped-access port, commit R1 Critical (adapted from AgroLink e8086340f, \"fix(actuation): propagate the cloud actor across the cloud->edge boundary\"): Route Command now copies cmd.actor_user_uuid onto msg._actorUserUuid uniformly for every command type before any branch runs; write-strega-expectation also reads raw.actor_user_uuid/raw.actorUserUuid as defense in depth. Without this, every scoped cloud-dispatched physical command was rejected scope_actor_required, including fully authorized ones -- automated remote valve control was completely dead on scoped gateways. Re-measured fresh with verify-flows-size-ratchet nodeSizes (func.length). Supersedes prior entry: origin/main -> HEAD delta 5298. Wave 3 scoped-access port, commit R3 (adapted from AgroLink 002033014, \"fix(actuation): move the E3 scope gate ahead of duration parsing\"): the scope/actor gate needs only deviceEui+commandType, not a parsed duration, so it now runs BEFORE duration parsing (a real cloud VALVE_COMMAND commonly carries no duration at all, which used to skip the gate entirely via an early return). Under scope, an invalid/missing duration now fails closed via queueCommandAck instead of the legacy pass-through. Re-measured fresh with verify-flows-size-ratchet nodeSizes (func.length). Supersedes prior entry: origin/main -> HEAD delta 3696. Wave 3 scoped-access port, commit E3 Critical (adapted from AgroLink 034f4dbc0, \"fix(actuation): require an actor on scoped physical device commands\"): when OSI_SCOPED_ACCESS=1, a PHYSICAL command without an actor is now rejected fail-closed (scope_actor_required, queued via osi-command-ledger.queueCommandAck) instead of skipping the scope check and actuating; when an actor IS present, pairs the existing assertFreshDeviceAccess call with an explicit scope.canMutate() role check. Re-measured fresh with verify-flows-size-ratchet nodeSizes (func.length). Supersedes prior entry: Wave 3 scoped-access port, commit 28 (AgroLink 95c6f5c8, feat(sync): apply scoped access commands): the 6-line scoped-access COMMAND_TYPES fallback registry entries (mirrored from cmd-type-registry/reject-indefinite-open, dispatch: scoped_access_apply) were appended to this node too. Re-measured fresh on port/wave3-edge-scope with verify-flows-size-ratchet nodeSizes over both maintained profiles (byte-identical bcm2712/bcm2709): origin/main 11695 -> HEAD 13569 = +1874. Supersedes the prior +1117 entry (commit 14/832d48ef), which is fully subsumed -- that entry's content (fresh-scope actor assertion + applied_commands audit trail on write) is preserved verbatim."
    },
    "cmd-type-registry": {
      "delta": 847,
      "reason": "Measured on port/sdi12-edge with verify-flows-size-ratchet's nodeSizes over both maintained profiles (byte-identical bcm2712/bcm2709): origin/main 4976 -> branch base a38c9db6 5700 -> HEAD 5823 = +847, +724 the pre-existing feat/valve-control baseline and +123 this SDI-12 port. SDI-12 contribution: SET_SDI12_IDENTIFY joins the command-type registry. Supersedes the previous entry: 1.C: expanded command-type registry from 8 to 33 types"
    },
    "reject-indefinite-open": {
      "delta": 847,
      "reason": "Measured on port/sdi12-edge with verify-flows-size-ratchet's nodeSizes over both maintained profiles (byte-identical bcm2712/bcm2709): origin/main 6024 -> branch base a38c9db6 6748 -> HEAD 6871 = +847, +724 the pre-existing feat/valve-control baseline and +123 this SDI-12 port. SDI-12 contribution: SET_SDI12_IDENTIFY joins the duration-bound exemption set. Supersedes the previous entry: 1.C: expanded COMMAND_TYPES_FALLBACK to match 33-type registry"
    },
    "cs-register-device-fn": {
      "delta": 808,
      "reason": "STREGA Gen2 device profile Task 4: params-capable run() helper plus the promotion-only valve_settings upsert (GEN1->GEN2 only, never demotes a stored GEN2) after successful provisioning (+778); R1 review fix (MINOR b): maintain updated_at on the valve_settings write per osi-valve-control/store.js's documented invariant (+30) (exact delta vs origin/main) [re-measured on port/sdi12-edge: origin/main 1678 -> HEAD 2486 = +808, all of it the pre-existing feat/valve-control baseline; this port did not touch the node]"
    },
    "format-devices": {
      "delta": 744,
      "reason": "Measured on port/sdi12-edge with verify-flows-size-ratchet's nodeSizes over both maintained profiles (byte-identical bcm2712/bcm2709): origin/main 3903 -> branch base a38c9db6 3903 -> HEAD 4647 = +744, all +744 of it this SDI-12 port. SDI-12 contribution: device rows carry the SDI-12 probe/layout/deployment fields."
    },
    "cdbaa3891d40d7a1": {
      "delta": 497,
      "reason": "Valve advanced controls consolidation Task 4 (E2): _log_ctx gains percentage/returnPosition fields for SET_PARTIAL_OPENING/SET_FLUSHING so the value the encoder already computes is no longer dropped before it reaches actuator_log (+497 exact delta vs origin/main) [re-measured on port/sdi12-edge: origin/main 10221 -> HEAD 10718 = +497, all of it the pre-existing feat/valve-control baseline; this port did not touch the node]"
    },
    "post-devices-auth": {
      "delta": 1953,
      "reason": "Supersedes the previous entry (485). port/wave3-auth-flaggate (fix(auth): flag-gate the shared scope resolver across all auth nodes + hermetic flag-off verifier, follow-up to PR #200 / commit 2c4fa9a5f): measured with verify-flows-size-ratchet's nodeSizes over both maintained profiles (byte-identical bcm2712/bcm2709): origin/main 4265 -> HEAD 6218 = +1953. getAuthSecret() previously called osiLib.require('scope') unconditionally, violating the default-off contract (a lost osi-lib helper file would 500 every auth endpoint on a flag-off gateway). Fixed per the history-api-router-fn/zone-env-fn reference pattern: getAuthSecret() now checks a freshly-recomputed OSI_SCOPED_ACCESS flag first (recomputed locally rather than closing over any outer scopedOn, avoiding TDZ risk uniformly across every node regardless of its own structure) and only resolves osiLib inside the flag-on branch; the flag-off branch inlines the exact pre-refactor secret-resolution logic (same file paths, same ENOENT-quiet read/generate/write behavior), byte-equivalent to the pre-port route."
    },
    "5c45136f382d501c": {
      "delta": 462,
      "reason": "Valve advanced controls consolidation Task 4 (E2): actuator_log INSERT composes the percentage (and returnPosition for flushing) into the action free-text column instead of writing the bare command string -- no schema change, actuator_log has no JSON/detail column to hold it in (+462 exact delta vs origin/main) [re-measured on port/sdi12-edge: origin/main 1047 -> HEAD 1509 = +462, all of it the pre-existing feat/valve-control baseline; this port did not touch the node]"
    },
    "8809bb5239dfb3d4": {
      "delta": 446,
      "reason": "Measured on port/sdi12-edge with verify-flows-size-ratchet's nodeSizes over both maintained profiles (byte-identical bcm2712/bcm2709): origin/main 12175 -> branch base a38c9db6 12333 -> HEAD 12621 = +446, +158 the pre-existing feat/valve-control baseline and +288 this SDI-12 port. SDI-12 contribution: Build Telemetry gains the sdi12Profiles dispatch arm for DRAGINO_SDI12. Supersedes the previous entry: STREGA Gen2 device profile Task 6 fix round R2 (NEW-MAJOR-A, NEW-MAJOR-B): Build Telemetry's getProfileKind() gains the CHIRPSTACK_PROFILE_STREGA_GEN2 whitelist entry so a re-pointed Gen2 valve is still recognised as STREGA_VALVE on the cloud telemetry path instead of falling through to the generic sensor branch (matches valve-ack-fn/strega-process-fn from earlier in this task); and the Gen2 vendor decoder's Actuator field is accepted as an alias for Valve via an explicit !== undefined && !== null check (never ||, since valve state 0/CLOSED is falsy), matching strega-process-fn's Task 1 shape exactly -- without it every Gen2 valve mirrored state: null to the cloud regardless of profile naming (exact delta vs origin/main)"
    },
    "fn_build_sensor_sql_params": {
      "delta": 4041,
      "reason": "origin/main -> HEAD delta 4041. Wave 3 scoped-access port, commit close-review-gaps (adapted from AgroLink de9a53df8, \"fix: close AgroLink parity review gaps\"): defense-in-depth authUserId/authUsername split -- the scoped-read auth check now queries 'WHERE id = ? AND username = ?' (not username alone) and rejects with 403 when the id/username pair does not resolve, closing a username-reuse-after-rename gap. Re-measured fresh on port/wave3-edge-scope with verify-flows-size-ratchet nodeSizes (func.length) over both maintained profiles (byte-identical bcm2712/bcm2709), delta vs origin/main. Supersedes prior entry: Wave 3 scoped-access port (AgroLink c034b2893, feat(api): enforce device scope on read paths): measured on port/wave3-edge-scope with verify-flows-size-ratchet's nodeSizes over both maintained profiles (byte-identical bcm2712/bcm2709): origin/main 1716 -> HEAD 5606 = +3890, superseding the previous +368 entry (fully subsumed: the full vwc_/soil_temp_/soil_ec_ column list is preserved verbatim). Wraps the /download-sensordata export in a bearer-auth + scope gate (own scopedReadAuthSecret/scopedReadVerifyBearer helpers, own catch handler -- outputs 1 -> 2): when OSI_SCOPED_ACCESS=1, resolves the caller's zone scope via osi-scope-helper and adds an iz.zone_uuid IN (...) OR d.type_id IN ('SENSECAP_S2120','AQUASCOPE_LORAIN') predicate (new irrigation_zones join); unscoped mode is unchanged."
    },
    "934bf2bc19a8ce22": {
      "delta": 706,
      "reason": "origin/main -> HEAD delta 706. Wave 3 scoped-access port, commit R1 Critical (adapted from AgroLink e8086340f, \"fix(actuation): propagate the cloud actor across the cloud->edge boundary\"): Route Command now copies cmd.actor_user_uuid onto msg._actorUserUuid uniformly for every command type before any branch runs; write-strega-expectation also reads raw.actor_user_uuid/raw.actorUserUuid as defense in depth. Without this, every scoped cloud-dispatched physical command was rejected scope_actor_required, including fully authorized ones -- automated remote valve control was completely dead on scoped gateways. Re-measured fresh with verify-flows-size-ratchet nodeSizes (func.length). Supersedes prior entry: Valve control Phase B Task 3: Route Command dispatches the four cloud valve-schedule commands (UPSERT_VALVE_SCHEDULE, DELETE_VALVE_SCHEDULE, RESEND_VALVE_PLAN, SET_VALVE_SCHEDULER_STATUS) to a new sixth output, wired to a thin Valve Cloud Command Bridge node that delegates to osi-valve-control's applyCloudCommand (+223 exact delta vs origin/main). cloud full-parity Task 1.4: the same hardcoded routing array is the actual gate for reaching the bridge (neither the bridge nor this node reads cmd-type-registry's `dispatch` field for this decision), so CANCEL_VALVE_ACTUATION was added to it too (+253 exact delta vs origin/main). cloud full-parity Task P2-E1: UPSERT_VALVE_SETTINGS added to the same hardcoded routing array for the same reason (+282 exact delta vs origin/main). [re-measured on port/sdi12-edge: origin/main 22347 -> HEAD 22629 = +282, all of it the pre-existing feat/valve-control baseline; this port did not touch the node]"
    },
    "post-zone-auth": {
      "delta": 1947,
      "reason": "Supersedes the previous entry (270). port/wave3-auth-flaggate (fix(auth): flag-gate the shared scope resolver across all auth nodes + hermetic flag-off verifier, follow-up to PR #200 / commit 2c4fa9a5f): measured with verify-flows-size-ratchet's nodeSizes over both maintained profiles (byte-identical bcm2712/bcm2709): origin/main 3521 -> HEAD 5468 = +1947. getAuthSecret() previously called osiLib.require('scope') unconditionally, violating the default-off contract (a lost osi-lib helper file would 500 every auth endpoint on a flag-off gateway). Fixed per the history-api-router-fn/zone-env-fn reference pattern: getAuthSecret() now checks a freshly-recomputed OSI_SCOPED_ACCESS flag first (recomputed locally rather than closing over any outer scopedOn, avoiding TDZ risk uniformly across every node regardless of its own structure) and only resolves osiLib inside the flag-on branch; the flag-off branch inlines the exact pre-refactor secret-resolution logic (same file paths, same ENOENT-quiet read/generate/write behavior), byte-equivalent to the pre-port route."
    },
    "get-actuations-response": {
      "delta": 214,
      "reason": "valve control: recent-actuations response payload maps trigger alongside cancelReason (+40); final-fix-wave MAJOR-2: GRACE_MS widened from 120s to 1800s to match strega-reconciliation-monitor's RECONCILIATION_GRACE_SEC, plus a comment naming that mirrored constant, so a still-reconciling run is not badged OPEN_TIMEOUT/CLOSE_TIMEOUT early (+174) [re-measured on port/sdi12-edge: origin/main 2538 -> HEAD 2752 = +214, all of it the pre-existing feat/valve-control baseline; this port did not touch the node]"
    },
    "al-link-store-mqtt": {
      "delta": 143,
      "reason": "Cloud MQTT broker URL fix: persist osi-server.cloud.mqtt_broker_url (from the already-validated al_mqtt_broker_url) alongside mqtt_password/server_host/link_gateway_device_eui, and back it up in _mqttConfigBackup for rollback (exact delta vs origin/main) [re-measured on port/sdi12-edge: origin/main 2715 -> HEAD 2858 = +143, all of it the pre-existing feat/valve-control baseline; this port did not touch the node]"
    },
    "al-unlink-clear-mqtt": {
      "delta": 132,
      "reason": "Cloud MQTT broker URL fix: clear osi-server.cloud.mqtt_broker_url on unlink and back up its prior value in _mqttConfigBackup for restore, mirroring the existing mqtt_password/server_host/link_gateway_device_eui handling (exact delta vs origin/main) [re-measured on port/sdi12-edge: origin/main 1463 -> HEAD 1595 = +132, all of it the pre-existing feat/valve-control baseline; this port did not touch the node]"
    },
    "al-link-rollback-mqtt": {
      "delta": 86,
      "reason": "Cloud MQTT broker URL fix: restore osi-server.cloud.mqtt_broker_url from _mqttConfigBackup on link rollback, mirroring the existing mqtt_password/server_host/link_gateway_device_eui restore (exact delta vs origin/main) [re-measured on port/sdi12-edge: origin/main 1335 -> HEAD 1421 = +86, all of it the pre-existing feat/valve-control baseline; this port did not touch the node]"
    },
    "al-unlink-restore-mqtt": {
      "delta": 86,
      "reason": "Cloud MQTT broker URL fix: restore osi-server.cloud.mqtt_broker_url from _mqttConfigBackup on unlink failure, mirroring the existing mqtt_password/server_host/link_gateway_device_eui restore (exact delta vs origin/main) [re-measured on port/sdi12-edge: origin/main 1329 -> HEAD 1415 = +86, all of it the pre-existing feat/valve-control baseline; this port did not touch the node]"
    },
    "check-existing-device": {
      "delta": 84,
      "reason": "STREGA Gen2 device profile Task 4 R1 review fix (MAJOR-2): LEFT JOIN valve_settings so post-devices-insert can see the stored generation, not just the request field, and keep a stored GEN2 sticky across re-registration (exact delta vs origin/main) [re-measured on port/sdi12-edge: origin/main 404 -> HEAD 488 = +84, all of it the pre-existing feat/valve-control baseline; this port did not touch the node]"
    },
    "catalog-response": {
      "delta": 69,
      "reason": "Measured on port/sdi12-edge with verify-flows-size-ratchet's nodeSizes over both maintained profiles (byte-identical bcm2712/bcm2709): origin/main 482 -> branch base a38c9db6 482 -> HEAD 551 = +69, all +69 of it this SDI-12 port. SDI-12 contribution: the device catalog gains the DRAGINO_SDI12 row."
    },
    "get-actuations-query": {
      "delta": 1299,
      "reason": "Wave 3 scoped-access port (AgroLink d49e1cd28, feat(api): scope shared analysis reads): measured on port/wave3-edge-scope with verify-flows-size-ratchet's nodeSizes over both maintained profiles (byte-identical bcm2712/bcm2709): origin/main 3011 -> HEAD 4310 = +1299, superseding the previous +54 entry (fully subsumed: the vae.trigger column, added on main after AgroLink's fork, is preserved verbatim in both the CREATE TABLE and SELECT). When OSI_SCOPED_ACCESS=1, resolves the caller's zone scope via osi-scope-helper (assertEnabledAccount + listScopeZoneUuids) and scopes the recent-actuations query to iz.zone_uuid IN (...) instead of d.user_id =; unscoped mode keeps the owner-id predicate. Adds osiLib to libs; the CREATE TABLE catch is now visible via the function-level catch/node.warn."
    },
    "zone-env-fn": {
      "delta": 2588,
      "reason": "Supersedes the prior entry (1155). Coordinator-flagged CI red fix: getAuthSecret() was calling osiLib.require('scope') unconditionally, so a flag-off (default) request touched osiLib even though OSI_SCOPED_ACCESS was unset -- violates the design contract that the flag-off state must be a pure passthrough with zero new dependencies, byte-equivalent to the pre-port route. Fixed by checking the scope flag FIRST and only calling osiLib.require('scope') inside the flag-on branch; the flag-off branch now inlines the original (pre-shared-resolver) secret-resolution logic directly, matching resolveAuthSecret()'s own behavior byte-for-byte but without the osiLib dependency. Verified via node scripts/capture-history-router-vectors.js --verify (4/4 golden routes) and node scripts/capture-zone-env-vectors.js --verify (2/2 vectors), both green with NO osiLib bound in the harness. Re-measured fresh with verify-flows-size-ratchet nodeSizes (func.length) over both maintained profiles."
    },
    "strega-today-liters-fn": {
      "delta": 3339,
      "reason": "origin/main -> HEAD delta 3339. Wave 3 scoped-access port, commit W1-P5 (adapted from AgroLink 43cb2191b, \"feat(scope): make all device-detail reads account-wide, including unassigned devices\"): the zone-membership/device-ownership check (assertDeviceAccess) replaced with the enabled-account check (assertEnabledAccount) alone; devices with no zone are now readable too. Re-measured fresh with verify-flows-size-ratchet nodeSizes (func.length). Supersedes prior entry: origin/main -> HEAD delta 3195. Wave 3 scoped-access port, commit close-review-gaps (adapted from AgroLink de9a53df8, \"fix: close AgroLink parity review gaps\"): defense-in-depth authUserId/authUsername split -- the scoped-read auth check now queries 'WHERE id = ? AND username = ?' (not username alone) and rejects with 403 when the id/username pair does not resolve, closing a username-reuse-after-rename gap. Re-measured fresh on port/wave3-edge-scope with verify-flows-size-ratchet nodeSizes (func.length) over both maintained profiles (byte-identical bcm2712/bcm2709), delta vs origin/main. Supersedes prior entry: Wave 3 scoped-access port (AgroLink c034b2893, feat(api): enforce device scope on read paths): measured on port/wave3-edge-scope with verify-flows-size-ratchet's nodeSizes over both maintained profiles (byte-identical bcm2712/bcm2709): origin/main 1517 -> HEAD 4551 = +3034, superseding the previous +23 entry (fully subsumed). This route stays one of the three deliberately unauthenticated device-api-tab routes when OSI_SCOPED_ACCESS is unset (default): the new scopedReadAuthSecret/scopedReadVerifyBearer bearer check and osi-scope-helper assertDeviceAccess call only run when OSI_SCOPED_ACCESS=1. The pre-existing STALE_NO_OBSERVATION exclusion (alongside CANCELLED) in the reconciliation_state filter is preserved unchanged; close()/catch handling converted to visible node.warn."
    },
    "d0b2b1c1a937e16d": {
      "delta": 17,
      "reason": "Measured on port/sdi12-edge with verify-flows-size-ratchet's nodeSizes over both maintained profiles (byte-identical bcm2712/bcm2709): origin/main 2177 -> branch base a38c9db6 2177 -> HEAD 2194 = +17, all +17 of it this SDI-12 port. SDI-12 contribution: SDI-12 telemetry fields join this response shape."
    },
    "062a0f9bf66d9789": {
      "delta": 850,
      "reason": "Build Heartbeat: gateway_health_samples (1.A5) + rtc_present/clock_source (5.6); wave-3 edge foundation port (cherry-picked e5e6648095f94dfc969f14d1d8d075dc949233a3) adds the windowed sync_rejected_recent field to the heartbeat health whitelist so the cloud can evaluate a 24h rejection window instead of the all-time terminal count. Re-measured on port/wave3-edge-foundation with verify-flows-size-ratchet's nodeSizes over both maintained profiles (byte-identical bcm2712/bcm2709): origin/main 4242 -> HEAD 4337 = +95, well within the existing 850 headroom (already absorbed into origin/main's own baseline from prior 1.A5/5.6 growth), so no allowance increase was needed."
    },
    "sys-stats-fn": {
      "delta": 4862,
      "reason": "MERGE 2026-08-31 (main only, carried as-is): filtered restartPending status (Option C Slice 1b), including explicit blocked/malformed/unreadable restartPending states, deduplicated fan-probe warnings, and a capped, hotplug-pruned context map (+4862)"
    },
    "2a4f142e3e9b6d80": {
      "delta": 400,
      "reason": "sync outbox eviction index guard (1.A5)"
    },
    "prune-sync-outbox": {
      "delta": 3000,
      "reason": "outbox pruning overhaul with eviction-index awareness (1.A5)"
    },
    "history-api-router-fn": {
      "delta": 3841,
      "reason": "Supersedes the prior entry (3001). Coordinator-flagged CI red fix: getAuthSecret() was calling osiLib.require('scope') unconditionally, so a flag-off (default) request touched osiLib even though OSI_SCOPED_ACCESS was unset -- violates the design contract that the flag-off state must be a pure passthrough with zero new dependencies, byte-equivalent to the pre-port route. Fixed by checking the scope flag FIRST and only calling osiLib.require('scope') inside the flag-on branch; the flag-off branch now inlines the original (pre-shared-resolver) secret-resolution logic directly, matching resolveAuthSecret()'s own behavior byte-for-byte but without the osiLib dependency. Verified via node scripts/capture-history-router-vectors.js --verify (4/4 golden routes) and node scripts/capture-zone-env-vectors.js --verify (2/2 vectors), both green with NO osiLib bound in the harness. Re-measured fresh with verify-flows-size-ratchet nodeSizes (func.length) over both maintained profiles."
    },
    "sync-pending-split": {
      "delta": 401,
      "reason": "Field Journal Task 11: preserve the trusted pending-command envelope alongside legacy flat routing fields"
    },
    "command-dedupe-dispatch": {
      "delta": 823,
      "reason": "Field Journal Task 10: shared command-ledger replay; PR #141 hardening: guarded osiLib loading and protected-type routing keep generic commands independent from optional journal hooks (+823 exact final delta vs origin/main)"
    },
    "9b3afb405207302e": {
      "delta": 800,
      "reason": "5.6: timestamp sanity clamp (clampRecordedAt) at ingest"
    },
    "5f0d2b7e9b9b1b3a": {
      "delta": 1700,
      "reason": "5.6: scheduler backward-jump debounce (sameLogicalWindow) on both DENDRO + threshold paths + threshold suppression early return"
    },
    "dendro-compute-fn": {
      "delta": 2304,
      "reason": "issue #10: INSERT OR REPLACE -> ON CONFLICT DO UPDATE upserts bumping sync_version (dendrometer_daily + zone_daily_recommendations); PR #140 review finding: add sync_version to the three inline fallback CREATE TABLE statements (+104)"
    },
    "sim-dendro-fn-setup": {
      "delta": 950,
      "reason": "issue #10: sim INSERT OR REPLACE -> ON CONFLICT DO UPDATE upserts bumping sync_version"
    },
    "sync-outbox-build": {
      "delta": 0,
      "reason": "live identity restart sentinel (Option C Slice 1) (main, 2026-08) + valve-control growth (retryable backoff cooldown-exclusion clause; PR #141 object-only payload parser, rollback propagation, strict delivery mapping, visible cleanup catches); re-measured at merge 2026-08-31 against origin/main (3c7b95bc) using verify-flows-size-ratchet's nodeSizes: HEAD's node body (18,056 chars) is byte-identical to origin/main's (18,056 chars) on both maintained profiles. valve-control's branch tip (e2bc5ce0) is itself byte-identical to the valve-control/main merge-base (60b82a36) for this node, so its historical +397 already predates this merge-base and needs no additional headroom here; main's identity-sentinel and retryable-backoff growth is fully carried by the origin/main baseline itself. Net allowance required beyond origin/main: 0."
    },
    "sync-pending-build": {
      "delta": 1344,
      "reason": "MERGE 2026-08-31 (main only, carried as-is): live identity restart sentinel (Option C Slice 1), including marked gateway-identity errors"
    },
    "sync-outbox-mark": {
      "delta": 1845,
      "reason": "wave3-tail-fixes fail-closed outbox mark (adapted from AgroLink 62f8d2dce): isHttpSuccess(statusCode) = Number.isInteger(statusCode) && 200-299 replaces the truthy statusCode check that let statusCode=0 (the sync-outbox-http transport-failure sentinel) fall through as \"not a failure\"; removed the whole-batch deliveredIds=msg._syncEventIds fallback so an absent/empty results array now classifies every requested event retryable instead of marking the batch delivered; added unique-result-per-eventUuid classification (missing/duplicate/malformed/unrequested) with a disjointness assertion before SQL construction. Re-measured with verify-flows-size-ratchet nodeSizes (func.length) against actual origin/main: 3902 -> 5747 = +1845 (supersedes the prior +294 Task J entry, already reflected in origin/main's current 3902)."
    },
    "sync-bootstrap-mark": {
      "delta": 244,
      "reason": "wave3-tail-fixes fail-closed bootstrap mark (adapted from AgroLink 62f8d2dce): same isHttpSuccess predicate, plus payload.success === true is now required before the sync_cursor write, sync-token rotation, lastBootstrapSuccessAt, or gateway-migration pause-flag clearing -- all gated behind one guard clause, so a 2xx with {success:false} (or a non-object payload) no longer advances state. Re-measured with verify-flows-size-ratchet nodeSizes (func.length) against actual origin/main: 3784 -> 4028 = +244 (new entry; node had no prior node_allowances pin)."
    },
    "work-request-status-apply": {
      "delta": 2089,
      "reason": "wave3-tail-fixes idempotent work-request-status-apply on command replay (adapted from AgroLink ccb39eb2b): on entry, look up the commandId in the shipped applied_commands ledger (command_id TEXT PRIMARY KEY); if a row exists, rebuild and return the original terminal ACK verbatim (using the stored applied_at instead of call-time now) without re-UPDATE-ing improvement_requests. A first-time APPLIED result also INSERTs an applied_commands row using the exact 'initial' shape osi-command-ledger.queueCommandAck writes, so the downstream command-ack-queue-rest call reproduces the identical command_ack_outbox row via its persistReplayAck branch on a replay. Re-measured with verify-flows-size-ratchet nodeSizes (func.length) against actual origin/main: 2990 -> 5079 = +2089 (new entry; node had no prior node_allowances pin)."
    },
    "command-ack-build-batch": {
      "delta": 975,
      "reason": "MERGE 2026-08-31 (main only, carried as-is): live identity restart sentinel (Option C Slice 1)"
    },
    "sync-state-build": {
      "delta": 1089,
      "reason": "Supersedes the previous entry (1951, live identity restart sentinel (Option C Slice 1) provenance carried forward; that delta was already fully baked into origin/main's own measured size for this node). Wave 3 durable-history-batch + installation-identity port (AgroLink 77d3c52a, renumbered migrations 0052-0053, port/wave3-edge-durable): measured with verify-flows-size-ratchet's nodeSizes over both maintained profiles (byte-identical bcm2712/bcm2709): origin/main 11319 -> HEAD 12408 = +1089. This node now reads the local installation_identity row and advertises an installationIdentity block (installationUuid, currentGatewayDeviceEui, previousGatewayDeviceEuis, recoveryState, recoveryOperationUuid, restoreStartedAt, reconciledAt) alongside the existing gatewayIdentity block in the sync-state payload."
    },
    "al-link-build-req": {
      "delta": 2511,
      "reason": "Supersedes the previous entry (969, live identity restart sentinel (Option C Slice 1) provenance carried forward; that delta was already fully baked into origin/main's own measured size for this node). Wave 3 durable-history-batch + installation-identity port (AgroLink 77d3c52a, renumbered migrations 0052-0053, port/wave3-edge-durable): measured with verify-flows-size-ratchet's nodeSizes over both maintained profiles (byte-identical bcm2712/bcm2709): origin/main 4142 -> HEAD 6653 = +2511. This node now reads/creates/merges the local installation_identity singleton via osi-lib's installation helper before the local-user lookup, advertises installationUuid plus the installation_recovery_v1 sync capability on the /auth/local-sync request payload, and gained osiLib to its libs."
    },
    "al-link-restart-node-red": {
      "delta": 1761,
      "reason": "MERGE 2026-08-31 (main only, carried as-is): live identity restart sentinel (Option C Slice 1), including bounded deterministic msg._msgid request key and fail-closed two-output publication (+1761)"
    },
    "al-unlink-restart-node-red": {
      "delta": 1773,
      "reason": "MERGE 2026-08-31 (main only, carried as-is): live identity restart sentinel (Option C Slice 1), including bounded deterministic msg._msgid request key and fail-closed two-output publication (+1773)"
    },
    "put-chameleon-enabled-auth-fn": {
      "delta": 1977,
      "reason": "Supersedes the previous entry (175). port/wave3-auth-flaggate (fix(auth): flag-gate the shared scope resolver across all auth nodes + hermetic flag-off verifier, follow-up to PR #200 / commit 2c4fa9a5f): measured with verify-flows-size-ratchet's nodeSizes over both maintained profiles (byte-identical bcm2712/bcm2709): origin/main 5139 -> HEAD 7116 = +1977. getAuthSecret() previously called osiLib.require('scope') unconditionally, violating the default-off contract (a lost osi-lib helper file would 500 every auth endpoint on a flag-off gateway). Fixed per the history-api-router-fn/zone-env-fn reference pattern: getAuthSecret() now checks a freshly-recomputed OSI_SCOPED_ACCESS flag first (recomputed locally rather than closing over any outer scopedOn, avoiding TDZ risk uniformly across every node regardless of its own structure) and only resolves osiLib inside the flag-on branch; the flag-off branch inlines the exact pre-refactor secret-resolution logic (same file paths, same ENOENT-quiet read/generate/write behavior), byte-equivalent to the pre-port route."
    },
    "dendro-ref-tree-fn": {
      "delta": 46,
      "reason": "issue #15: UPDATE bumps sync_version so a reference-tree toggle after the first delivered DEVICE event does not hit equal_version_payload_conflict, matching the put-chameleon-enabled-auth-fn / dendro_enabled / temp_enabled / rain_gauge_enabled / flow_meter_enabled precedent"
    },
    "lsn50-zone-agg-fn": {
      "delta": 70,
      "reason": "issue #10: zone_daily_environment upsert bumps sync_version"
    },
    "s2120-rain-agg-fn": {
      "delta": 70,
      "reason": "issue #10: zone_daily_environment upsert bumps sync_version"
    },
    "lorain-rain-agg-fn": {
      "delta": 70,
      "reason": "issue #10: zone_daily_environment upsert bumps sync_version"
    },
    "device-api-http500": {
      "delta": 2550,
      "reason": "Wave 3 scoped-access port: cumulative through AgroLink 708e11410 (feat(api): require admin for diagnostic reads). Measured on port/wave3-edge-scope with verify-flows-size-ratchet's nodeSizes over both maintained profiles (byte-identical bcm2712/bcm2709): origin/main 293 -> HEAD 2843 = +2550, superseding the previous +1982 entry (fully subsumed: the authSources allowlist, publicByCode map, and the merged fallback-to-thrown-statusCode design from c034b2893 are preserved verbatim). 708e11410 adds api-me-auth to the authSources allowlist (api-me-auth's own Decode Token node now also sets _osiAuthFailure) and no further structural change to this node beyond the earlier merge."
    },
    "dendro-raw-fn": {
      "delta": 1945,
      "reason": "Supersedes the previous entry (1170). port/wave3-auth-flaggate (fix(auth): flag-gate the shared scope resolver across all auth nodes + hermetic flag-off verifier, follow-up to PR #200 / commit 2c4fa9a5f): measured with verify-flows-size-ratchet's nodeSizes over both maintained profiles (byte-identical bcm2712/bcm2709): origin/main 5871 -> HEAD 7816 = +1945. getAuthSecret() previously called osiLib.require('scope') unconditionally, violating the default-off contract (a lost osi-lib helper file would 500 every auth endpoint on a flag-off gateway). Fixed per the history-api-router-fn/zone-env-fn reference pattern: getAuthSecret() now checks a freshly-recomputed OSI_SCOPED_ACCESS flag first (recomputed locally rather than closing over any outer scopedOn, avoiding TDZ risk uniformly across every node regardless of its own structure) and only resolves osiLib inside the flag-on branch; the flag-off branch inlines the exact pre-refactor secret-resolution logic (same file paths, same ENOENT-quiet read/generate/write behavior), byte-equivalent to the pre-port route."
    },
    "get-zones-query": {
      "delta": 1394,
      "reason": "origin/main -> HEAD delta 1394. Wave 3 scoped-access port, commit W1 write-only-scoping (adapted from AgroLink 71b8103d5, \"feat(scope): make device and zone list reads account-wide\"): zone/device list reads become account-wide (no zone-uuid/owner predicate); the enabled-account check (assertEnabledAccount) is now the only remaining scope gate on reads. Writes stay grant-gated (unaffected by this commit). Re-measured fresh with verify-flows-size-ratchet nodeSizes (func.length). Supersedes prior entry: Wave 3 scoped-access port (AgroLink 29ff9d08, feat(api): enforce scope on zone and device reads): measured on port/wave3-edge-scope with verify-flows-size-ratchet's nodeSizes over both maintained profiles (byte-identical bcm2712/bcm2709): origin/main 3564 -> HEAD 4688 = +1124. New node-local scope gate: when OSI_SCOPED_ACCESS=1, resolves the caller's owned-plus-granted zone UUID set via osi-scope-helper's listScopeZoneUuids and scopes the zone list query to iz.zone_uuid IN (...); unscoped mode keeps the existing iz.user_id predicate."
    },
    "dendro-zone-rec-fn": {
      "delta": 1955,
      "reason": "Supersedes the previous entry (1049). port/wave3-auth-flaggate (fix(auth): flag-gate the shared scope resolver across all auth nodes + hermetic flag-off verifier, follow-up to PR #200 / commit 2c4fa9a5f): measured with verify-flows-size-ratchet's nodeSizes over both maintained profiles (byte-identical bcm2712/bcm2709): origin/main 5194 -> HEAD 7149 = +1955. getAuthSecret() previously called osiLib.require('scope') unconditionally, violating the default-off contract (a lost osi-lib helper file would 500 every auth endpoint on a flag-off gateway). Fixed per the history-api-router-fn/zone-env-fn reference pattern: getAuthSecret() now checks a freshly-recomputed OSI_SCOPED_ACCESS flag first (recomputed locally rather than closing over any outer scopedOn, avoiding TDZ risk uniformly across every node regardless of its own structure) and only resolves osiLib inside the flag-on branch; the flag-off branch inlines the exact pre-refactor secret-resolution logic (same file paths, same ENOENT-quiet read/generate/write behavior), byte-equivalent to the pre-port route."
    },
    "dendro-history-fn": {
      "delta": 1953,
      "reason": "Supersedes the previous entry (1843). port/wave3-auth-flaggate (fix(auth): flag-gate the shared scope resolver across all auth nodes + hermetic flag-off verifier, follow-up to PR #200 / commit 2c4fa9a5f): measured with verify-flows-size-ratchet's nodeSizes over both maintained profiles (byte-identical bcm2712/bcm2709): origin/main 5336 -> HEAD 7289 = +1953. getAuthSecret() previously called osiLib.require('scope') unconditionally, violating the default-off contract (a lost osi-lib helper file would 500 every auth endpoint on a flag-off gateway). Fixed per the history-api-router-fn/zone-env-fn reference pattern: getAuthSecret() now checks a freshly-recomputed OSI_SCOPED_ACCESS flag first (recomputed locally rather than closing over any outer scopedOn, avoiding TDZ risk uniformly across every node regardless of its own structure) and only resolves osiLib inside the flag-on branch; the flag-off branch inlines the exact pre-refactor secret-resolution logic (same file paths, same ENOENT-quiet read/generate/write behavior), byte-equivalent to the pre-port route."
    },
    "sensor-history-fn": {
      "delta": 1953,
      "reason": "Supersedes the previous entry (1843). port/wave3-auth-flaggate (fix(auth): flag-gate the shared scope resolver across all auth nodes + hermetic flag-off verifier, follow-up to PR #200 / commit 2c4fa9a5f): measured with verify-flows-size-ratchet's nodeSizes over both maintained profiles (byte-identical bcm2712/bcm2709): origin/main 5484 -> HEAD 7437 = +1953. getAuthSecret() previously called osiLib.require('scope') unconditionally, violating the default-off contract (a lost osi-lib helper file would 500 every auth endpoint on a flag-off gateway). Fixed per the history-api-router-fn/zone-env-fn reference pattern: getAuthSecret() now checks a freshly-recomputed OSI_SCOPED_ACCESS flag first (recomputed locally rather than closing over any outer scopedOn, avoiding TDZ risk uniformly across every node regardless of its own structure) and only resolves osiLib inside the flag-on branch; the flag-off branch inlines the exact pre-refactor secret-resolution logic (same file paths, same ENOENT-quiet read/generate/write behavior), byte-equivalent to the pre-port route."
    },
    "rain-history-fn": {
      "delta": 1949,
      "reason": "Supersedes the previous entry (1825). port/wave3-auth-flaggate (fix(auth): flag-gate the shared scope resolver across all auth nodes + hermetic flag-off verifier, follow-up to PR #200 / commit 2c4fa9a5f): measured with verify-flows-size-ratchet's nodeSizes over both maintained profiles (byte-identical bcm2712/bcm2709): origin/main 5497 -> HEAD 7446 = +1949. getAuthSecret() previously called osiLib.require('scope') unconditionally, violating the default-off contract (a lost osi-lib helper file would 500 every auth endpoint on a flag-off gateway). Fixed per the history-api-router-fn/zone-env-fn reference pattern: getAuthSecret() now checks a freshly-recomputed OSI_SCOPED_ACCESS flag first (recomputed locally rather than closing over any outer scopedOn, avoiding TDZ risk uniformly across every node regardless of its own structure) and only resolves osiLib inside the flag-on branch; the flag-off branch inlines the exact pre-refactor secret-resolution logic (same file paths, same ENOENT-quiet read/generate/write behavior), byte-equivalent to the pre-port route."
    },
    "dendro-daily-fn": {
      "delta": 1949,
      "reason": "Supersedes the previous entry (1073). port/wave3-auth-flaggate (fix(auth): flag-gate the shared scope resolver across all auth nodes + hermetic flag-off verifier, follow-up to PR #200 / commit 2c4fa9a5f): measured with verify-flows-size-ratchet's nodeSizes over both maintained profiles (byte-identical bcm2712/bcm2709): origin/main 5809 -> HEAD 7758 = +1949. getAuthSecret() previously called osiLib.require('scope') unconditionally, violating the default-off contract (a lost osi-lib helper file would 500 every auth endpoint on a flag-off gateway). Fixed per the history-api-router-fn/zone-env-fn reference pattern: getAuthSecret() now checks a freshly-recomputed OSI_SCOPED_ACCESS flag first (recomputed locally rather than closing over any outer scopedOn, avoiding TDZ risk uniformly across every node regardless of its own structure) and only resolves osiLib inside the flag-on branch; the flag-off branch inlines the exact pre-refactor secret-resolution logic (same file paths, same ENOENT-quiet read/generate/write behavior), byte-equivalent to the pre-port route."
    },
    "s2120-zones-get-fn": {
      "delta": 1955,
      "reason": "Supersedes the previous entry (1411). port/wave3-auth-flaggate (fix(auth): flag-gate the shared scope resolver across all auth nodes + hermetic flag-off verifier, follow-up to PR #200 / commit 2c4fa9a5f): measured with verify-flows-size-ratchet's nodeSizes over both maintained profiles (byte-identical bcm2712/bcm2709): origin/main 4180 -> HEAD 6135 = +1955. getAuthSecret() previously called osiLib.require('scope') unconditionally, violating the default-off contract (a lost osi-lib helper file would 500 every auth endpoint on a flag-off gateway). Fixed per the history-api-router-fn/zone-env-fn reference pattern: getAuthSecret() now checks a freshly-recomputed OSI_SCOPED_ACCESS flag first (recomputed locally rather than closing over any outer scopedOn, avoiding TDZ risk uniformly across every node regardless of its own structure) and only resolves osiLib inside the flag-on branch; the flag-off branch inlines the exact pre-refactor secret-resolution logic (same file paths, same ENOENT-quiet read/generate/write behavior), byte-equivalent to the pre-port route."
    },
    "al-status-decode": {
      "delta": 1951,
      "reason": "Supersedes the previous entry (761). port/wave3-auth-flaggate (fix(auth): flag-gate the shared scope resolver across all auth nodes + hermetic flag-off verifier, follow-up to PR #200 / commit 2c4fa9a5f): measured with verify-flows-size-ratchet's nodeSizes over both maintained profiles (byte-identical bcm2712/bcm2709): origin/main 3807 -> HEAD 5758 = +1951. getAuthSecret() previously called osiLib.require('scope') unconditionally, violating the default-off contract (a lost osi-lib helper file would 500 every auth endpoint on a flag-off gateway). Fixed per the history-api-router-fn/zone-env-fn reference pattern: getAuthSecret() now checks a freshly-recomputed OSI_SCOPED_ACCESS flag first (recomputed locally rather than closing over any outer scopedOn, avoiding TDZ risk uniformly across every node regardless of its own structure) and only resolves osiLib inside the flag-on branch; the flag-off branch inlines the exact pre-refactor secret-resolution logic (same file paths, same ENOENT-quiet read/generate/write behavior), byte-equivalent to the pre-port route."
    },
    "get-gateway-location-auth-fn": {
      "delta": 1975,
      "reason": "Supersedes the previous entry (1591). port/wave3-auth-flaggate (fix(auth): flag-gate the shared scope resolver across all auth nodes + hermetic flag-off verifier, follow-up to PR #200 / commit 2c4fa9a5f): measured with verify-flows-size-ratchet's nodeSizes over both maintained profiles (byte-identical bcm2712/bcm2709): origin/main 5159 -> HEAD 7134 = +1975. getAuthSecret() previously called osiLib.require('scope') unconditionally, violating the default-off contract (a lost osi-lib helper file would 500 every auth endpoint on a flag-off gateway). Fixed per the history-api-router-fn/zone-env-fn reference pattern: getAuthSecret() now checks a freshly-recomputed OSI_SCOPED_ACCESS flag first (recomputed locally rather than closing over any outer scopedOn, avoiding TDZ risk uniformly across every node regardless of its own structure) and only resolves osiLib inside the flag-on branch; the flag-off branch inlines the exact pre-refactor secret-resolution logic (same file paths, same ENOENT-quiet read/generate/write behavior), byte-equivalent to the pre-port route."
    },
    "improvement-requests-api-router": {
      "delta": 1981,
      "reason": "Supersedes the previous entry (635). port/wave3-auth-flaggate (fix(auth): flag-gate the shared scope resolver across all auth nodes + hermetic flag-off verifier, follow-up to PR #200 / commit 2c4fa9a5f): measured with verify-flows-size-ratchet's nodeSizes over both maintained profiles (byte-identical bcm2712/bcm2709): origin/main 18307 -> HEAD 20288 = +1981. getAuthSecret() previously called osiLib.require('scope') unconditionally, violating the default-off contract (a lost osi-lib helper file would 500 every auth endpoint on a flag-off gateway). Fixed per the history-api-router-fn/zone-env-fn reference pattern: getAuthSecret() now checks a freshly-recomputed OSI_SCOPED_ACCESS flag first (recomputed locally rather than closing over any outer scopedOn, avoiding TDZ risk uniformly across every node regardless of its own structure) and only resolves osiLib inside the flag-on branch; the flag-off branch inlines the exact pre-refactor secret-resolution logic (same file paths, same ENOENT-quiet read/generate/write behavior), byte-equivalent to the pre-port route."
    },
    "journal-api-router-fn": {
      "delta": 229,
      "reason": "Wave 3 scoped-access port (AgroLink 46f1d0fc, feat(journal): apply union rule to reads, auto-merged cleanly): measured on port/wave3-edge-scope with verify-flows-size-ratchet's nodeSizes over both maintained profiles (byte-identical bcm2712/bcm2709): origin/main 1020 -> HEAD 1249 = +229. Journal HTTP requests now resolve an optional scope module (osiLib.require('scope'), gated on OSI_SCOPED_ACCESS) and pass it through to osiJournal.handleHttpRequest as scope/scopedMode, replacing the previous direct osiJournal-only dispatch."
    },
    "analysis-api-router-fn": {
      "delta": 1963,
      "reason": "Supersedes the previous entry (1145). port/wave3-auth-flaggate (fix(auth): flag-gate the shared scope resolver across all auth nodes + hermetic flag-off verifier, follow-up to PR #200 / commit 2c4fa9a5f): measured with verify-flows-size-ratchet's nodeSizes over both maintained profiles (byte-identical bcm2712/bcm2709): origin/main 8456 -> HEAD 10419 = +1963. getAuthSecret() previously called osiLib.require('scope') unconditionally, violating the default-off contract (a lost osi-lib helper file would 500 every auth endpoint on a flag-off gateway). Fixed per the history-api-router-fn/zone-env-fn reference pattern: getAuthSecret() now checks a freshly-recomputed OSI_SCOPED_ACCESS flag first (recomputed locally rather than closing over any outer scopedOn, avoiding TDZ risk uniformly across every node regardless of its own structure) and only resolves osiLib inside the flag-on branch; the flag-off branch inlines the exact pre-refactor secret-resolution logic (same file paths, same ENOENT-quiet read/generate/write behavior), byte-equivalent to the pre-port route."
    },
    "83bb4a452dd9ae37": {
      "delta": 2423,
      "reason": "origin/main -> HEAD delta 2423. Wave 3 scoped-access port, commit E6 (adapted from AgroLink 17462573a, \"fix(scope): replace viewer-denylist with a canMutate() allowlist on every mutation gate\"): role === 'viewer' denylist checks replaced with !scope.canMutate(role) allowlist checks. Re-measured fresh with verify-flows-size-ratchet nodeSizes (func.length) over both maintained profiles. Supersedes prior entry: Wave 3 scoped-access port (AgroLink 832d48ef, feat(api): enforce fresh scope on valve effects): measured on port/wave3-edge-scope with verify-flows-size-ratchet's nodeSizes over both maintained profiles (byte-identical bcm2712/bcm2709): origin/main 5883 -> HEAD 8291 = +2408. This is the STREGA/valve command boundary node (Auth + Validate + Normalize): when OSI_SCOPED_ACCESS=1, resolves the caller's own user_uuid and calls osi-scope-helper's assertFreshDeviceAccess against the target deveui (rejecting a viewer role with 403), stamping msg.actor_user_uuid for the downstream write; verifyBearer tags msg._osiAuthFailure on each 401 path; two secret read/write catches converted to visible node.warn."
    },
    "dde8e1ef265e96d7": {
      "delta": 121,
      "reason": "Wave 3 scoped-access port (AgroLink 832d48ef, feat(api): enforce fresh scope on valve effects), auto-merged cleanly: measured on port/wave3-edge-scope with verify-flows-size-ratchet's nodeSizes over both maintained profiles (byte-identical bcm2712/bcm2709): origin/main 3531 -> HEAD 3652 = +121. Build actuator_command + DB writes threads the upstream actor_user_uuid through to the command-ledger write."
    },
    "cancel-strega-actuation-fn": {
      "delta": 1971,
      "reason": "Supersedes the previous entry (1958). port/wave3-auth-flaggate (fix(auth): flag-gate the shared scope resolver across all auth nodes + hermetic flag-off verifier, follow-up to PR #200 / commit 2c4fa9a5f): measured with verify-flows-size-ratchet's nodeSizes over both maintained profiles (byte-identical bcm2712/bcm2709): origin/main 7924 -> HEAD 9895 = +1971. getAuthSecret() previously called osiLib.require('scope') unconditionally, violating the default-off contract (a lost osi-lib helper file would 500 every auth endpoint on a flag-off gateway). Fixed per the history-api-router-fn/zone-env-fn reference pattern: getAuthSecret() now checks a freshly-recomputed OSI_SCOPED_ACCESS flag first (recomputed locally rather than closing over any outer scopedOn, avoiding TDZ risk uniformly across every node regardless of its own structure) and only resolves osiLib inside the flag-on branch; the flag-off branch inlines the exact pre-refactor secret-resolution logic (same file paths, same ENOENT-quiet read/generate/write behavior), byte-equivalent to the pre-port route."
    },
    "70fcbea336401bd1": {
      "delta": 2894,
      "reason": "origin/main -> HEAD delta 2894. Wave 3 scoped-access port, commit E6 (adapted from AgroLink 17462573a, \"fix(scope): replace viewer-denylist with a canMutate() allowlist on every mutation gate\"): role === 'viewer' denylist checks replaced with !scope.canMutate(role) allowlist checks. Re-measured fresh with verify-flows-size-ratchet nodeSizes (func.length) over both maintained profiles. Supersedes prior entry: Wave 3 scoped-access port (AgroLink a6289e96, feat(api): enforce schedule scope and authority): measured on port/wave3-edge-scope with verify-flows-size-ratchet's nodeSizes over both maintained profiles (byte-identical bcm2712/bcm2709): origin/main 2981 -> HEAD 5860 = +2879. Schedule-mutation boundary node (Decode Token): when OSI_SCOPED_ACCESS=1, resolves the target zone by id, asserts fresh zone access via osi-scope-helper (403 on a viewer role), and stamps msg.actor_user_uuid; verifyBearer tags msg._osiAuthFailure on each 401 path; two secret read/write catches converted to visible node.warn."
    },
    "22cc64fa2a899cea": {
      "delta": 103,
      "reason": "Wave 3 scoped-access port (AgroLink a6289e96, feat(api): enforce schedule scope and authority), auto-merged cleanly: measured on port/wave3-edge-scope with verify-flows-size-ratchet's nodeSizes over both maintained profiles (byte-identical bcm2712/bcm2709): origin/main 4256 -> HEAD 4359 = +103. Verify Zone Ownership threads the upstream actor_user_uuid through."
    },
    "settings-disable-schedules-fn": {
      "delta": 2992,
      "reason": "origin/main -> HEAD delta 2992. Wave 3 scoped-access port, commit E8 (adapted from AgroLink 76c40e4c9, \"fix(scope): scope disable-all-schedules admins to owned-plus-granted zones\"): removed the role===admin exemption from the zone filter, so an admin bulk-disable now only touches owned-plus-granted zones like every other scoped write surface, not fleet-wide. Re-measured fresh with verify-flows-size-ratchet nodeSizes (func.length). Supersedes prior entry: origin/main -> HEAD delta 2490. Wave 3 scoped-access port, commit E6 (adapted from AgroLink 17462573a, \"fix(scope): replace viewer-denylist with a canMutate() allowlist on every mutation gate\"): role === 'viewer' denylist checks replaced with !scope.canMutate(role) allowlist checks. Re-measured fresh with verify-flows-size-ratchet nodeSizes (func.length) over both maintained profiles. Supersedes prior entry: Wave 3 scoped-access port (AgroLink a6289e96, feat(api): enforce schedule scope and authority): measured on port/wave3-edge-scope with verify-flows-size-ratchet's nodeSizes over both maintained profiles (byte-identical bcm2712/bcm2709): origin/main 4249 -> HEAD 6724 = +2475. When OSI_SCOPED_ACCESS=1, resolves the actor's role via osi-scope-helper's assertFreshRole and scopes the bulk schedule-disable UPDATE to owned-plus-granted zone_uuids for a non-admin actor (admin disables account-wide, matching unscoped semantics); verifyBearer tags msg._osiAuthFailure on each 401 path; one token-parse catch converted to visible node.warn."
    },
    "a0a61f4b7dca1c2e": {
      "delta": 383,
      "reason": "Wave 3 scoped-access port (AgroLink a6289e96, feat(api): enforce schedule scope and authority), auto-merged cleanly: measured on port/wave3-edge-scope with verify-flows-size-ratchet's nodeSizes over both maintained profiles (byte-identical bcm2712/bcm2709): origin/main 882 -> HEAD 1265 = +383. Build zones query (enabled schedules) threads the upstream actor_user_uuid/scope context through to its own zone query."
    },
    "post-devices-response": {
      "delta": 90,
      "reason": "Supersedes the prior entry (70). Wave 3 scoped-access port, pick-list commit 63 (AgroLink 7ddbe5dd4, \"fix(zones): preserve device assignments during scoped registration\"): the response payload now reflects msg._deviceEffectiveZoneId when set (the COALESCE-preserved existing zone on a re-post), falling back to the prior _deviceZoneId/_scopedTargetZoneId behavior (+20, exact vs the prior entry). origin/main 933 -> HEAD 1023 = 90, exact delta vs origin/main. Re-measured fresh with verify-flows-size-ratchet nodeSizes (func.length) over both maintained profiles. Prior provenance retained: AgroLink ace19a13 (feat(api): scoped device provisioning R2/R5) made the response payload's irrigation_zone_id reflect the scoped target zone instead of always null."
    },
    "dendro-location-fn": {
      "delta": 1955,
      "reason": "Supersedes the previous entry (120). port/wave3-auth-flaggate (fix(auth): flag-gate the shared scope resolver across all auth nodes + hermetic flag-off verifier, follow-up to PR #200 / commit 2c4fa9a5f): measured with verify-flows-size-ratchet's nodeSizes over both maintained profiles (byte-identical bcm2712/bcm2709): origin/main 3778 -> HEAD 5733 = +1955. getAuthSecret() previously called osiLib.require('scope') unconditionally, violating the default-off contract (a lost osi-lib helper file would 500 every auth endpoint on a flag-off gateway). Fixed per the history-api-router-fn/zone-env-fn reference pattern: getAuthSecret() now checks a freshly-recomputed OSI_SCOPED_ACCESS flag first (recomputed locally rather than closing over any outer scopedOn, avoiding TDZ risk uniformly across every node regardless of its own structure) and only resolves osiLib inside the flag-on branch; the flag-off branch inlines the exact pre-refactor secret-resolution logic (same file paths, same ENOENT-quiet read/generate/write behavior), byte-equivalent to the pre-port route."
    },
    "zone-config-fn": {
      "delta": 1947,
      "reason": "Supersedes the previous entry (180). port/wave3-auth-flaggate (fix(auth): flag-gate the shared scope resolver across all auth nodes + hermetic flag-off verifier, follow-up to PR #200 / commit 2c4fa9a5f): measured with verify-flows-size-ratchet's nodeSizes over both maintained profiles (byte-identical bcm2712/bcm2709): origin/main 7759 -> HEAD 9706 = +1947. getAuthSecret() previously called osiLib.require('scope') unconditionally, violating the default-off contract (a lost osi-lib helper file would 500 every auth endpoint on a flag-off gateway). Fixed per the history-api-router-fn/zone-env-fn reference pattern: getAuthSecret() now checks a freshly-recomputed OSI_SCOPED_ACCESS flag first (recomputed locally rather than closing over any outer scopedOn, avoiding TDZ risk uniformly across every node regardless of its own structure) and only resolves osiLib inside the flag-on branch; the flag-off branch inlines the exact pre-refactor secret-resolution logic (same file paths, same ENOENT-quiet read/generate/write behavior), byte-equivalent to the pre-port route."
    },
    "zone-calibration-fn": {
      "delta": 1957,
      "reason": "Supersedes the previous entry (60). port/wave3-auth-flaggate (fix(auth): flag-gate the shared scope resolver across all auth nodes + hermetic flag-off verifier, follow-up to PR #200 / commit 2c4fa9a5f): measured with verify-flows-size-ratchet's nodeSizes over both maintained profiles (byte-identical bcm2712/bcm2709): origin/main 5775 -> HEAD 7732 = +1957. getAuthSecret() previously called osiLib.require('scope') unconditionally, violating the default-off contract (a lost osi-lib helper file would 500 every auth endpoint on a flag-off gateway). Fixed per the history-api-router-fn/zone-env-fn reference pattern: getAuthSecret() now checks a freshly-recomputed OSI_SCOPED_ACCESS flag first (recomputed locally rather than closing over any outer scopedOn, avoiding TDZ risk uniformly across every node regardless of its own structure) and only resolves osiLib inside the flag-on branch; the flag-off branch inlines the exact pre-refactor secret-resolution logic (same file paths, same ENOENT-quiet read/generate/write behavior), byte-equivalent to the pre-port route."
    },
    "put-strega-timed-auth-fn": {
      "delta": 2836,
      "reason": "Supersedes the prior entry (2692). Wave 3 scoped-access port, pick-list commit 65 (AgroLink d2e81bd6a, \"fix(edge): close auth and scoped-access review gaps\"): getAuthSecret() now delegates to osi-scope-helper's shared resolveAuthSecret via osiLib.require('scope') instead of duplicating the persisted-secret file-read/generate/write logic inline, and verifyBearer tags each failure with a stable _osiAuthFailure {format,code,sourceId} for the shared auth-catch-http500 response node to surface as a real HTTP error instead of an unhandled-exception 500. Re-measured fresh with verify-flows-size-ratchet nodeSizes (func.length) over both maintained profiles, exact delta vs origin/main."
    },
    "put-strega-advanced-authorize-fn": {
      "delta": 630,
      "reason": "origin/main -> HEAD delta 630. Wave 3 scoped-access port, commit X2 (adapted from AgroLink 0913a6902, \"fix(actuation): close the GUI STREGA advanced chain's scope gap\"): the GUI STREGA advanced chain (put-strega-timed-auth-fn -> put-strega-advanced-lookup -> put-strega-advanced-authorize-fn -> To Actuator) did bearer auth only, so a TIMED_ACTION OPEN physical command reached write-strega-expectation with no actor and was rejected while the HTTP response had already reported success. put-strega-timed-auth-fn now runs a fresh assertFreshDeviceAccess+canMutate check and sets msg.actor_user_uuid; put-strega-advanced-authorize-fn copies it onto every actuatorMsg it builds. Re-measured fresh with verify-flows-size-ratchet nodeSizes (func.length)."
    },
    "get-devices-auth": {
      "delta": 1951,
      "reason": "Supersedes the previous entry (97). port/wave3-auth-flaggate (fix(auth): flag-gate the shared scope resolver across all auth nodes + hermetic flag-off verifier, follow-up to PR #200 / commit 2c4fa9a5f): measured with verify-flows-size-ratchet's nodeSizes over both maintained profiles (byte-identical bcm2712/bcm2709): origin/main 3050 -> HEAD 5001 = +1951. getAuthSecret() previously called osiLib.require('scope') unconditionally, violating the default-off contract (a lost osi-lib helper file would 500 every auth endpoint on a flag-off gateway). Fixed per the history-api-router-fn/zone-env-fn reference pattern: getAuthSecret() now checks a freshly-recomputed OSI_SCOPED_ACCESS flag first (recomputed locally rather than closing over any outer scopedOn, avoiding TDZ risk uniformly across every node regardless of its own structure) and only resolves osiLib inside the flag-on branch; the flag-off branch inlines the exact pre-refactor secret-resolution logic (same file paths, same ENOENT-quiet read/generate/write behavior), byte-equivalent to the pre-port route."
    },
    "delete-device-auth": {
      "delta": 1955,
      "reason": "Supersedes the previous entry (109). port/wave3-auth-flaggate (fix(auth): flag-gate the shared scope resolver across all auth nodes + hermetic flag-off verifier, follow-up to PR #200 / commit 2c4fa9a5f): measured with verify-flows-size-ratchet's nodeSizes over both maintained profiles (byte-identical bcm2712/bcm2709): origin/main 3189 -> HEAD 5144 = +1955. getAuthSecret() previously called osiLib.require('scope') unconditionally, violating the default-off contract (a lost osi-lib helper file would 500 every auth endpoint on a flag-off gateway). Fixed per the history-api-router-fn/zone-env-fn reference pattern: getAuthSecret() now checks a freshly-recomputed OSI_SCOPED_ACCESS flag first (recomputed locally rather than closing over any outer scopedOn, avoiding TDZ risk uniformly across every node regardless of its own structure) and only resolves osiLib inside the flag-on branch; the flag-off branch inlines the exact pre-refactor secret-resolution logic (same file paths, same ENOENT-quiet read/generate/write behavior), byte-equivalent to the pre-port route."
    },
    "get-zones-auth": {
      "delta": 1947,
      "reason": "Supersedes the previous entry (85). port/wave3-auth-flaggate (fix(auth): flag-gate the shared scope resolver across all auth nodes + hermetic flag-off verifier, follow-up to PR #200 / commit 2c4fa9a5f): measured with verify-flows-size-ratchet's nodeSizes over both maintained profiles (byte-identical bcm2712/bcm2709): origin/main 3038 -> HEAD 4985 = +1947. getAuthSecret() previously called osiLib.require('scope') unconditionally, violating the default-off contract (a lost osi-lib helper file would 500 every auth endpoint on a flag-off gateway). Fixed per the history-api-router-fn/zone-env-fn reference pattern: getAuthSecret() now checks a freshly-recomputed OSI_SCOPED_ACCESS flag first (recomputed locally rather than closing over any outer scopedOn, avoiding TDZ risk uniformly across every node regardless of its own structure) and only resolves osiLib inside the flag-on branch; the flag-off branch inlines the exact pre-refactor secret-resolution logic (same file paths, same ENOENT-quiet read/generate/write behavior), byte-equivalent to the pre-port route."
    },
    "delete-zone-auth": {
      "delta": 1951,
      "reason": "Supersedes the previous entry (97). port/wave3-auth-flaggate (fix(auth): flag-gate the shared scope resolver across all auth nodes + hermetic flag-off verifier, follow-up to PR #200 / commit 2c4fa9a5f): measured with verify-flows-size-ratchet's nodeSizes over both maintained profiles (byte-identical bcm2712/bcm2709): origin/main 3050 -> HEAD 5001 = +1951. getAuthSecret() previously called osiLib.require('scope') unconditionally, violating the default-off contract (a lost osi-lib helper file would 500 every auth endpoint on a flag-off gateway). Fixed per the history-api-router-fn/zone-env-fn reference pattern: getAuthSecret() now checks a freshly-recomputed OSI_SCOPED_ACCESS flag first (recomputed locally rather than closing over any outer scopedOn, avoiding TDZ risk uniformly across every node regardless of its own structure) and only resolves osiLib inside the flag-on branch; the flag-off branch inlines the exact pre-refactor secret-resolution logic (same file paths, same ENOENT-quiet read/generate/write behavior), byte-equivalent to the pre-port route."
    },
    "assign-device-auth": {
      "delta": 1955,
      "reason": "Supersedes the previous entry (109). port/wave3-auth-flaggate (fix(auth): flag-gate the shared scope resolver across all auth nodes + hermetic flag-off verifier, follow-up to PR #200 / commit 2c4fa9a5f): measured with verify-flows-size-ratchet's nodeSizes over both maintained profiles (byte-identical bcm2712/bcm2709): origin/main 3062 -> HEAD 5017 = +1955. getAuthSecret() previously called osiLib.require('scope') unconditionally, violating the default-off contract (a lost osi-lib helper file would 500 every auth endpoint on a flag-off gateway). Fixed per the history-api-router-fn/zone-env-fn reference pattern: getAuthSecret() now checks a freshly-recomputed OSI_SCOPED_ACCESS flag first (recomputed locally rather than closing over any outer scopedOn, avoiding TDZ risk uniformly across every node regardless of its own structure) and only resolves osiLib inside the flag-on branch; the flag-off branch inlines the exact pre-refactor secret-resolution logic (same file paths, same ENOENT-quiet read/generate/write behavior), byte-equivalent to the pre-port route."
    },
    "unassign-device-auth": {
      "delta": 1959,
      "reason": "Supersedes the previous entry (121). port/wave3-auth-flaggate (fix(auth): flag-gate the shared scope resolver across all auth nodes + hermetic flag-off verifier, follow-up to PR #200 / commit 2c4fa9a5f): measured with verify-flows-size-ratchet's nodeSizes over both maintained profiles (byte-identical bcm2712/bcm2709): origin/main 3074 -> HEAD 5033 = +1959. getAuthSecret() previously called osiLib.require('scope') unconditionally, violating the default-off contract (a lost osi-lib helper file would 500 every auth endpoint on a flag-off gateway). Fixed per the history-api-router-fn/zone-env-fn reference pattern: getAuthSecret() now checks a freshly-recomputed OSI_SCOPED_ACCESS flag first (recomputed locally rather than closing over any outer scopedOn, avoiding TDZ risk uniformly across every node regardless of its own structure) and only resolves osiLib inside the flag-on branch; the flag-off branch inlines the exact pre-refactor secret-resolution logic (same file paths, same ENOENT-quiet read/generate/write behavior), byte-equivalent to the pre-port route."
    },
    "put-dendro-auth-fn": {
      "delta": 1955,
      "reason": "Supersedes the previous entry (109). port/wave3-auth-flaggate (fix(auth): flag-gate the shared scope resolver across all auth nodes + hermetic flag-off verifier, follow-up to PR #200 / commit 2c4fa9a5f): measured with verify-flows-size-ratchet's nodeSizes over both maintained profiles (byte-identical bcm2712/bcm2709): origin/main 3482 -> HEAD 5437 = +1955. getAuthSecret() previously called osiLib.require('scope') unconditionally, violating the default-off contract (a lost osi-lib helper file would 500 every auth endpoint on a flag-off gateway). Fixed per the history-api-router-fn/zone-env-fn reference pattern: getAuthSecret() now checks a freshly-recomputed OSI_SCOPED_ACCESS flag first (recomputed locally rather than closing over any outer scopedOn, avoiding TDZ risk uniformly across every node regardless of its own structure) and only resolves osiLib inside the flag-on branch; the flag-off branch inlines the exact pre-refactor secret-resolution logic (same file paths, same ENOENT-quiet read/generate/write behavior), byte-equivalent to the pre-port route."
    },
    "put-temp-auth-fn": {
      "delta": 1951,
      "reason": "Supersedes the previous entry (97). port/wave3-auth-flaggate (fix(auth): flag-gate the shared scope resolver across all auth nodes + hermetic flag-off verifier, follow-up to PR #200 / commit 2c4fa9a5f): measured with verify-flows-size-ratchet's nodeSizes over both maintained profiles (byte-identical bcm2712/bcm2709): origin/main 3466 -> HEAD 5417 = +1951. getAuthSecret() previously called osiLib.require('scope') unconditionally, violating the default-off contract (a lost osi-lib helper file would 500 every auth endpoint on a flag-off gateway). Fixed per the history-api-router-fn/zone-env-fn reference pattern: getAuthSecret() now checks a freshly-recomputed OSI_SCOPED_ACCESS flag first (recomputed locally rather than closing over any outer scopedOn, avoiding TDZ risk uniformly across every node regardless of its own structure) and only resolves osiLib inside the flag-on branch; the flag-off branch inlines the exact pre-refactor secret-resolution logic (same file paths, same ENOENT-quiet read/generate/write behavior), byte-equivalent to the pre-port route."
    },
    "put-lsn50-mode-auth-fn": {
      "delta": 1963,
      "reason": "Supersedes the previous entry (133). port/wave3-auth-flaggate (fix(auth): flag-gate the shared scope resolver across all auth nodes + hermetic flag-off verifier, follow-up to PR #200 / commit 2c4fa9a5f): measured with verify-flows-size-ratchet's nodeSizes over both maintained profiles (byte-identical bcm2712/bcm2709): origin/main 4292 -> HEAD 6255 = +1963. getAuthSecret() previously called osiLib.require('scope') unconditionally, violating the default-off contract (a lost osi-lib helper file would 500 every auth endpoint on a flag-off gateway). Fixed per the history-api-router-fn/zone-env-fn reference pattern: getAuthSecret() now checks a freshly-recomputed OSI_SCOPED_ACCESS flag first (recomputed locally rather than closing over any outer scopedOn, avoiding TDZ risk uniformly across every node regardless of its own structure) and only resolves osiLib inside the flag-on branch; the flag-off branch inlines the exact pre-refactor secret-resolution logic (same file paths, same ENOENT-quiet read/generate/write behavior), byte-equivalent to the pre-port route."
    },
    "put-lsn50-interval-auth-fn": {
      "delta": 1971,
      "reason": "Supersedes the previous entry (156). port/wave3-auth-flaggate (fix(auth): flag-gate the shared scope resolver across all auth nodes + hermetic flag-off verifier, follow-up to PR #200 / commit 2c4fa9a5f): measured with verify-flows-size-ratchet's nodeSizes over both maintained profiles (byte-identical bcm2712/bcm2709): origin/main 4474 -> HEAD 6445 = +1971. getAuthSecret() previously called osiLib.require('scope') unconditionally, violating the default-off contract (a lost osi-lib helper file would 500 every auth endpoint on a flag-off gateway). Fixed per the history-api-router-fn/zone-env-fn reference pattern: getAuthSecret() now checks a freshly-recomputed OSI_SCOPED_ACCESS flag first (recomputed locally rather than closing over any outer scopedOn, avoiding TDZ risk uniformly across every node regardless of its own structure) and only resolves osiLib inside the flag-on branch; the flag-off branch inlines the exact pre-refactor secret-resolution logic (same file paths, same ENOENT-quiet read/generate/write behavior), byte-equivalent to the pre-port route."
    },
    "put-kiwi-interval-auth-fn": {
      "delta": 1969,
      "reason": "Supersedes the previous entry (150). port/wave3-auth-flaggate (fix(auth): flag-gate the shared scope resolver across all auth nodes + hermetic flag-off verifier, follow-up to PR #200 / commit 2c4fa9a5f): measured with verify-flows-size-ratchet's nodeSizes over both maintained profiles (byte-identical bcm2712/bcm2709): origin/main 4375 -> HEAD 6344 = +1969. getAuthSecret() previously called osiLib.require('scope') unconditionally, violating the default-off contract (a lost osi-lib helper file would 500 every auth endpoint on a flag-off gateway). Fixed per the history-api-router-fn/zone-env-fn reference pattern: getAuthSecret() now checks a freshly-recomputed OSI_SCOPED_ACCESS flag first (recomputed locally rather than closing over any outer scopedOn, avoiding TDZ risk uniformly across every node regardless of its own structure) and only resolves osiLib inside the flag-on branch; the flag-off branch inlines the exact pre-refactor secret-resolution logic (same file paths, same ENOENT-quiet read/generate/write behavior), byte-equivalent to the pre-port route."
    },
    "post-kiwi-enable-auth-fn": {
      "delta": 1967,
      "reason": "Supersedes the previous entry (145). port/wave3-auth-flaggate (fix(auth): flag-gate the shared scope resolver across all auth nodes + hermetic flag-off verifier, follow-up to PR #200 / commit 2c4fa9a5f): measured with verify-flows-size-ratchet's nodeSizes over both maintained profiles (byte-identical bcm2712/bcm2709): origin/main 4345 -> HEAD 6312 = +1967. getAuthSecret() previously called osiLib.require('scope') unconditionally, violating the default-off contract (a lost osi-lib helper file would 500 every auth endpoint on a flag-off gateway). Fixed per the history-api-router-fn/zone-env-fn reference pattern: getAuthSecret() now checks a freshly-recomputed OSI_SCOPED_ACCESS flag first (recomputed locally rather than closing over any outer scopedOn, avoiding TDZ risk uniformly across every node regardless of its own structure) and only resolves osiLib inside the flag-on branch; the flag-off branch inlines the exact pre-refactor secret-resolution logic (same file paths, same ENOENT-quiet read/generate/write behavior), byte-equivalent to the pre-port route."
    },
    "put-strega-interval-auth-fn": {
      "delta": 1973,
      "reason": "Supersedes the previous entry (162). port/wave3-auth-flaggate (fix(auth): flag-gate the shared scope resolver across all auth nodes + hermetic flag-off verifier, follow-up to PR #200 / commit 2c4fa9a5f): measured with verify-flows-size-ratchet's nodeSizes over both maintained profiles (byte-identical bcm2712/bcm2709): origin/main 5005 -> HEAD 6978 = +1973. getAuthSecret() previously called osiLib.require('scope') unconditionally, violating the default-off contract (a lost osi-lib helper file would 500 every auth endpoint on a flag-off gateway). Fixed per the history-api-router-fn/zone-env-fn reference pattern: getAuthSecret() now checks a freshly-recomputed OSI_SCOPED_ACCESS flag first (recomputed locally rather than closing over any outer scopedOn, avoiding TDZ risk uniformly across every node regardless of its own structure) and only resolves osiLib inside the flag-on branch; the flag-off branch inlines the exact pre-refactor secret-resolution logic (same file paths, same ENOENT-quiet read/generate/write behavior), byte-equivalent to the pre-port route."
    },
    "put-lsn50-interrupt-auth-fn": {
      "delta": 1973,
      "reason": "Supersedes the previous entry (163). port/wave3-auth-flaggate (fix(auth): flag-gate the shared scope resolver across all auth nodes + hermetic flag-off verifier, follow-up to PR #200 / commit 2c4fa9a5f): measured with verify-flows-size-ratchet's nodeSizes over both maintained profiles (byte-identical bcm2712/bcm2709): origin/main 4349 -> HEAD 6322 = +1973. getAuthSecret() previously called osiLib.require('scope') unconditionally, violating the default-off contract (a lost osi-lib helper file would 500 every auth endpoint on a flag-off gateway). Fixed per the history-api-router-fn/zone-env-fn reference pattern: getAuthSecret() now checks a freshly-recomputed OSI_SCOPED_ACCESS flag first (recomputed locally rather than closing over any outer scopedOn, avoiding TDZ risk uniformly across every node regardless of its own structure) and only resolves osiLib inside the flag-on branch; the flag-off branch inlines the exact pre-refactor secret-resolution logic (same file paths, same ENOENT-quiet read/generate/write behavior), byte-equivalent to the pre-port route."
    },
    "put-lsn50-5v-auth-fn": {
      "delta": 1959,
      "reason": "Supersedes the previous entry (121). port/wave3-auth-flaggate (fix(auth): flag-gate the shared scope resolver across all auth nodes + hermetic flag-off verifier, follow-up to PR #200 / commit 2c4fa9a5f): measured with verify-flows-size-ratchet's nodeSizes over both maintained profiles (byte-identical bcm2712/bcm2709): origin/main 4301 -> HEAD 6260 = +1959. getAuthSecret() previously called osiLib.require('scope') unconditionally, violating the default-off contract (a lost osi-lib helper file would 500 every auth endpoint on a flag-off gateway). Fixed per the history-api-router-fn/zone-env-fn reference pattern: getAuthSecret() now checks a freshly-recomputed OSI_SCOPED_ACCESS flag first (recomputed locally rather than closing over any outer scopedOn, avoiding TDZ risk uniformly across every node regardless of its own structure) and only resolves osiLib inside the flag-on branch; the flag-off branch inlines the exact pre-refactor secret-resolution logic (same file paths, same ENOENT-quiet read/generate/write behavior), byte-equivalent to the pre-port route."
    },
    "put-strega-model-auth-fn": {
      "delta": 1967,
      "reason": "Supersedes the previous entry (144). port/wave3-auth-flaggate (fix(auth): flag-gate the shared scope resolver across all auth nodes + hermetic flag-off verifier, follow-up to PR #200 / commit 2c4fa9a5f): measured with verify-flows-size-ratchet's nodeSizes over both maintained profiles (byte-identical bcm2712/bcm2709): origin/main 3838 -> HEAD 5805 = +1967. getAuthSecret() previously called osiLib.require('scope') unconditionally, violating the default-off contract (a lost osi-lib helper file would 500 every auth endpoint on a flag-off gateway). Fixed per the history-api-router-fn/zone-env-fn reference pattern: getAuthSecret() now checks a freshly-recomputed OSI_SCOPED_ACCESS flag first (recomputed locally rather than closing over any outer scopedOn, avoiding TDZ risk uniformly across every node regardless of its own structure) and only resolves osiLib inside the flag-on branch; the flag-off branch inlines the exact pre-refactor secret-resolution logic (same file paths, same ENOENT-quiet read/generate/write behavior), byte-equivalent to the pre-port route."
    },
    "put-strega-magnet-auth-fn": {
      "delta": 1969,
      "reason": "Supersedes the previous entry (150). port/wave3-auth-flaggate (fix(auth): flag-gate the shared scope resolver across all auth nodes + hermetic flag-off verifier, follow-up to PR #200 / commit 2c4fa9a5f): measured with verify-flows-size-ratchet's nodeSizes over both maintained profiles (byte-identical bcm2712/bcm2709): origin/main 4071 -> HEAD 6040 = +1969. getAuthSecret() previously called osiLib.require('scope') unconditionally, violating the default-off contract (a lost osi-lib helper file would 500 every auth endpoint on a flag-off gateway). Fixed per the history-api-router-fn/zone-env-fn reference pattern: getAuthSecret() now checks a freshly-recomputed OSI_SCOPED_ACCESS flag first (recomputed locally rather than closing over any outer scopedOn, avoiding TDZ risk uniformly across every node regardless of its own structure) and only resolves osiLib inside the flag-on branch; the flag-off branch inlines the exact pre-refactor secret-resolution logic (same file paths, same ENOENT-quiet read/generate/write behavior), byte-equivalent to the pre-port route."
    },
    "put-strega-partial-auth-fn": {
      "delta": 1971,
      "reason": "Supersedes the previous entry (156). port/wave3-auth-flaggate (fix(auth): flag-gate the shared scope resolver across all auth nodes + hermetic flag-off verifier, follow-up to PR #200 / commit 2c4fa9a5f): measured with verify-flows-size-ratchet's nodeSizes over both maintained profiles (byte-identical bcm2712/bcm2709): origin/main 4472 -> HEAD 6443 = +1971. getAuthSecret() previously called osiLib.require('scope') unconditionally, violating the default-off contract (a lost osi-lib helper file would 500 every auth endpoint on a flag-off gateway). Fixed per the history-api-router-fn/zone-env-fn reference pattern: getAuthSecret() now checks a freshly-recomputed OSI_SCOPED_ACCESS flag first (recomputed locally rather than closing over any outer scopedOn, avoiding TDZ risk uniformly across every node regardless of its own structure) and only resolves osiLib inside the flag-on branch; the flag-off branch inlines the exact pre-refactor secret-resolution logic (same file paths, same ENOENT-quiet read/generate/write behavior), byte-equivalent to the pre-port route."
    },
    "put-strega-flush-auth-fn": {
      "delta": 1967,
      "reason": "Supersedes the previous entry (144). port/wave3-auth-flaggate (fix(auth): flag-gate the shared scope resolver across all auth nodes + hermetic flag-off verifier, follow-up to PR #200 / commit 2c4fa9a5f): measured with verify-flows-size-ratchet's nodeSizes over both maintained profiles (byte-identical bcm2712/bcm2709): origin/main 4499 -> HEAD 6466 = +1967. getAuthSecret() previously called osiLib.require('scope') unconditionally, violating the default-off contract (a lost osi-lib helper file would 500 every auth endpoint on a flag-off gateway). Fixed per the history-api-router-fn/zone-env-fn reference pattern: getAuthSecret() now checks a freshly-recomputed OSI_SCOPED_ACCESS flag first (recomputed locally rather than closing over any outer scopedOn, avoiding TDZ risk uniformly across every node regardless of its own structure) and only resolves osiLib inside the flag-on branch; the flag-off branch inlines the exact pre-refactor secret-resolution logic (same file paths, same ENOENT-quiet read/generate/write behavior), byte-equivalent to the pre-port route."
    },
    "put-rain-gauge-auth-fn": {
      "delta": 1963,
      "reason": "Supersedes the previous entry (133). port/wave3-auth-flaggate (fix(auth): flag-gate the shared scope resolver across all auth nodes + hermetic flag-off verifier, follow-up to PR #200 / commit 2c4fa9a5f): measured with verify-flows-size-ratchet's nodeSizes over both maintained profiles (byte-identical bcm2712/bcm2709): origin/main 3514 -> HEAD 5477 = +1963. getAuthSecret() previously called osiLib.require('scope') unconditionally, violating the default-off contract (a lost osi-lib helper file would 500 every auth endpoint on a flag-off gateway). Fixed per the history-api-router-fn/zone-env-fn reference pattern: getAuthSecret() now checks a freshly-recomputed OSI_SCOPED_ACCESS flag first (recomputed locally rather than closing over any outer scopedOn, avoiding TDZ risk uniformly across every node regardless of its own structure) and only resolves osiLib inside the flag-on branch; the flag-off branch inlines the exact pre-refactor secret-resolution logic (same file paths, same ENOENT-quiet read/generate/write behavior), byte-equivalent to the pre-port route."
    },
    "put-flow-meter-auth-fn": {
      "delta": 1963,
      "reason": "Supersedes the previous entry (133). port/wave3-auth-flaggate (fix(auth): flag-gate the shared scope resolver across all auth nodes + hermetic flag-off verifier, follow-up to PR #200 / commit 2c4fa9a5f): measured with verify-flows-size-ratchet's nodeSizes over both maintained profiles (byte-identical bcm2712/bcm2709): origin/main 3514 -> HEAD 5477 = +1963. getAuthSecret() previously called osiLib.require('scope') unconditionally, violating the default-off contract (a lost osi-lib helper file would 500 every auth endpoint on a flag-off gateway). Fixed per the history-api-router-fn/zone-env-fn reference pattern: getAuthSecret() now checks a freshly-recomputed OSI_SCOPED_ACCESS flag first (recomputed locally rather than closing over any outer scopedOn, avoiding TDZ risk uniformly across every node regardless of its own structure) and only resolves osiLib inside the flag-on branch; the flag-off branch inlines the exact pre-refactor secret-resolution logic (same file paths, same ENOENT-quiet read/generate/write behavior), byte-equivalent to the pre-port route."
    },
    "s2120-zones-put-auth-fn": {
      "delta": 1965,
      "reason": "Supersedes the previous entry (480). port/wave3-auth-flaggate (fix(auth): flag-gate the shared scope resolver across all auth nodes + hermetic flag-off verifier, follow-up to PR #200 / commit 2c4fa9a5f): measured with verify-flows-size-ratchet's nodeSizes over both maintained profiles (byte-identical bcm2712/bcm2709): origin/main 5185 -> HEAD 7150 = +1965. getAuthSecret() previously called osiLib.require('scope') unconditionally, violating the default-off contract (a lost osi-lib helper file would 500 every auth endpoint on a flag-off gateway). Fixed per the history-api-router-fn/zone-env-fn reference pattern: getAuthSecret() now checks a freshly-recomputed OSI_SCOPED_ACCESS flag first (recomputed locally rather than closing over any outer scopedOn, avoiding TDZ risk uniformly across every node regardless of its own structure) and only resolves osiLib inside the flag-on branch; the flag-off branch inlines the exact pre-refactor secret-resolution logic (same file paths, same ENOENT-quiet read/generate/write behavior), byte-equivalent to the pre-port route."
    },
    "put-soil-depth-fn": {
      "delta": 1953,
      "reason": "Supersedes the previous entry (103). port/wave3-auth-flaggate (fix(auth): flag-gate the shared scope resolver across all auth nodes + hermetic flag-off verifier, follow-up to PR #200 / commit 2c4fa9a5f): measured with verify-flows-size-ratchet's nodeSizes over both maintained profiles (byte-identical bcm2712/bcm2709): origin/main 5842 -> HEAD 7795 = +1953. getAuthSecret() previously called osiLib.require('scope') unconditionally, violating the default-off contract (a lost osi-lib helper file would 500 every auth endpoint on a flag-off gateway). Fixed per the history-api-router-fn/zone-env-fn reference pattern: getAuthSecret() now checks a freshly-recomputed OSI_SCOPED_ACCESS flag first (recomputed locally rather than closing over any outer scopedOn, avoiding TDZ risk uniformly across every node regardless of its own structure) and only resolves osiLib inside the flag-on branch; the flag-off branch inlines the exact pre-refactor secret-resolution logic (same file paths, same ENOENT-quiet read/generate/write behavior), byte-equivalent to the pre-port route."
    },
    "put-dendro-config-auth-fn": {
      "delta": 1969,
      "reason": "Supersedes the previous entry (151). port/wave3-auth-flaggate (fix(auth): flag-gate the shared scope resolver across all auth nodes + hermetic flag-off verifier, follow-up to PR #200 / commit 2c4fa9a5f): measured with verify-flows-size-ratchet's nodeSizes over both maintained profiles (byte-identical bcm2712/bcm2709): origin/main 9729 -> HEAD 11698 = +1969. getAuthSecret() previously called osiLib.require('scope') unconditionally, violating the default-off contract (a lost osi-lib helper file would 500 every auth endpoint on a flag-off gateway). Fixed per the history-api-router-fn/zone-env-fn reference pattern: getAuthSecret() now checks a freshly-recomputed OSI_SCOPED_ACCESS flag first (recomputed locally rather than closing over any outer scopedOn, avoiding TDZ risk uniformly across every node regardless of its own structure) and only resolves osiLib inside the flag-on branch; the flag-off branch inlines the exact pre-refactor secret-resolution logic (same file paths, same ENOENT-quiet read/generate/write behavior), byte-equivalent to the pre-port route."
    },
    "post-dendro-baseline-reset-auth-fn": {
      "delta": 1987,
      "reason": "Supersedes the previous entry (205). port/wave3-auth-flaggate (fix(auth): flag-gate the shared scope resolver across all auth nodes + hermetic flag-off verifier, follow-up to PR #200 / commit 2c4fa9a5f): measured with verify-flows-size-ratchet's nodeSizes over both maintained profiles (byte-identical bcm2712/bcm2709): origin/main 4780 -> HEAD 6767 = +1987. getAuthSecret() previously called osiLib.require('scope') unconditionally, violating the default-off contract (a lost osi-lib helper file would 500 every auth endpoint on a flag-off gateway). Fixed per the history-api-router-fn/zone-env-fn reference pattern: getAuthSecret() now checks a freshly-recomputed OSI_SCOPED_ACCESS flag first (recomputed locally rather than closing over any outer scopedOn, avoiding TDZ risk uniformly across every node regardless of its own structure) and only resolves osiLib inside the flag-on branch; the flag-off branch inlines the exact pre-refactor secret-resolution logic (same file paths, same ENOENT-quiet read/generate/write behavior), byte-equivalent to the pre-port route."
    },
    "44e7d74ff3668e01": {
      "delta": 1951,
      "reason": "Supersedes the previous entry (97). port/wave3-auth-flaggate (fix(auth): flag-gate the shared scope resolver across all auth nodes + hermetic flag-off verifier, follow-up to PR #200 / commit 2c4fa9a5f): measured with verify-flows-size-ratchet's nodeSizes over both maintained profiles (byte-identical bcm2712/bcm2709): origin/main 3012 -> HEAD 4963 = +1951. getAuthSecret() previously called osiLib.require('scope') unconditionally, violating the default-off contract (a lost osi-lib helper file would 500 every auth endpoint on a flag-off gateway). Fixed per the history-api-router-fn/zone-env-fn reference pattern: getAuthSecret() now checks a freshly-recomputed OSI_SCOPED_ACCESS flag first (recomputed locally rather than closing over any outer scopedOn, avoiding TDZ risk uniformly across every node regardless of its own structure) and only resolves osiLib inside the flag-on branch; the flag-off branch inlines the exact pre-refactor secret-resolution logic (same file paths, same ENOENT-quiet read/generate/write behavior), byte-equivalent to the pre-port route."
    },
    "8b93fa005d78e25f": {
      "delta": 1951,
      "reason": "Supersedes the previous entry (97). port/wave3-auth-flaggate (fix(auth): flag-gate the shared scope resolver across all auth nodes + hermetic flag-off verifier, follow-up to PR #200 / commit 2c4fa9a5f): measured with verify-flows-size-ratchet's nodeSizes over both maintained profiles (byte-identical bcm2712/bcm2709): origin/main 3012 -> HEAD 4963 = +1951. getAuthSecret() previously called osiLib.require('scope') unconditionally, violating the default-off contract (a lost osi-lib helper file would 500 every auth endpoint on a flag-off gateway). Fixed per the history-api-router-fn/zone-env-fn reference pattern: getAuthSecret() now checks a freshly-recomputed OSI_SCOPED_ACCESS flag first (recomputed locally rather than closing over any outer scopedOn, avoiding TDZ risk uniformly across every node regardless of its own structure) and only resolves osiLib inside the flag-on branch; the flag-off branch inlines the exact pre-refactor secret-resolution logic (same file paths, same ENOENT-quiet read/generate/write behavior), byte-equivalent to the pre-port route."
    },
    "get-actuations-auth": {
      "delta": 1957,
      "reason": "Supersedes the previous entry (115). port/wave3-auth-flaggate (fix(auth): flag-gate the shared scope resolver across all auth nodes + hermetic flag-off verifier, follow-up to PR #200 / commit 2c4fa9a5f): measured with verify-flows-size-ratchet's nodeSizes over both maintained profiles (byte-identical bcm2712/bcm2709): origin/main 3784 -> HEAD 5741 = +1957. getAuthSecret() previously called osiLib.require('scope') unconditionally, violating the default-off contract (a lost osi-lib helper file would 500 every auth endpoint on a flag-off gateway). Fixed per the history-api-router-fn/zone-env-fn reference pattern: getAuthSecret() now checks a freshly-recomputed OSI_SCOPED_ACCESS flag first (recomputed locally rather than closing over any outer scopedOn, avoiding TDZ risk uniformly across every node regardless of its own structure) and only resolves osiLib inside the flag-on branch; the flag-off branch inlines the exact pre-refactor secret-resolution logic (same file paths, same ENOENT-quiet read/generate/write behavior), byte-equivalent to the pre-port route."
    },
    "sys-reboot-fn": {
      "delta": 1945,
      "reason": "port/wave3-auth-flaggate (fix(auth): flag-gate the shared scope resolver across all auth nodes + hermetic flag-off verifier, follow-up to PR #200 / commit 2c4fa9a5f): measured with verify-flows-size-ratchet's nodeSizes over both maintained profiles (byte-identical bcm2712/bcm2709): origin/main 2645 -> HEAD 4590 = +1945. getAuthSecret() previously called osiLib.require('scope') unconditionally, violating the default-off contract (a lost osi-lib helper file would 500 every auth endpoint on a flag-off gateway). Fixed per the history-api-router-fn/zone-env-fn reference pattern: getAuthSecret() now checks a freshly-recomputed OSI_SCOPED_ACCESS flag first (recomputed locally rather than closing over any outer scopedOn, avoiding TDZ risk uniformly across every node regardless of its own structure) and only resolves osiLib inside the flag-on branch; the flag-off branch inlines the exact pre-refactor secret-resolution logic (same file paths, same ENOENT-quiet read/generate/write behavior), byte-equivalent to the pre-port route."
    },
    "sys-fan-fn": {
      "delta": 1939,
      "reason": "port/wave3-auth-flaggate (fix(auth): flag-gate the shared scope resolver across all auth nodes + hermetic flag-off verifier, follow-up to PR #200 / commit 2c4fa9a5f): measured with verify-flows-size-ratchet's nodeSizes over both maintained profiles (byte-identical bcm2712/bcm2709): origin/main 4653 -> HEAD 6592 = +1939. getAuthSecret() previously called osiLib.require('scope') unconditionally, violating the default-off contract (a lost osi-lib helper file would 500 every auth endpoint on a flag-off gateway). Fixed per the history-api-router-fn/zone-env-fn reference pattern: getAuthSecret() now checks a freshly-recomputed OSI_SCOPED_ACCESS flag first (recomputed locally rather than closing over any outer scopedOn, avoiding TDZ risk uniformly across every node regardless of its own structure) and only resolves osiLib inside the flag-on branch; the flag-off branch inlines the exact pre-refactor secret-resolution logic (same file paths, same ENOENT-quiet read/generate/write behavior), byte-equivalent to the pre-port route."
    },
    "al-link-validate": {
      "delta": 1951,
      "reason": "port/wave3-auth-flaggate (fix(auth): flag-gate the shared scope resolver across all auth nodes + hermetic flag-off verifier, follow-up to PR #200 / commit 2c4fa9a5f): measured with verify-flows-size-ratchet's nodeSizes over both maintained profiles (byte-identical bcm2712/bcm2709): origin/main 7590 -> HEAD 9541 = +1951. getAuthSecret() previously called osiLib.require('scope') unconditionally, violating the default-off contract (a lost osi-lib helper file would 500 every auth endpoint on a flag-off gateway). Fixed per the history-api-router-fn/zone-env-fn reference pattern: getAuthSecret() now checks a freshly-recomputed OSI_SCOPED_ACCESS flag first (recomputed locally rather than closing over any outer scopedOn, avoiding TDZ risk uniformly across every node regardless of its own structure) and only resolves osiLib inside the flag-on branch; the flag-off branch inlines the exact pre-refactor secret-resolution logic (same file paths, same ENOENT-quiet read/generate/write behavior), byte-equivalent to the pre-port route."
    },
    "al-unlink-func": {
      "delta": 1947,
      "reason": "port/wave3-auth-flaggate (fix(auth): flag-gate the shared scope resolver across all auth nodes + hermetic flag-off verifier, follow-up to PR #200 / commit 2c4fa9a5f): measured with verify-flows-size-ratchet's nodeSizes over both maintained profiles (byte-identical bcm2712/bcm2709): origin/main 2491 -> HEAD 4438 = +1947. getAuthSecret() previously called osiLib.require('scope') unconditionally, violating the default-off contract (a lost osi-lib helper file would 500 every auth endpoint on a flag-off gateway). Fixed per the history-api-router-fn/zone-env-fn reference pattern: getAuthSecret() now checks a freshly-recomputed OSI_SCOPED_ACCESS flag first (recomputed locally rather than closing over any outer scopedOn, avoiding TDZ risk uniformly across every node regardless of its own structure) and only resolves osiLib inside the flag-on branch; the flag-off branch inlines the exact pre-refactor secret-resolution logic (same file paths, same ENOENT-quiet read/generate/write behavior), byte-equivalent to the pre-port route."
    },
    "history-rollup-tick-fn": {
      "delta": 1963,
      "reason": "port/wave3-auth-flaggate (fix(auth): flag-gate the shared scope resolver across all auth nodes + hermetic flag-off verifier, follow-up to PR #200 / commit 2c4fa9a5f): measured with verify-flows-size-ratchet's nodeSizes over both maintained profiles (byte-identical bcm2712/bcm2709): origin/main 3190 -> HEAD 5153 = +1963. getAuthSecret() previously called osiLib.require('scope') unconditionally, violating the default-off contract (a lost osi-lib helper file would 500 every auth endpoint on a flag-off gateway). Fixed per the history-api-router-fn/zone-env-fn reference pattern: getAuthSecret() now checks a freshly-recomputed OSI_SCOPED_ACCESS flag first (recomputed locally rather than closing over any outer scopedOn, avoiding TDZ risk uniformly across every node regardless of its own structure) and only resolves osiLib inside the flag-on branch; the flag-off branch inlines the exact pre-refactor secret-resolution logic (same file paths, same ENOENT-quiet read/generate/write behavior), byte-equivalent to the pre-port route."
    },
    "api-me-auth": {
      "delta": 1941,
      "reason": "port/wave3-auth-flaggate (fix(auth): flag-gate the shared scope resolver across all auth nodes + hermetic flag-off verifier, follow-up to PR #200 / commit 2c4fa9a5f): measured with verify-flows-size-ratchet's nodeSizes over both maintained profiles (byte-identical bcm2712/bcm2709): origin/main 2924 -> HEAD 4865 = +1941. getAuthSecret() previously called osiLib.require('scope') unconditionally, violating the default-off contract (a lost osi-lib helper file would 500 every auth endpoint on a flag-off gateway). Fixed per the history-api-router-fn/zone-env-fn reference pattern: getAuthSecret() now checks a freshly-recomputed OSI_SCOPED_ACCESS flag first (recomputed locally rather than closing over any outer scopedOn, avoiding TDZ risk uniformly across every node regardless of its own structure) and only resolves osiLib inside the flag-on branch; the flag-off branch inlines the exact pre-refactor secret-resolution logic (same file paths, same ENOENT-quiet read/generate/write behavior), byte-equivalent to the pre-port route."
    },
    "api-me-fn": {
      "delta": 334,
      "reason": "port/wave3-auth-flaggate (fix(auth): flag-gate the shared scope resolver across all auth nodes + hermetic flag-off verifier, follow-up to PR #200 / commit 2c4fa9a5f): measured with verify-flows-size-ratchet's nodeSizes over both maintained profiles (byte-identical bcm2712/bcm2709): origin/main 2029 -> HEAD 2363 = +334. This node has no getAuthSecret() helper -- it computed scopedOn then called osiLib.require('scope') unconditionally just to obtain the scope module handle S, even though S is only read inside the pre-existing 'if (scopedOn)' branch further down. Fixed by wrapping the osiLib.require('scope') call itself in 'if (scopedOn)' (scopedOn is already declared above this point, so no TDZ risk) and defaulting S to null; matches the get-devices-query wrap-guard precedent."
    },
    "sdi12-profiles-scope-fn": {
      "delta": 544,
      "reason": "port/wave3-auth-flaggate (fix(auth): flag-gate the shared scope resolver across all auth nodes + hermetic flag-off verifier, follow-up to PR #200 / commit 2c4fa9a5f): measured with verify-flows-size-ratchet's nodeSizes over both maintained profiles (byte-identical bcm2712/bcm2709): origin/main 5039 -> HEAD 5583 = +544. This node's own getAuthSecret() was already the clean pre-refactor inline version (never touched by the shared-resolver refactor) -- the violation was a separate, unconditional osiLib.require('scope') call made AFTER verifyBearer() succeeded, solely to reach scope.assertEnabledAccount(), which ran on every authenticated request regardless of OSI_SCOPED_ACCESS. Fixed by wrapping the scope-load + db-open + assertEnabledAccount block in a freshly-computed 'if (scopedOn)', matching the get-devices-query / catalog-authenticated-read-guard write-only-scoping precedent (the enabled-account gate is a scoped-mode-only feature); flag-off is now a pure bearer-auth passthrough with no DB access and no osiLib, byte-equivalent to the pre-scoped-access route."
    },
    "auth-db-query": {
      "delta": 209,
      "reason": "Wave 3 durable-history-batch + installation-identity port (AgroLink 77d3c52a, renumbered migrations 0052-0053, port/wave3-edge-durable): measured with verify-flows-size-ratchet's nodeSizes over both maintained profiles (byte-identical bcm2712/bcm2709): origin/main 1332 -> HEAD 1541 = +209. The login SELECT now joins a subselect for installation_identity.installation_uuid (singleton_id=1) so auth-process-result can key its offline verifier subject on the installation UUID instead of the gateway EUI when the linked account was created under verifier v2; the pre-existing DB-close catch(_){} was also converted to a visible node.warn (touched-node silent-catch ratchet)."
    },
    "al-link-handle-auth": {
      "delta": 795,
      "reason": "Wave 3 durable-history-batch + installation-identity port (AgroLink 77d3c52a, renumbered migrations 0052-0053, port/wave3-edge-durable): measured with verify-flows-size-ratchet's nodeSizes over both maintained profiles (byte-identical bcm2712/bcm2709): origin/main 4513 -> HEAD 5308 = +795. Persists the installation_uuid the cloud echoed back in the /auth/local-sync response alongside the existing linked-account fields."
    },
    "al-link-finalize": {
      "delta": 637,
      "reason": "Wave 3 durable-history-batch + installation-identity port (AgroLink 77d3c52a, renumbered migrations 0052-0053, port/wave3-edge-durable): measured with verify-flows-size-ratchet's nodeSizes over both maintained profiles (byte-identical bcm2712/bcm2709): origin/main 3076 -> HEAD 3713 = +637. Records the installation_uuid into the persisted linked-account row; the pre-existing DB-close catch(_){} was also converted to a visible node.warn (touched-node silent-catch ratchet)."
    },
    "sync-history-build": {
      "delta": 4087,
      "reason": "Wave 3 durable-history-batch promotion (AgroLink 7e30e6a4, renumbered migration 0051, port/wave3-edge-durable): measured with verify-flows-size-ratchet's nodeSizes over both maintained profiles (byte-identical bcm2712/bcm2709): origin/main 5913 -> HEAD 10000 = +4087. Promotes durable history batches: snapshot-high-key cursor tracking, manifest tombstone accounting, and dirty-key-driven correction coverage for irrigation_events/valve_actuation_expectations, sourced from osi-history-sync-helper's expanded batch-build logic."
    },
    "sync-history-mark": {
      "delta": 3949,
      "reason": "Wave 3 durable-history-batch promotion (AgroLink 7e30e6a4, renumbered migration 0051, port/wave3-edge-durable): measured with verify-flows-size-ratchet's nodeSizes over both maintained profiles (byte-identical bcm2712/bcm2709): origin/main 5387 -> HEAD 9336 = +3949. ACK handling for durable batches: clears acknowledged sync_history_dirty_keys rows and advances the promoted snapshot-high-key cursor."
    },
    "sync-history-manifest-build": {
      "delta": 73,
      "reason": "Wave 3 durable-history-batch promotion (AgroLink 7e30e6a4, renumbered migration 0051, port/wave3-edge-durable): measured with verify-flows-size-ratchet's nodeSizes over both maintained profiles (byte-identical bcm2712/bcm2709): origin/main 3431 -> HEAD 3504 = +73. Manifest segments now report tombstone_count."
    },
    "sync-history-manifest-mark": {
      "delta": 2351,
      "reason": "Wave 3 durable-history-batch promotion (AgroLink 7e30e6a4, renumbered migration 0051, port/wave3-edge-durable): measured with verify-flows-size-ratchet's nodeSizes over both maintained profiles (byte-identical bcm2712/bcm2709): origin/main 685 -> HEAD 3036 = +2351. Manifest ACK handling gains tombstone-aware quarantine bookkeeping to match the promoted durable-batch cursor state."
    },
    "4f4a765f36cee6f3": {
      "delta": 284,
      "reason": "SDI-12 identify command dispatch (wave 3, port/wave3-edge-durable, no AgroLink source arc -- osi-server main issues POST /devices/{eui}/sdi12/identify as cloud command SET_SDI12_IDENTIFY, which cmd-type-registry/reject-indefinite-open/write-strega-expectation's Command Type Registry duplicate already listed with dispatch:'sdi12_identify' but Route Command had no case for, so the command queued and aged out). Build UPDATE SQL gets a SET_SDI12_IDENTIFY branch that mirrors the SET_STREGA_MAGNET_MODE / SET_STREGA_PARTIAL_OPENING pattern: validate the device EUI shape and ack, since the real device_row bookkeeping (sdi12_probe_status, sdi12_identify_attempts) is already owned entirely by sdi12-identify-trigger-fn on the sibling dispatch path Route Command's new 7th output feeds. Measured with verify-flows-size-ratchet's nodeSizes over both maintained profiles (byte-identical bcm2712/bcm2709): origin/main 17943 -> HEAD 18227 = +284."
    }
  },
  "total_allowance": {
    "delta": 21893,
    "reason": "Wave 3 durable-history-batch + installation-identity + SDI-12 identify dispatch port onto origin/main d8a6d9cc (branch port/wave3-edge-durable). This branch is a clean slice off merged origin/main (which already includes the full wave3-edge-zonesync program, superseding the prior 8428 figure measured against an earlier, smaller origin/main from that now-merged port branch) -- there is no separate inherited half here. Re-measured directly in this port worktree with verify-flows-size-ratchet totalChars over both maintained flows.json profiles, which are byte-identical: origin/main 1430228 chars -> HEAD 1452121 chars = 21893. Covers: durable history batch promotion (sync-history-build +4087, sync-history-mark +3949, sync-history-manifest-build +73, sync-history-manifest-mark +2351, itemized above, AgroLink 7e30e6a4 renumbered migration 0051); installation identity (auth-db-query +209, al-link-handle-auth +795, al-link-finalize +637, sync-bootstrap-build/sync-state-build/al-link-build-req re-pinned to fresh exact deltas 2483/1089/2511 superseding their stale pre-merge pins, itemized above, AgroLink 77d3c52a renumbered migrations 0052-0053); auth-process-result's growth fit within its existing pre-dated allowance (no repin needed). sync-force-build (+2493, measured origin/main 60196 -> HEAD 62689, comfortably inside its existing 5786 node_allowances ceiling so that per-node entry is left unchanged) is a deliberate expansion beyond AgroLink 77d3c52a's literal diff: it duplicates sync-bootstrap-build's whole gatewayIdentity-building block for the force-sync trigger and the two nodes have always been kept in lockstep by every prior feature (see the sync-bootstrap-build/sync-force-build size-allowance history above); test-journal-bootstrap.js's assertReadyAdvertisement enforces this symmetry for both bootstrap paths, so leaving sync-force-build behind would have been a real behavioral inconsistency, not a faithful narrower port. SDI-12 identify dispatch (+776 of the total: Build UPDATE SQL +284 itemized above; Route Command +492, comfortably inside its existing 706 node_allowances ceiling so that per-node entry is left unchanged) is not an AgroLink source-arc port -- osi-server main independently started issuing SET_SDI12_IDENTIFY as a cloud command with no corresponding Route Command case, so commands queued and aged out; wires it into the pre-existing sdi12-identify-trigger-fn machinery via a new link-out node (not measured by this ratchet, which only sizes function-node func text)."
  },
  "new_node_ceilings": {
    "sdi12-identify-action-fn": {
      "max_chars": 7126,
      "reason": "SDI-12 port: new function node measured at exactly 7126 chars on both maintained profiles, over this ratchet's 4096-char new-node ceiling. Ported as-is from the source SDI-12 arc rather than re-architected mid-port; extracting these bodies into osi-lib seam modules is the standing follow-up. The upstream ratchet (refactor-program A0) replaced the ceiling with exactly this shape -- a committed, reviewed absolute max_chars per node."
    },
    "sdi12-config-auth-fn": {
      "max_chars": 7114,
      "reason": "SDI-12 port: new function node measured at exactly 7114 chars on both maintained profiles, over this ratchet's 4096-char new-node ceiling. Ported as-is from the source SDI-12 arc rather than re-architected mid-port; extracting these bodies into osi-lib seam modules is the standing follow-up. The upstream ratchet (refactor-program A0) replaced the ceiling with exactly this shape -- a committed, reviewed absolute max_chars per node."
    },
    "sdi12-config-action-fn": {
      "max_chars": 6836,
      "reason": "SDI-12 port: new function node measured at exactly 6836 chars on both maintained profiles, over this ratchet's 4096-char new-node ceiling. Ported as-is from the source SDI-12 arc rather than re-architected mid-port; extracting these bodies into osi-lib seam modules is the standing follow-up. The upstream ratchet (refactor-program A0) replaced the ceiling with exactly this shape -- a committed, reviewed absolute max_chars per node."
    },
    "sdi12-write-fn": {
      "max_chars": 6296,
      "reason": "SDI-12 port: new function node measured at exactly 6296 chars on both maintained profiles, over this ratchet's 4096-char new-node ceiling. Ported as-is from the source SDI-12 arc rather than re-architected mid-port; extracting these bodies into osi-lib seam modules is the standing follow-up. The upstream ratchet (refactor-program A0) replaced the ceiling with exactly this shape -- a committed, reviewed absolute max_chars per node."
    },
    "sdi12-identify-trigger-fn": {
      "max_chars": 5218,
      "reason": "SDI-12 port: new function node measured at exactly 5218 chars on both maintained profiles, over this ratchet's 4096-char new-node ceiling. Ported as-is from the source SDI-12 arc rather than re-architected mid-port; extracting these bodies into osi-lib seam modules is the standing follow-up. The upstream ratchet (refactor-program A0) replaced the ceiling with exactly this shape -- a committed, reviewed absolute max_chars per node."
    },
    "sdi12-identify-fn": {
      "max_chars": 4101,
      "reason": "SDI-12 port: new function node measured at exactly 4101 chars on both maintained profiles, over this ratchet's 4096-char new-node ceiling. Ported as-is from the source SDI-12 arc rather than re-architected mid-port; extracting these bodies into osi-lib seam modules is the standing follow-up. The upstream ratchet (refactor-program A0) replaced the ceiling with exactly this shape -- a committed, reviewed absolute max_chars per node."
    },
    "scoped-zone-delete-router": {
      "max_chars": 5023,
      "reason": "Exact measured size after commit E6 (adapted from AgroLink 17462573a): Wave 3 scoped-access port, commit E6 (adapted from AgroLink 17462573a, \"fix(scope): replace viewer-denylist with a canMutate() allowlist on every mutation gate\"): role === 'viewer' denylist checks replaced with !scope.canMutate(role) allowlist checks. Re-measured fresh with verify-flows-size-ratchet nodeSizes (func.length) over both maintained profiles. Supersedes prior entry: Wave 3 scoped-access port (AgroLink bc156fb2, feat(api): enforce scoped zone lifecycle): new function node measured at exactly 5018 chars on both maintained profiles, over this ratchet's 4096-char new-node ceiling. Zone delete under OSI_SCOPED_ACCESS=1 is inherently larger than a typical scoped-gate wrapper: it resolves the target zone and its grantees, requires either sole ownership/grant or an admin role for non-owner deletion, and runs a 5-statement transaction (devices, journal_plots, user_zone_assignments, irrigation_schedules, irrigation_zones) with per-grantee scope-cache invalidation. Ported as-is rather than re-architected mid-port."
    },
    "scoped-device-config-guard": {
      "max_chars": 4358,
      "reason": "Supersedes the prior entry (4249). Wave 3 scoped-access port, guard-extension work (binding note 4, pick-list commit 73 / AgroLink 853c1b3584, \"fix(sdi12): realign config scope wires and cover denial\"): scoped-device-config-guard's routeTable extended from 23 to 25 entries with POST /sdi12/identify -> sdi12-identify-action-fn (index 23) and PUT /sdi12/config -> sdi12-config-auth-fn (index 24), matching AgroLink's own post-commit-73 guard structure exactly (verified byte-for-byte against AgroLink's ground truth via git show). sdi12-recipe-apply-http/sdi12-recipe-rollback-http remain unguarded, matching AgroLink's own scope -- upstream never extended the routeTable for those two routes either. Re-measured fresh with verify-flows-size-ratchet nodeSizes (func.length) over both maintained profiles."
    },
    "scoped-admin-account-router": {
      "max_chars": 10508,
      "reason": "Supersedes the prior ceiling (10117). Wave 3 scoped-access port, pick-list commit 65 (AgroLink d2e81bd6a, \"fix(edge): close auth and scoped-access review gaps\"): getAuthSecret() now delegates to osi-scope-helper's shared resolveAuthSecret via osiLib.require('scope') instead of duplicating the persisted-secret file-read/generate/write logic inline, and verifyBearer tags each failure with a stable _osiAuthFailure {format,code,sourceId} for the shared auth-catch-http500 response node to surface as a real HTTP error instead of an unhandled-exception 500. Re-measured fresh with verify-flows-size-ratchet nodeSizes (func.length) over both maintained profiles, exact delta vs origin/main. Also hardens the guarded derole/disable SQL anchor comparisons (buildDeroleUserGuardedSql/buildDisableUserGuardedSql) per X12."
    },
    "scoped-zone-create-router": {
      "max_chars": 3962,
      "reason": "Exact measured size after commit E6 (adapted from AgroLink 17462573a): Wave 3 scoped-access port, commit E6 (adapted from AgroLink 17462573a, \"fix(scope): replace viewer-denylist with a canMutate() allowlist on every mutation gate\"): role === 'viewer' denylist checks replaced with !scope.canMutate(role) allowlist checks. Re-measured fresh with verify-flows-size-ratchet nodeSizes (func.length) over both maintained profiles."
    },
    "scoped-device-claim-router": {
      "max_chars": 4354,
      "reason": "Supersedes the prior entry (3704). Wave 3 scoped-access port, pick-list commit 63 (AgroLink 7ddbe5dd4, \"fix(zones): preserve device assignments during scoped registration\"): a re-post naming a DIFFERENT zone than the device's current one now returns 409 with current_zone_id/current_zone_name instead of silently moving it, matching the assign-router's own 409-on-conflict contract, so a re-post can only ever preserve or leave-unassigned an existing zone, never reassign it implicitly (+650, exact vs the prior entry). Re-measured fresh with verify-flows-size-ratchet nodeSizes (func.length). Prior provenance retained: commit W5/W3 (adapted from AgroLink 9a3094a63) made zone_id an explicit optional integer id; commit E6 (adapted from AgroLink 17462573a) replaced the viewer-denylist with a canMutate() allowlist."
    },
    "scoped-device-assign-router": {
      "max_chars": 5410,
      "reason": "Supersedes the prior ceiling (4804). Wave 3 scoped-access port, pick-list commit 65 (AgroLink d2e81bd6a, \"fix(edge): close auth and scoped-access review gaps\"): getAuthSecret() now delegates to osi-scope-helper's shared resolveAuthSecret via osiLib.require('scope') instead of duplicating the persisted-secret file-read/generate/write logic inline, and verifyBearer tags each failure with a stable _osiAuthFailure {format,code,sourceId} for the shared auth-catch-http500 response node to surface as a real HTTP error instead of an unhandled-exception 500. Re-measured fresh with verify-flows-size-ratchet nodeSizes (func.length) over both maintained profiles, exact delta vs origin/main. Also includes E11: assigning an already-assigned device to the SAME zone is now accepted (idempotent) instead of hitting the 409 conflict path."
    },
    "scoped-device-unassign-router": {
      "max_chars": 3559,
      "reason": "Exact measured size after commit E6 (adapted from AgroLink 17462573a): Wave 3 scoped-access port, commit E6 (adapted from AgroLink 17462573a, \"fix(scope): replace viewer-denylist with a canMutate() allowlist on every mutation gate\"): role === 'viewer' denylist checks replaced with !scope.canMutate(role) allowlist checks. Re-measured fresh with verify-flows-size-ratchet nodeSizes (func.length) over both maintained profiles."
    },
    "scoped-device-delete-router": {
      "max_chars": 3858,
      "reason": "Supersedes the prior E6 entry (3042). Wave 3 scoped-access port, pick-list commit 56 (AgroLink 09bcf11c0, \"feat(scope): let any write role delete an unassigned device\"): an unassigned device may now be deleted by any write-capable role (not admin-only), while a zone-assigned device still requires zone write access -- 3042 -> 3858 (+816). Re-measured fresh with verify-flows-size-ratchet nodeSizes (func.length) over both maintained profiles."
    },
    "scoped-zone-config-guard": {
      "max_chars": 3724,
      "reason": "Exact measured size after commit E6 (adapted from AgroLink 17462573a): Wave 3 scoped-access port, commit E6 (adapted from AgroLink 17462573a, \"fix(scope): replace viewer-denylist with a canMutate() allowlist on every mutation gate\"): role === 'viewer' denylist checks replaced with !scope.canMutate(role) allowlist checks. Re-measured fresh with verify-flows-size-ratchet nodeSizes (func.length) over both maintained profiles."
    }
  }
}
