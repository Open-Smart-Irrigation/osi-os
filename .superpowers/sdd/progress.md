Task 1: complete (commits a28a6237..f72c6e2b, review clean; Minor for final review: journal_products lacks updated_at — spec-inherited)
HANDOVER 2026-07-12: Tasks 2-14 handed to Codex orchestrator (docs/superpowers/prompts/field-journal-slice1-codex/prompt.md). Task 1 remains complete/reviewed.
Task 2: complete (commits c8e0b95f..eaf63ae7; controller gates green; re-review spec PASS / quality APPROVED; catalog v1 is a data migration; source 128 slots reconcile to 129 repaired runtime slots)
Task 3: complete (commits e3f92ca1..cce8ed94; 39/39 focused tests; helper/profile/deploy/full-sync gates green; final re-review spec PASS / quality APPROVED)
Task 4: complete (commits 355a9d02..d27a605f; 60/60 focused tests on both profiles; helper/profile/deploy/full-sync gates green; final re-review spec PASS / quality APPROVED)
Task 5: complete (commits 0f51c6a1..1c652bde; 75/75 focused tests on both profiles; 122/122 dependency rules audited; helper/profile/deploy/full-sync gates green; final re-review spec PASS / quality APPROVED)
Task 6: complete (commits 914ef911..afb48f92; 97/97 focused tests on both profiles; Java 21/Jackson differential 78,633 cases with 0 mismatches; helper/profile/deploy/full-sync/contract gates green; final re-review spec PASS / quality APPROVED; canonical contract pins UTF-16 key order and recursive string candidates)
Task 7: complete (commits 7d5356e8..a0fb227e; lifecycle 56/56; both profiles 97/97; helper/profile/deploy/full-sync gates green; transaction/outbox/command rollback evidence green; final re-review spec PASS / quality APPROVED)
Task 8: complete (commits 81a48756..6aaae56f; lifecycle 89/89; both profiles 97/97; bounded context reads and true SWT provenance; deploy/helper/profile/full-sync gates green; final re-review spec PASS / quality APPROVED)
Task 9: complete (commits 9e501287..7b2a88ea; contract/parity gates green; final re-review spec PASS / quality APPROVED; whole aggregates, semantic bindings, and direct SQL parity fail closed)
Task 10: complete (commits 7b2a88ea..52b27350; API 22/22, lifecycle 92/92, schema/sync/profile/deploy gates green; final re-review spec PASS / quality APPROVED; same-gateway ownership, reproducible exports, and research-identity exclusion verified; integration note: current origin/main independently claimed migration 0014, so renumbering remains an integration-time requirement under the no-rebase task contract)
Task 11: complete (commits 52b27350..e2782c8f; command 43/43 and controller gates green; final re-review spec PASS / quality APPROVED; replay is owner/gateway-scoped, same-effect apply is serialized, custom dependencies retry structurally, and leased ACK replacement uses a fresh row ID)
Task 12: complete (commits e2782c8f..b7aaffc2; bootstrap 25/25 and controller gates green; final re-review spec PASS / quality APPROVED; capability/catalog advertisement fails closed, the UI flag stays off, and asynchronous SQLite close errors reach contextual warnings)
Task 13: complete (commits b7aaffc2..5e4aca55; 10k entries/150k values, four intended SEARCH plans, list <100 ms, streamed CSV <64 MiB; controller gates green; final re-review spec PASS / quality APPROVED; additive 0017 adds D10 plot-scoped duplicate/sticky indexes and the fixture pins page-wise CSV writes)
Task 14: complete (commits 5e4aca55..043b5bdc; Node 22 field-journal CI workflow and 37-gate local suite green; independent review spec PASS / quality APPROVED; workflow is read-only, credential-free, dependency-free, and runs both maintained module mirrors plus schema/lifecycle/perf/profile gates)
Final-review correction: complete in this commit (IMP-1 through IMP-8 and MIN-2 corrected; journal modules 98/98 per profile, lifecycle 92/92, API 28/28, command 49/49, bootstrap 53/53, read snapshot 4/4, migration engine 60/60, perf and controller gates green; current origin/main migration 0014 collision remains integration-time work by explicit scope)
Final-review follow-up — custom-vocabulary admissibility: complete in this commit (choice-parent contract, closed custom constraint grammar, and reachable canonical-unit paths corrected; journal modules 99/99 per profile, API 32/32, command 51/51, full sync/profile/helper/contract gates green; no migrations, DBs, flows, workflow files, sister repo, or live systems touched; existing migration 0014 collision unchanged)
Fixplan Task 1 (2026-07-14 review fixes): complete (merge commit 7f3d1372; main @0f1361a3 merged with safety tag field-journal-slice1-pre-main-merge; journal migrations renumbered 0014/0015/0016/0017 -> 0018/0019/0020/0021 with 0019 regenerated by generate-journal-catalog.js; CHECKSUMS/seed/7 bundled DBs/both flows.json/size-ratchet baseline+allowances/silent-catch baseline all regenerated; migrate-flows-journal-commands+bootstrap one-shot input hashes re-pinned to main's file with anchors verified; UC512 fallback-registry insertion made presence-aware to avoid duplicating main's row; full 21-gate suite green; report .superpowers/sdd/fixplan-task-1-report.md)
Task 1: complete (commits 15d35d2f..7f3d1372 merge; 22 gates green; reviewed by orchestrator — op-parity resolution + one-shot script re-pins accepted; UC512 presence-guard correct)
Task 2: complete (commits 7f3d1372..50775bc4; api 33/33, module 99/99, parity green; orchestrator-reviewed — error-path fix in scope)
Task 3: complete (commit 1358eaa2; lifecycle 93/93, api 33/33, parity green; resolvePlotContext zone JOIN now matches NULL-gateway-EUI legacy zones, aligned with api.js's ownedZone predicate; only one join of that shape existed in lifecycle.js)
Task 3: complete (commits 50775bc4..1358eaa2; lifecycle 93/93, api 33/33, parity green; orchestrator-reviewed)
MINOR (final-review triage): lifecycle.js resolveDeviceEui zoneMatches (~line 424) has the same NULL-EUI under-matching shape on the device-ownership path — follow-up candidate, out of Task 3 scope.
Task 4: complete (commits 1358eaa2..39cd3077; api 36/36, module 99/99, parity green; orchestrator-reviewed — one-line removal, masking regressions covered)
Task 5: complete (commits 39cd3077..d0e7f3e6; api 37/37, module 99/99, parity green; orchestrator-reviewed — one-line active filter, create fall-through verified)
Task 6: complete (commits d0e7f3e6..82142b07; command 52/52, lifecycle 93/93, api 37/37, parity green; orchestrator-reviewed — one-line precedence flip, fallback preserved)
Task 7: complete (commits 82142b07..5cce17d7; lifecycle 94/94, api 37/37, module 99/99, parity green; orchestrator-reviewed — NUL-byte scare verified clean in committed file)
MINOR (final-review triage): draft duplicate_value error.details uses {field} object shape vs final path's array shape — matches file's 409 precedent, unify later if desired.
Task 8: complete (commits 5cce17d7..3655e9fd; 6 gates green incl. plan pin; orchestrator-reviewed — migration+seed+7 DBs+CHECKSUMS+consistency contract all consistent)
Task 9: complete (commits 3655e9fd..b1236e0a; wiring test 7/7, 12/12 files fenced + negative self-test; orchestrator-reviewed)
Task 10: complete (commit b1236e0a..02ec049b; new osi-command-ledger module smoke test 10/10, journal command-path 52/52, journal module 99/99, flows wiring/sync-flow/helper-registration/deploy-fence/profile-parity/no-stray-ddl all green; extraction resolved one hidden coupling beyond the plan's single validator hook — deduplicatePendingCommand's journal-type duplicate lookup also needed the intent-hash builder, so a second injected hook (opts.extraSubmittedIntentHash) was added alongside opts.extraEffectBindingValidator; journal-specific logic stays in osi-journal either way)
Task 10: complete (commits b1236e0a..02ec049b; 9 gates green incl. new ledger smoke 10/10; orchestrator-reviewed — second injected hook (extraSubmittedIntentHash) and fuller signatures approved as within plan intent; ACK node SPOF resolved, dedupe node retains osiJournal dep for hooks by design)
Final review (orchestrator, Fable): complete — cumulative diff 7f3d1372..c4a4f3a5 verified as exact union of approved task diffs; Minor #1 (resolveDeviceEui NULL-EUI) fixed as c4a4f3a5; Minor #2 (draft error.details shape) documented follow-up. Full 23-gate suite green post-fix (21 migrations, seed replay, 7-DB consistency, profile parity, sync flow/contract/op-parity 35/35, helper registration, contract schemas, flows wiring, deploy fence, journal schema/catalog/lifecycle 95/95/api 37/37/command 52/52/bootstrap/perf, module 99/99, ledger 10/10). Branch ready for push/PR.
PR141 hardening Task 1: complete (commit c8d8ec53; lifecycle 108/108, command 53/53, journal module 99/99, profile parity green; independent Luna review approved with mutation-sensitive reference and timezone regressions)
PR141 hardening Task 2: complete (commit 201247af; ledger 13/13, snapshot 10/10, command 53/53, profile parity green; independent Luna re-review approved after exact sync-version normalization and rollback-observation corrections)
PR141 hardening Task 3: complete (commit 81b1c161; API 38/38, 10k/150k perf fixture and profile parity green; independent Luna re-review approved after byte-bounded CSV/NDJSON writes and exact multibyte reassembly coverage)
PR141 hardening Task 4: complete (commit 82aad58f; verifier 44/44, live edge/server operation parity, sync-flow, and profile parity green; independent Luna re-review approved after recursive template-interpolation scanning closed the nested-call bypass)
PR141 hardening Task 5: complete (commit b26bfe04; contract, sync-contract, communication-contract, ledger 13/13, workflow syntax green; independent Luna re-review approved after Boolean-schema, JSON numeric equality, required-array, and JSON Pointer traversal corrections)
PR141 hardening Task 6: complete (commit e7570efa; catalog generator, generated-artifact check, migration manifest and diff gates green; independent Luna review approved for pre-write immutable checksum recovery)
PR141 hardening Task 7: complete (commit 399059be; all seven DBs preserve 0->0 settings rows, integrity/FK checks and nine schema gates green; independent Luna review approved migration, seed, checksum, bundled DBs, and frozen-boot boundary)
PR141 hardening Task 8: complete (commit 7df1d329; strict bootstrap/event/force outbox parsing, STREGA fallback, transformer replay and full flow gates green; independent Luna review approved exact hashes/allowances and silent-catch baseline 228->225)
PR141 hardening Task 9: complete (commit 0e796397; osi-lib 6/6, shared exact-source audit plus generator collision suite 44/44 and full flow gates green; independent Luna third-pass review approved four-node SHA policy and genuine current/legacy replay shape coverage)
PR141 hardening Task 10: complete in this commit (authoritative docs aligned to migrations 0018-0021, 13 tables, plot-scoped settings/indexes, formula-neutralized CSV and lossless records.ndjson; historical prompt/consolidation/task reports annotated without rewriting their snapshots; open+closed issue audit found no equivalents and created bootstrap-helper #142, command-registry #143, dynamic-SQL verifier #144, and canonical timestamp v2 #145; anti-slop, consistency, and diff checks green)
Slice 2 Task 26: complete (Sol spec/quality approved; focused 260/260, TSX 94/94, Vitest 1197/1197, typecheck and build green)
Slice 2 Task 27: BLOCKED (verified) — verify-migrations enforces contiguous numbering (0022 reserved blocks 0023), and generate-journal-catalog.js has no incremental/versioned publish (pins entire catalog into immutable 0019). parseTemplate carry_forward guard can't ship without v2 (would reject live farmer_quick@1). Awaiting product-owner decision on catalog-publishing approach. Proceeding with Tasks 28-33 (GUI, unblocked).
Slice 2 Task 28: complete in this commit (desktop shell + scope rail; ScopeRail 13/13 incl. 72-plot single-row regression, JournalWorkspace 6/6, JournalPage 36/36 — 2 new desktop-branch tests plus all 34 pre-existing mobile tests unchanged; journal locale parity 38/38 across all 7 locales with new workspace/filters keys genuinely translated; full test:unit 1293/1293 (94 node:test + 1199 vitest), typecheck and slop-check clean; mobile capture/timeline route preserved byte-identical except the new isDesktopBrowser() branch point).
Slice 2 Task 28: complete (commits c9eb4e3a..7aa013c5; ScopeRail 13/13, JournalWorkspace 6/6, JournalPage 36/36, locale 38/38, full test:unit 1293/1293, tsc, slop; Opus review Spec OK + Quality Approved; 4 MINOR deferred to final-review triage: (1) filters fieldset legend labeled 'Activity' not 'Filters'; (2) nav+section both aria-label 'Journal' — nav should be 'Scope'; (3) label+aria-label duplication; (4) reachedTimelineBranch parallel guard in JournalPage).
Slice 2 Task 27 (re-scoped 2026-07-19): complete — product owner released the 0022 reservation and asked for the versioned delta-publishing capability itself, not just the P4 fix. generate-journal-catalog.js gained per-row `since` + a CATALOG_MIGRATIONS registry so a catalog version compiles to a pure-delta migration (0019 stays frozen/byte-identical, proven by test); farmer_quick@2 (0022, data-risk) surfaces attr.operator/equipment/method as a visible section so the new parseTemplate carry_forward-visibility guard (catalogModel.ts) accepts it — @1 stays unchanged for historical resolution. All 9 named schema gates green (7-DB consistency, seed replay, runtime parity, no-stray-ddl, profile parity, migrations, --check); broader field-journal suite green after fixing 3 pre-existing hardcoded catalog_version==1 assertions it exposed; focused GUI vitest 204/204 + JournalPage 36/36 (fixed a version-1 pin) + full test:unit 1297/1297, tsc, build all green. Report: .superpowers/sdd/task-27-report.md.
Slice 2 Task 27: complete (commits 7aa013c5..022f8048; generator versioned-delta capability + farmer_quick@2 (P4) + parseTemplate guard; 0019 frozen byte-identical, 0022 delta idempotent (Opus executed it), 22 contiguous migrations, 7 DBs at v2, both profiles, all schema-change-control gates, full test:unit 1297/1297, tsc, build; Opus review Spec OK + Quality Approved, verified all 4 risk areas + every test change legit/strengthened; 2 MINOR deferred: (1) generator rowSince non-template delta path untested (forward path, no consumer yet); (2) cosmetic 0022 header delta-hash vs cumulative-hash could confuse a reader).
Slice 2 Task 29: complete (base 022f8048; keyset EntryTable + filter-scoped exports — entryTablePagination 8/8, EntryTable 21/21, journalApi export methods 19/19, JournalWorkspace wiring 10/10, journal locale parity 38/38 across all 7 locales; full test:unit 1303/1303 (94 node:test + 1209 vitest), typecheck and slop-check clean; TDD caught a real stability bug in the default sort — sorting ascending then reversing the array flipped tied-timestamp rows, fixed by negating the comparator; known documented gap: station/group scope does not narrow the entry list by plot, since the shipped API only accepts one plot_uuid/zone_uuid). Report: .superpowers/sdd/task-29-report.md.
Slice 2 Task 29 REVIEW (Opus): Spec OK / Quality Approved; no Critical. Fix in flight (Sonnet, shared tree): Important-1 (station/group scope silently lists+exports ALL plots' entries with no cue → add honest non-blocking disclosure affordance; real multi-plot filter needs a new endpoint = out of slice scope, documented for MERGE-READINESS) + Minor-2 (EntryTable STATUS_CLASS duplicates JournalEntryRow mapping → extract shared helper) + Minor-4 (stale export-error banner not cleared on filters change). DEFERRED to final-review triage: Minor-1 page-local column sort has no visual cue (only reorders loaded keyset page); Minor-3 voided badge uses hardcoded bg-red-100/text-red-800 non-theme token (pre-existing, shared with JournalEntryRow, not dark-mode aware); Minor-5 possible two simultaneous role=alert regions (table-body error + export error).
Slice 2 Task 31: complete (commit dd829fc8 on sdd/task-31, base 022f8048; NOT yet merged — held for last in merge order because it edits JournalWorkspace.tsx). Edge draft-discard contract over existing PUT transport (flows.json untouched, no DDL): 281 node:test green (106 index + 118 lifecycle + 57 api), both NO-DDL + profile-parity gates green, all 3 changed edge files byte-identical across bcm2712/bcm2709. GUI drafts queue (server status=draft truth only, no localStorage): 1225/1231 vitest (the 6 red = English-only drafts.* keys, the designed Task 34 handoff), tsc + build clean. Opus review: Spec OK / Quality Approved, no Critical; verified atomicity (fault-injection rollback), owner-scope (server-side assertOwnedEntry + assertNoRequestIdentity), version-drift guard, no-outbox (full-ledger deepEqual snapshots), idempotent-success — tests are mutation-sensitive not tautological. IMPORTANT (coordination, not code): must land AFTER Task 34 closes the drafts.* locale gap or CI is red. Resume exposes onResume(entryUuid,focusFieldCode) seam (finalize is Task 30 territory). DEFERRED Minors to final triage: (1) when draft photo attachments land, discardDraft must also delete on-disk blobs (CASCADE removes row not file); (2) DraftResumePanel no-onResume fallback silently drops edits on Close — controller MUST wire onResume; (3) useDraftsQueue(false) sticks in loading state (latent, no caller); (4) focus-field computation duplicated between focusCodeFor and DraftResumePanel.
Slice 2 Task 29: COMPLETE (commits 022f8048..02cb8e49 impl, 448d93f6 review-fix, a4431f2f gate-fix on design-sync/agrolink). Review-fix landed Important-1 (honest station/group scope-limitation notice) + Minor-2 (shared status-badge class helper) + Minor-4 (stale export-error cleared on filters change); covering 82/82. GATE-COVERAGE FIX (a4431f2f, foundational): test:unit:vitest used a hand-maintained dir list that OMITTED src/components/journal/desktop/__tests__ (and would omit markers/) — the entire desktop workspace suite (28/29/30 + 31 DraftsQueue) was silently excluded from the gate; every prior "test:unit NNNN green" under-counted. Fixed by scoping vitest discovery to src/**/*.{test,spec}.{ts,tsx} in vitest.config.ts (tests/ stays with tsx --test). Shared tree now 138 files / 1266 vitest green (was 133/1209), parity still green (31/32/33 English-only keys not yet merged). NOTE for all merges + Task 35: after merging any isolated branch, the true green must be measured with this fixed gate.
Slice 2 Task 32: complete pending review (commit b71a62bb on sdd/task-32, base 022f8048; NOT merged). Layout-transition review sheet: 115/115 focused (8 pure-diff + 9 sheet + 98 JournalCaptureFlow), tsc clean, full suite 1218/1224 (6 red = English-only transition keys, Task 34 handoff). Fixed a real bug in-flight (diff must use pre-reset activity leaf or option_dependencies never fire). Concern to verify in review: plot->plot direct switch passes a momentary empty selection that sanitizeValues(undefined layout) wipes to [] BEFORE the gate sees values — implementer calls it pre-existing/out-of-scope and tests the farm->plot atomic flow; must confirm no silent value-loss path bypasses the sheet. Scope boundaries: "kept" values bypass choice-membership validation by design; "kept-due-to-hidden-field" values don't cascade-rediff on later transitions.
Slice 2 Task 32 REVIEW (Opus): Spec FAIL / Quality Changes-needed — 1 CRITICAL confirmed. applyLayoutTransitionGate (JournalCaptureFlow.tsx ~986-1025) has an unhandled undefined-target-layout branch: on [plotA greenhouse] -> [] (deselect, forced because PlotPicker rejects co-selecting different-layout plots) -> [plotB open_field], the empty step calls sanitizeValues(undefined)->[] and computeLayoutTransitionDiff's !newLayout guard returns [], so entered values are SILENTLY DROPPED with no sheet — the drop runs through the NEW gate's own code = in scope. Test suite only exercised the atomic farm->plot path, hiding it. Fix in flight (Sonnet, sdd/task-32): surface entered values as field_hidden pending items (or carry last-real-layout) when target layout undefined; RED [plotA]->[]->[plotB] integration test mandatory; avoid the trap that after empty step layout='' so picking B diffs vs oldLayout=undefined and !oldLayout re-suppresses. Reviewer CLEARED concern#4 (kept bypasses choice-membership = intended keep semantics, not corruption) + cascade-rediff (kept values ride payload, no silent loss) + confirmed oldLeafForDiff pre-reset-leaf fix genuine & tested.
Slice 2 Task 32: COMPLETE pending merge (commits b71a62bb impl + c80cb016 Critical-fix on sdd/task-32, base 022f8048; NOT merged — merge order 33->32->31, only en/journal.json + JournalCaptureFlow.tsx footprint). Critical FIX: applyLayoutTransitionGate now branches nextLayout ? computeLayoutTransitionDiff : fieldHiddenForEmptyTarget(candidateValues) so an undefined target layout surfaces every entered value as field_hidden (preserved in values, sheet opens, next/finalize/finalizeBatch gated) instead of sanitizeValues(undefined)->[]; lastRealDiffContextRef carries {greenhouse,irrigation} across the empty step so picking plot B diffs vs the real old layout (defeats the !oldLayout re-suppression trap) and re-surfaces method.b as choice_invalid. Opus re-review (same reviewer, full context): Critical RESOLVED, Spec OK / Quality Approved; verified trap defeated, terminal "deselect and stop" gated, new [plotA]->[]->[plotB] test non-vacuous (fails if carry removed), no regressions to defined->defined/keep/oldLeafForDiff. Focused 614/620 (6 expected-red capture.transition.* locale parity -> Task 34), tsc clean. DEFERRED Minor (final triage): field_hidden reason copy "...no longer shows this field" reads oddly for the no-layout/No-plot case (cosmetic).
Slice 2 Task 30: done, OPUS REVIEW running (commit fcfb2324 on design-sync/agrolink, parent a4431f2f). Desktop DetailPanel read-back + parsed context snapshot + final-entry void-with-reason + full-record correction via aggregate->update adapter (entryCorrection). Kept shared-tree parity GREEN (keys added to all 7 locales). entryCorrection 10/10, DetailPanel 18/18, JournalWorkspace 14/14, JournalPage 36/36, parity 38/38; full test:unit 94 tsx + 1297 vitest (gate now discovers desktop dir), tsc clean. Implementer concerns to verify in review: (a) correction stays on the entry's ORIGINAL template because osi-journal edge hard-rejects a template/layout change on correction — "full-record" = full field set of that template, not rebind to full_record; (b) latent adapter gap if conditional_fields were gated on another attribute's live value that flips mid-session (no shipped layout does this); (c) blocks correction for draft/voided, preservation round-trip (one field changed keeps all identity/context/occurrence/grouped/batch_uuid).
Slice 2 Task 30 REVIEW (Opus): Spec FAIL / Quality Changes-needed — 1 CRITICAL + 1 Important. C1 (CRITICAL silent full-record wipe): DetailPanel correction form payload state starts [] and EntryForm never emits onChange on mount, so clicking "Save correction" with NO edit calls buildCorrectionPayload(...,[])->values:[] and the edge replaceExistingWithFinal (UPDATE + DELETE journal_entry_values + re-INSERT) ERASES every value row — validates+saves for farmer_quick / any activity w/o activity_requirements (layout minimum_fields NOT server-enforced), bumps sync_version, emits JOURNAL_ENTRY_UPSERTED -> wipe propagates to cloud. Suite missed it (all preservation tests edit a field first). I2 (Important, 1-line): formOwnedAttributeCodes owns ALL attribute fieldStates but form emits only VISIBLE -> invisible-but-owned value dropped; latent (no shipped visible_if) but live silent-loss once one ships. Fix in flight (Sonnet, shared tree): C1 seed payload/valid from validateEntryForm/buildEntryValues over aggregate.values so a no-op save re-emits the full original set (RED test: unedited save calls updateEntry with full values not []); I2 filter ownership on state.visible. Reviewer CLEARED (attacked, held): adapter field reconstruction + multi-field grouped/batch round-trip test genuine; void uses shipped POST /void (reason required client+server), blocked for draft/voided at UI+server; batch_uuid immutable; template/layout-immutable "full-record" reading correct; read-back/focus/locales safe. DEFERRED Minors (accepted limitations): M3 DST-spanning occurrence single-offset -> invalid_utc_offset (schema has one offset col); M4 localTimeFromInstant .slice(0,16) seconds truncation (only bites sub-minute cloud-origin entries).
Slice 2 Task 30: COMPLETE (commits fcfb2324 impl + 2afcbac2 C1/I2 fix on design-sync/agrolink). C1 FIXED: initialCorrectionSeed(model,template,layout,aggregate,products,t) runs validateEntryForm over aggregate.values and lazily seeds payload/valid useState (was []/true), so a no-op "Save correction" re-emits the FULL original value set (verified: RED test asserted values.length===0 pre-fix, full set post-fix). I2 FIXED: formOwnedAttributeCodes now filters on state.visible && attribute-kind, matching EntryForm's visibleAttributeStates emission (owned-but-invisible value no longer dropped). EntryForm shared mount behavior untouched (protects mobile capture). DetailPanel 20/20, desktop+entryCorrection 87/87, full test:unit 94+1299, tsc clean, parity 38/38. Controller-verified the diff (seed + visible filter exactly as prescribed). Trivial non-issue: initialCorrectionSeed runs validateEntryForm twice on mount (payload + valid lazy inits) — mount-only, negligible.
=== ALL 9 PHASE 5-6 TASKS (26-33) COMPLETE & REVIEW-CLEAN. Shared tree design-sync/agrolink @ 2afcbac2 (27,28,29,30). Isolated branches ready: sdd/task-33 (1c1a398f+78466215), sdd/task-32 (b71a62bb+c80cb016), sdd/task-31 (dd829fc8). NEXT: merge 33->32->31, then Task 34 (locales+feed mirror closes the transient parity reds), then Task 35 (final Opus whole-branch review + MERGE-READINESS). Two Criticals caught by adversarial review that green suites missed: T32 plot->plot silent value drop, T30 no-op correction full-record wipe. ===
MERGE PHASE COMPLETE & VERIFIED: merged sdd/task-33 (clean), sdd/task-32 (clean), sdd/task-31 (1 conflict: JournalWorkspace.tsx import block, resolved by unioning imports — DraftsQueue import+<DraftsQueue/> wiring intact) into design-sync/agrolink. Merge commits + HEAD fdcbc0d9. Verified on merged tree: tsc CLEAN; tsx-runner 94/94; edge osi-journal 281 (lifecycle 118 + api 57 + index 106) all green; edge profile copies (lifecycle/api/index .js) byte-identical bcm2712==bcm2709; vitest ONLY 12 expected reds = journalLocales parity (6 locales x key-shape+interpolation) from English-only drafts.*/transition.*/markers.* keys awaiting Task 34. No functional/component regressions. NEXT: Task 34 fills those keys into de-CH/es/fr/it/lg/pt, mirrors all 7 journal.json to the feed tree, extends agrolinkBranding.test.ts -> closes the 12 reds; then Task 35.
Slice 2 Task 34: COMPLETE (commit 4cff0b5a on design-sync/agrolink). Filled drafts.*/capture.transition.*/markers.* into all 6 non-English source locales (good-faith, interpolation-parity, de-CH ss-not-ss[sharp]); created journal.json in all 7 FEED locale dirs byte-identical to source (none existed before); added 'journal.json' to agrolinkBranding.test.ts mirror namespace list (RED->GREEN). BRANCH NOW FULLY GREEN — independently verified by controller: vitest 150 files/1399 tests, tsx-runner 94/94, edge osi-journal 281, feed journal.json byte-identical x7, edge profile parity x3, tsc clean, anti-slop PASS. Concern: en capture.transition.* prose still says "growing setting" vs the spec-6.2 "Layout" rename (G2, en untouched by T34) -> final-review triage.
Slice 2 Task 35: final whole-branch Opus review RUNNING (range c9eb4e3a..4cff0b5a, 16 commits). Given triage list scratchpad/final-review-triage.md to adjudicate must-fix vs acceptable + rule on G1 (DraftsQueue onResume NOT wired -> desktop draft completion non-functional, Close-only DraftResumePanel; vs Phase-6 acceptance "drafts queue opens the detail panel with field-level focus"). Pending: apply must-fix items, write MERGE-READINESS.md, finishing-a-development-branch (KEEP the branch, off-mainline by design).
Slice 2 FINAL whole-branch review (Opus, c9eb4e3a..4cff0b5a): Spec FAIL / Quality Changes-needed / MERGE-READY: NO — blocked SOLELY on G1. Both prior Criticals CONFIRMED CLOSED in integrated code (T32 fieldHiddenForEmptyTarget + lastRealDiffContextRef carry; T30 initialCorrectionSeed + state.visible ownership). All binding constraints HOLD (no flows.json; 0022 data-only INSERT/UPDATE, 0019 frozen; edge api/index/lifecycle/index.test .js + bundled farming.db byte-identical bcm2712==bcm2709; single markers fetch via useJournalMarkers; shared EntryForm reused not forked; EntryTable<->JournalWorkspace<->DetailPanel wiring coherent; merge left no broken imports). G1 (MUST-FIX): DraftsQueue draft completion non-functional on desktop — <DraftsQueue/> unwired onResume -> Close-only DraftResumePanel discards edits, DetailPanel blocks drafts -> no finalize path (misses Phase-6 "drafts queue ... so the draft can be completed"). Fix in flight (Sonnet, shared tree): add Complete action to DraftResumePanel finalizing via POST createEntry/promoteDraftInTransaction (NOT PUT updateEntry which rejects non-final with invalid_state), reuse buildCorrectionPayload but POST; +drafts.complete key x7 + feed mirror; folds T31 focus-dup Minor. Bundled G2 (reviewer's strongest ACCEPTABLE, cheap fast-follow): en capture.transition.* "growing setting" -> "Layout" (spec 6.2) x7 locales + feed. ALL OTHER triage items ruled ACCEPTABLE -> document in MERGE-READINESS (G2 note if unfixed, T30 DST single-offset limit, T33 focus-trap + 4px dendro residual as follow-ups, lg good-faith). After fix: verify full green + re-review G1 (edge finalize contract) + MERGE-READINESS.md + keep-branch finalize.
Slice 2 FINAL FIX: G1+G2 landed commit 5236d9a3 on design-sync/agrolink. G1: DraftResumePanel Complete action finalizes via journalApi.createEntry (POST -> promoteDraftInTransaction) reusing buildCorrectionPayload + base_sync_version:0 (controller-confirmed createEntry not updateEntry; only 'updateEntry' occurrence is an explanatory comment); folded T31 focus-dup into shared draftFieldStates; +drafts.complete/completing/completeError/completeStale x7 + feed. G2: "growing setting"->"layout" in capture.transition.{body,reasonFieldHidden,reasonChoiceInvalid} x7 + feed. Full suite GREEN: vitest 150 files/1401 (0 fail), tsx 94/94 (agrolinkBranding 10/10), journalLocales 38/38, tsc clean, all 7 feed journal.json byte-identical, 2 new G1 RED->GREEN tests. Non-blocking concerns: DraftResumePanel keeps showValidation always-true + disabled-button guard (no toggle-on-submit); G2 translations good-faith. G1 RE-REVIEW running (same Opus reviewer, fix delta 4cff0b5a..5236d9a3). If MERGE-READY -> MERGE-READINESS.md + keep-branch finalize.
Slice 2 G1 RE-REVIEW result: G1 core CLOSED (general case, POST->promoteDraftInTransaction path exact, base_sync_version:0 correct, buildCorrectionPayload carries all needed fields, context correctly omitted for server regen; RED tests genuine + pinning; showValidation-always-true + double-guarded Complete is acceptable/safer; no regressions), G2 CLOSED, Criticals still closed. BUT found G3 (new small MUST-FIX): DraftResumePanel never threads catalog.products into initialResumeSeed/EntryForm (passes []), so fertilization/fertigation drafts (attr.product_uuid first-class visible field) validate product vs empty list -> invalidDependency -> valid=false -> Complete permanently disabled = product-activity drafts uncompletable on desktop (NOT data loss, disabled=safe; same class as G1 for a core activity). Fix in flight (Sonnet, shared tree): thread catalog.products (DraftsQueue already has it via useJournalCatalog) -> DraftResumePanel -> initialResumeSeed + <EntryForm products>, symmetric with DetailPanel EntryCorrectionForm; RED-first product-bearing draft test. Self-contained in DraftsQueue.tsx, no prop plumbing.
Slice 2 Task 33: COMPLETE pending merge (commit 1c1a398f impl + 78466215 I1/M3 fix on sdd/task-33, base 022f8048; NOT merged — merge FIRST, cleanest: only en/journal.json overlaps). History-owned marker data hook (sole fetch owner) + pure JournalMarkerLane (icon+shape+color, >=48px, keyboard, bottom sheet, cluster-by-rendered-px). Opus review: Spec OK / Quality Changes-needed -> I1 FIXED: markerXPx now insets domain to [HISTORY_CHART_MARGIN.left+52, width-margin.right] matching the chart plot area (was full-width -> ~60px misalignment); RED-first test asserts window-start maps to left inset not 0 (verified non-vacuous). M3 FIXED: caps test now toBe(25) (verified fails when MAX_PAGES=50). markers+history suites 250/250, tsc clean. RESIDUAL (final triage / MERGE-READINESS): YAxis width 52 matches soil/environment/daily-min-max exactly but the 2 dendro chart views use 48 -> markers 4px off there (pre-existing chart-config inconsistency, visually within a marker width). DEFERRED Minors: bottom-sheet 20-row cap makes rows 21..N unreachable (M1); shape channel weak vs icon (M2); no dialog focus-trap (M4); pre-measurement width=0 collapse flash (M5); SWR key minute-rounding vs full-ISO fetch (M6). Expected-red: 12 markers.* locale-parity (Task 34).
