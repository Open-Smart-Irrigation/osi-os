Task 1: complete (commits a28a6237..f72c6e2b, review clean; Minor for final review: journal_products lacks updated_at — spec-inherited)
HANDOVER 2026-07-12: Tasks 2-14 handed to Codex orchestrator (docs/superpowers/prompts/field-journal-slice1-codex/prompt.md). Task 1 remains complete/reviewed.
Task 2: complete (commits c8e0b95f..eaf63ae7; controller gates green; re-review spec PASS / quality APPROVED; catalog v1 is a data migration; source 128 slots reconcile to 129 repaired runtime slots)
Task 3: complete (commits e3f92ca1..cce8ed94; 39/39 focused tests; helper/profile/deploy/full-sync gates green; final re-review spec PASS / quality APPROVED)
Task 4: complete (commits 355a9d02..d27a605f; 60/60 focused tests on both profiles; helper/profile/deploy/full-sync gates green; final re-review spec PASS / quality APPROVED)
Task 5: complete (commits 0f51c6a1..1c652bde; 75/75 focused tests on both profiles; 122/122 dependency rules audited; helper/profile/deploy/full-sync gates green; final re-review spec PASS / quality APPROVED)
Task 6: complete (commits 914ef911..afb48f92; 97/97 focused tests on both profiles; Java 21/Jackson differential 78,633 cases with 0 mismatches; helper/profile/deploy/full-sync/contract gates green; final re-review spec PASS / quality APPROVED; canonical contract pins UTF-16 key order and recursive string candidates)
Task 7: complete (commits 7d5356e8..a0fb227e; lifecycle 56/56; both profiles 97/97; helper/profile/deploy/full-sync gates green; transaction/outbox/command rollback evidence green; final re-review spec PASS / quality APPROVED)
Task 8: complete (commits 81a48756..6aaae56f; lifecycle 89/89; both profiles 97/97; bounded context reads and true SWT provenance; deploy/helper/profile/full-sync gates green; final re-review spec PASS / quality APPROVED)
Task 9: complete (commits 9e501287..7b2a88ea; contract/parity gates green; final re-review spec PASS / quality APPROVED; whole aggregates, semantic bindings, and direct SQL parity fail closed)
Task 10: complete (commits 7b2a88ea..52b27350; API 22/22, lifecycle 92/92, schema/sync/profile/deploy gates green; final re-review spec PASS / quality APPROVED; same-gateway ownership, reproducible exports, and research-identity exclusion verified; integration note: current origin/main independently claimed migration 0014, so renumbering remains an integration-time requirement under the no-rebase task contract)
Task 11: complete (commits 52b27350..e2782c8f; command 43/43 and controller gates green; final re-review spec PASS / quality APPROVED; replay is owner/gateway-scoped, same-effect apply is serialized, custom dependencies retry structurally, and leased ACK replacement uses a fresh row ID)
Task 12: complete (commits e2782c8f..b7aaffc2; bootstrap 25/25 and controller gates green; final re-review spec PASS / quality APPROVED; capability/catalog advertisement fails closed, the UI flag stays off, and asynchronous SQLite close errors reach contextual warnings)
Task 13: complete (commits b7aaffc2..5e4aca55; 10k entries/150k values, four intended SEARCH plans, list <100 ms, streamed CSV <64 MiB; controller gates green; final re-review spec PASS / quality APPROVED; additive 0017 adds D10 plot-scoped duplicate/sticky indexes and the fixture pins page-wise CSV writes)
Task 14: complete (commits 5e4aca55..043b5bdc; Node 22 field-journal CI workflow and 37-gate local suite green; independent review spec PASS / quality APPROVED; workflow is read-only, credential-free, dependency-free, and runs both maintained module mirrors plus schema/lifecycle/perf/profile gates)
Final-review correction: complete in this commit (IMP-1 through IMP-8 and MIN-2 corrected; journal modules 98/98 per profile, lifecycle 92/92, API 28/28, command 49/49, bootstrap 53/53, read snapshot 4/4, migration engine 60/60, perf and controller gates green; current origin/main migration 0014 collision remains integration-time work by explicit scope)
Final-review follow-up — custom-vocabulary admissibility: complete in this commit (choice-parent contract, closed custom constraint grammar, and reachable canonical-unit paths corrected; journal modules 99/99 per profile, API 32/32, command 51/51, full sync/profile/helper/contract gates green; no migrations, DBs, flows, workflow files, sister repo, or live systems touched; existing migration 0014 collision unchanged)
Fixplan Task 1 (2026-07-14 review fixes): complete (merge commit 7f3d1372; main @0f1361a3 merged with safety tag field-journal-slice1-pre-main-merge; journal migrations renumbered 0014/0015/0016/0017 -> 0018/0019/0020/0021 with 0019 regenerated by generate-journal-catalog.js; CHECKSUMS/seed/7 bundled DBs/both flows.json/size-ratchet baseline+allowances/silent-catch baseline all regenerated; migrate-flows-journal-commands+bootstrap one-shot input hashes re-pinned to main's file with anchors verified; UC512 fallback-registry insertion made presence-aware to avoid duplicating main's row; full 21-gate suite green; report .superpowers/sdd/fixplan-task-1-report.md)
Task 1: complete (commits 15d35d2f..7f3d1372 merge; 22 gates green; reviewed by orchestrator — op-parity resolution + one-shot script re-pins accepted; UC512 presence-guard correct)
Task 2: complete (commits 7f3d1372..50775bc4; api 33/33, module 99/99, parity green; orchestrator-reviewed — error-path fix in scope)
Task 3: complete (commit 1358eaa2; lifecycle 93/93, api 33/33, parity green; resolvePlotContext zone JOIN now matches NULL-gateway-EUI legacy zones, aligned with api.js's ownedZone predicate; only one join of that shape existed in lifecycle.js)
Task 3: complete (commits 50775bc4..1358eaa2; lifecycle 93/93, api 33/33, parity green; orchestrator-reviewed)
MINOR (final-review triage): lifecycle.js resolveDeviceEui zoneMatches (~line 424) has the same NULL-EUI under-matching shape on the device-ownership path — follow-up candidate, out of Task 3 scope.
Task 4: complete (commits 1358eaa2..39cd3077; api 36/36, module 99/99, parity green; orchestrator-reviewed — one-line removal, masking regressions covered)
Task 5: complete (commits 39cd3077..d0e7f3e6; api 37/37, module 99/99, parity green; orchestrator-reviewed — one-line active filter, create fall-through verified)
Task 6: complete (commits d0e7f3e6..82142b07; command 52/52, lifecycle 93/93, api 37/37, parity green; orchestrator-reviewed — one-line precedence flip, fallback preserved)
Task 7: complete (commits 82142b07..5cce17d7; lifecycle 94/94, api 37/37, module 99/99, parity green; orchestrator-reviewed — NUL-byte scare verified clean in committed file)
MINOR (final-review triage): draft duplicate_value error.details uses {field} object shape vs final path's array shape — matches file's 409 precedent, unify later if desired.
Task 8: complete (commits 5cce17d7..3655e9fd; 6 gates green incl. plan pin; orchestrator-reviewed — migration+seed+7 DBs+CHECKSUMS+consistency contract all consistent)
Task 9: complete (commits 3655e9fd..b1236e0a; wiring test 7/7, 12/12 files fenced + negative self-test; orchestrator-reviewed)
Task 10: complete (commit b1236e0a..02ec049b; new osi-command-ledger module smoke test 10/10, journal command-path 52/52, journal module 99/99, flows wiring/sync-flow/helper-registration/deploy-fence/profile-parity/no-stray-ddl all green; extraction resolved one hidden coupling beyond the plan's single validator hook — deduplicatePendingCommand's journal-type duplicate lookup also needed the intent-hash builder, so a second injected hook (opts.extraSubmittedIntentHash) was added alongside opts.extraEffectBindingValidator; journal-specific logic stays in osi-journal either way)
Task 10: complete (commits b1236e0a..02ec049b; 9 gates green incl. new ledger smoke 10/10; orchestrator-reviewed — second injected hook (extraSubmittedIntentHash) and fuller signatures approved as within plan intent; ACK node SPOF resolved, dedupe node retains osiJournal dep for hooks by design)
Final review (orchestrator, Fable): complete — cumulative diff 7f3d1372..c4a4f3a5 verified as exact union of approved task diffs; Minor #1 (resolveDeviceEui NULL-EUI) fixed as c4a4f3a5; Minor #2 (draft error.details shape) documented follow-up. Full 23-gate suite green post-fix (21 migrations, seed replay, 7-DB consistency, profile parity, sync flow/contract/op-parity 35/35, helper registration, contract schemas, flows wiring, deploy fence, journal schema/catalog/lifecycle 95/95/api 37/37/command 52/52/bootstrap/perf, module 99/99, ledger 10/10). Branch ready for push/PR.
PR141 hardening Task 1: complete (commit c8d8ec53; lifecycle 108/108, command 53/53, journal module 99/99, profile parity green; independent Luna review approved with mutation-sensitive reference and timezone regressions)
PR141 hardening Task 2: complete (commit 201247af; ledger 13/13, snapshot 10/10, command 53/53, profile parity green; independent Luna re-review approved after exact sync-version normalization and rollback-observation corrections)
PR141 hardening Task 3: complete (commit 81b1c161; API 38/38, 10k/150k perf fixture and profile parity green; independent Luna re-review approved after byte-bounded CSV/NDJSON writes and exact multibyte reassembly coverage)
PR141 hardening Task 4: complete (commit 82aad58f; verifier 44/44, live edge/server operation parity, sync-flow, and profile parity green; independent Luna re-review approved after recursive template-interpolation scanning closed the nested-call bypass)
PR141 hardening Task 5: complete (commit b26bfe04; contract, sync-contract, communication-contract, ledger 13/13, workflow syntax green; independent Luna re-review approved after Boolean-schema, JSON numeric equality, required-array, and JSON Pointer traversal corrections)
PR141 hardening Task 6: complete (commit e7570efa; catalog generator, generated-artifact check, migration manifest and diff gates green; independent Luna review approved for pre-write immutable checksum recovery)
PR141 hardening Task 7: complete (commit 399059be; all seven DBs preserve 0->0 settings rows, integrity/FK checks and nine schema gates green; independent Luna review approved migration, seed, checksum, bundled DBs, and frozen-boot boundary)
PR141 hardening Task 8: complete (commit 7df1d329; strict bootstrap/event/force outbox parsing, STREGA fallback, transformer replay and full flow gates green; independent Luna review approved exact hashes/allowances and silent-catch baseline 228->225)
PR141 hardening Task 9: complete (commit 0e796397; osi-lib 6/6, shared exact-source audit plus generator collision suite 44/44 and full flow gates green; independent Luna third-pass review approved four-node SHA policy and genuine current/legacy replay shape coverage)
PR141 hardening Task 10: complete in this commit (authoritative docs aligned to migrations 0018-0021, 13 tables, plot-scoped settings/indexes, formula-neutralized CSV and lossless records.ndjson; historical prompt/consolidation/task reports annotated without rewriting their snapshots; open+closed issue audit found no equivalents and created bootstrap-helper #142, command-registry #143, dynamic-SQL verifier #144, and canonical timestamp v2 #145; anti-slop, consistency, and diff checks green)

---

# Refactor repair program — Train A pre-A0 primitives (started 2026-07-16)

Base: f50950b1 (origin/main, PR #146 merge). Orchestrator: Fable; workers: Sonnet in isolated worktrees.
Wave 1 scratch branches (proposal vehicles; final integration history is reconstructed green per program rules):
- sdd/writer-primitives (plan 4 Tasks 1-2, module-only)
- sdd/chirpstack-helper (plan 5 Tasks 1-3, helper-only)
- sdd/sync-stoploss-harness (plan 3 Tasks 1-2; ack/commands sections expected red until activation slice)
- sdd/device-api-auth (plan 6 Tasks 1-2; flows patch is an orchestrator-replayed proposal)
Shared files (flows.json integration order, verify-sync-flow.js, test-flows-wiring.js, test-ci-guard-wiring.js, workflows, deploy.sh, allowances format, AGENTS.md) are orchestrator-owned; workers write proposals into reports.
Writer primitives (plan 4 T1-T2): complete (branch sdd/writer-primitives, f50950b1..a08c0672; writer 16/16, lsn50 13/13, uc512 5/5, integration 15/15, parity green; review spec PASS / quality APPROVED, 0 Critical/Important; Minors for final review: fixture duplication productionDecoded vs productionLsn50Decoded incl. modeLabelToStore divergence, .get() weaker than "exactly one" comment at index.test.js:483, redundant inserted:true test :490; forbidden-file proposals recorded in writer-report.md:99-106; NOTE integration must land the flows.json await companion or the async writer is inert live)
Sync stop-loss T1-T2 (plan 3): complete (branch sdd/sync-stoploss-harness, f50950b1..01338cf6; delivery 3/15 red -> 15/15 green, ack/commands red by design 2/8+2/9; parity/fn-parse/silent-catch/ratchet green; review spec PASS / quality APPROVED, 0 Critical/Important; reviewer independently verified only sync-outbox-mark+sync-bootstrap-mark changed of 602 nodes, sentinel text untouched, exact deltas 1845+244. Minors: [MUST-FIX-AT-INTEGRATION] present-but-empty results[] yields per-id protocol_response_missing_result:<id> instead of plan-literal batch protocol_response_missing_results + add harness case; boundedIssueSummary truncation path untested; red-harness scratch commit = orchestrator-authorized adaptation of T1 Step 7 (adjudicated, no action). Proposed verify-sync-flow.js pins in sync-report.md.)
ChirpStack helper T1-T3 (plan 5): review returned Needs fixes (2 Important): (1) verifyKeys compensation bypassed create-rollback leaving an orphaned new device — adjudicated against plan exit criterion "delete a newly created device only while the aggregate ownership fence still matches"; fix = fence-checked delete for ctx.deviceCreated, RECONCILIATION_REQUIRED on drift; (2) applySnapshotToDevice truthy JoinEUI guard left restore incomplete for empty-original JoinEUI. Minor: constantTimeEqual dead ternary. Fix dispatched back to original implementer on sdd/chirpstack-helper; re-review pending. Reviewer verified byte-identical mirrors via blob hashes and confirmed breaking-change inventory (deviceCreated/grpcStatus/details) complete vs both flow nodes; ripple note for Task 4: cs-reg-cloud-ack-fn ack.grpcStatus now always no-ops, Format Response embeds provisioning shape opaquely.
Device API auth T1-T2 (plan 6): complete (branch sdd/device-api-auth, f50950b1..1a7ccada; 27/75 red on base -> 75/75 green both profiles; fn-parse/silent-catch/ratchet/parity green; review spec PASS / quality APPROVED, 0 Critical/Important; reviewer verified all 41 tag protocols + classifier char-for-char, exactly 42 nodes changed, exact deltas, 43+3 fixture vs live graph, byte-identical mirror; bonus: closed incidental error.message echo on shared 500 path. Minors for final review: normalize routes array sort; pre-existing local-handler error.message echo in dendro-history-fn/cancel-strega-actuation-fn 500 bodies -> follow-up issue candidate. verify-sync-flow.yml/ci-guard proposals in deviceapi-report.md:88-98.)
ChirpStack helper T1-T3 (plan 5): complete (branch sdd/chirpstack-helper, f50950b1..d046c5a3; 41 pass/0 fail/8 expected-red-skip; re-review spec PASS / quality APPROVED; fence-gated create-rollback + explicit JoinEUI restore verified via read-back; Minors: fence-indeterminate comment, resourceKind label imprecision. Task 4 flow rewiring MUST land with this slice: deviceCreated->deviceAction, error.grpcStatus/details->code (ripples to cs-reg-cloud-ack-fn + Format Response), close() in both nodes, INTENT_PERSISTED boundary.)
WAVE 1 COMPLETE: all four scratch branches implemented and review-approved. Integration order on the future train-a branch: A0 first, then sync -> writer -> chirpstack -> device-api flow patches replayed by orchestrator with shared-verifier pins + allowance re-derivation.
WAVE 2 DISPATCHED (2026-07-16): sdd/a0-ratchet-absolute (A0 commit-3 scope: absolute ceilings + identity ownership split + parity lstat part (a)); sdd/a0-detect-backup-helpers (detect-rpi-profile.sh, backup-chirpstack-sqlite.js + tests, no deploy wiring); sdd/dbhelper-primitives (plan 3 durableTransaction/createDedicatedDatabase/enterFailStop, additions-only, both profiles). Deferred to dedicated waves: deployment-state library/CLI core (A0 commit 1), factory provenance/image bootstrap (A0 commit 2), osi-sync-protocol-state + activity witness core (plan 3 T3 commit 1-2), remaining flow slices (plan 3 T3-T5, plan 5 T4, writer T3), pipeline tasks, plan 7, Train B.
WAVE 2 interruption: all three workers terminated by session usage limit mid-run (2026-07-16 night); state at interruption: ratchet branch had both commits clean, helpers branch had detector commit + backup helper uncommitted red, dbhelper branch uncommitted tests + partial impl. All three resumed from transcripts 2026-07-17 with state-aware instructions; no work lost.
A0 ratchet migration: complete (branch sdd/a0-ratchet-absolute, f50950b1..61c3ce96, 2 commits; ratchet 25/25, ownership-split companion 6/6, parity test 12/12, identity/parity/sync-flow verifiers green, reviewer re-ran all gates live and re-measured ceilings exactly; review spec PASS / quality APPROVED). OPEN DECISION FOR PLAN OWNER (Important, plan-mandated, disclosed): absolute model bounds only the 32 owned nodes + exact max_total; ~208 unowned nodes lost individual ceilings and the 4096 new-node cap was dropped, so a same-commit grow-unowned/shrink-other rebalance passes silently. Matches plan text ("max_chars per owned node"); reinstating per-unowned-node bounds would need a baseline file the plan deliberately deleted. Decide: accept (rely on review + later-slice ceiling discipline) or add an unowned-node guard in a later A0 slice. Minors: text-heuristic duplicate-key scan; total_allowance dup-keys unchecked; flows-size-scan isThinNewNode/NEW_NODE_CEILING now dead exports (remove or reinstate with the guard).
A0 helpers (detector+backup): review NEEDS FIXES (3 Important, repro-verified): (1) default procd adapter blind to same-name respawn (no PID in identity) — the exact "service untouched" invariant; (2) failed attempt leaves stale destination(+journal), rerun collides, untested; (3) detector accepts SoC-only compatible (brcm,bcm2712 without raspberrypi,* board entry). Fix wave dispatched to implementer; re-review after. Reviewer ⚠️ follow-up for Task A4 Kaba100 rehearsal: exercise the real (non-injected) procd adapter live before trusting it. CI busybox-static gate lands with the workflow-union slice, not here.
db-helper primitives: review NEEDS FIXES (1 Important, deterministic repro): outer-facade db.run/exec inside durableTransaction work wedges the whole queue silently (runQueued/exec lack the ALS guard). Minors: restore-failure only tested on commit path; readSnapshot lacks post-open poison re-check. Fix dispatched to implementer; re-review after. Reviewer independently verified: double poison-check closes pre-enqueued TOCTOU, ALS does not false-reject unrelated concurrent callers, restore runs on all six exit paths, consumers (verify-sync-flow, sync-state-build) unaffected, mirrors byte-identical, no sandbox-env coupling in tests.
A0 helpers (detector+backup): complete (branch sdd/a0-detect-backup-helpers, f50950b1..0d33b769, 6 commits; detector 18/18 both shells, backup 23/23; re-review spec PASS / quality APPROVED; respawn-visible identity w/ unit-tested normalization, structurally-scoped failure cleanup + proven same-path rerun, board+SoC+model triple-agreement authority). Minors for final review: running-instance-with-null-pid could be rejected as ambiguous; cleanup swallows non-ENOENT unlink errors. Carryovers: exercise real procd adapter exec plumbing at Task A4 Kaba100 rehearsal; busybox-static CI gate lands with A0 workflow-union slice.
db-helper primitives: complete (branch sdd/dbhelper-primitives, f50950b1..578b7073; 16/16 both node forms; re-review spec PASS / quality APPROVED; choke-point guard, serialize silent-skip adjudicated consistent with pre-existing contract (zero in-repo callers), insertions-only, mirrors identical). Minors for final review: ALS context outlives durable work for detached continuations (fails loud+safe, misleading message post-commit; cheap active-flag tightening available); readSnapshot re-check covers fail-stop only (symmetry note).
WAVE 2 COMPLETE: sdd/a0-ratchet-absolute (61c3ce96), sdd/a0-detect-backup-helpers (0d33b769), sdd/dbhelper-primitives (578b7073) all APPROVED.
WAVE 3 DISPATCHED: sdd/deployment-state-core (A0 commit-1 sub-tranche: ordinary-lifecycle state library/CLI, no guard-bootstrap/factory/GC verbs, no resident copies, no deploy wiring); sdd/protocol-state-core (plan 3 commit-1 sub-tranche: four-root initialization + capability codecs/locks/load-verification + initialize/status verbs; runWitnessedOperation/checkpoints/disposition/reset/restore verbs deferred). Both instructed to commit green sub-checkpoints frequently due to session-limit interruption risk.
deployment-state core: review NEEDS FIXES — 1 CRITICAL (reproduced two-process race: concurrent first-arm both report ok, blind rename loses first writer; plan-verbatim O_EXCL violated; contention test was sequential and did not exercise it) + 3 Important (recover self-issued topology-activation receipt shape-indistinguishable from future guard-bootstrap authorization -> discriminator required; wrong-owner branches zero-tested despite getuid monkey-patch being available; acquireLock mkdir->writeLockOwner crash window deadlocks reclamation permanently) + 1 Minor (phase-pinned-during-recovery holds only by accident). Fix wave dispatched (link-exclusive first-arm + mutation lockfile CAS + real race tests). Integration follow-up from reviewer: deploy.sh must hold /var/lock/osi-deploy.lock.d across every verb call of an attempt — module does not self-enforce.
protocol-state core: review NEEDS FIXES (3 Important): (1) tail-deletion rollback — orchestrator adjudication: reviewer attack (both-roots consistent deletion) is outside plan threat model per line 352; plan-mandated single-root variant likely also undetected -> bidirectional generation/witness set-equality fix + three tests incl. documented out-of-model case; (2) deployment-state-gate parses invented flat format:1 envelope vs real {format:2,parentDeployment,activeSubOperation} -> strict real-shape rewrite; (3) pre-existing dirs accepted with wrong modes + activity.sqlite mode unchecked at load. Minors: synchronous enforced-at-open doc; restore-invalidation recoveryPhase field; export factory activity-anchor helper; verbatim stderr in reports. Reviewer verified: byte-exact genesis/witness/checkpoint schemas, correct pragma order, real crash-kill child tests, hot-journal workaround traced SAFE (unlink only post-close after guarded read). Fix dispatched.
deployment-state core: complete (branch sdd/deployment-state-core, f50950b1..5233f6ab, 11 commits; 175/175; re-review spec PASS / quality APPROVED; original Critical races re-run by reviewer: 50/50 fail closed; link(2)-exclusive first-arm independent of lock protocol; zero-byte-steal closed by construction; rename-quarantine reclaim with byte verification). REQUIRED HARDENING FOLLOW-UPS for the integration slice (named by reviewer, must not evaporate): (1) EEXIST-tolerant linkSync restore in acquireMutationLock mismatch path instead of overwrite-rename; (2) post-link self-verification read before tenure entry; (3) module-header note that full closure of the 3-process stale-reclaim window needs kernel flock(2) the platform precludes + hardlink-fs requirement note. Minors: non-dot-prefixed tmp/stale lock clutter; race-loser missing-flag vs lease-active error; lockfile read skips symlink/owner checks. Live follow-up: cross-uid wrong-owner at Kaba100 rehearsal. Reviewer integration reminder stands: deploy.sh must hold the attempt lock across all verb calls.
protocol-state core: complete (branch sdd/protocol-state-core, f50950b1..409299b5, 11 commits; module 88/88, CLI 21/21, all gates green; re-review spec PASS / quality APPROVED; reviewer re-ran original attack matrix live — all single-root rollbacks block with distinct codes; both-roots case test-pinned as out-of-threat-model w/ plan citation; tolerate-unknown-inside parentDeployment endorsed as correct for the provisional gate — full closed validation belongs to the integration slice importing the sibling library). Cosmetic: plan citation says line 352, sentence is on 353.
WAVE 3 COMPLETE: sdd/deployment-state-core (5233f6ab) + sdd/protocol-state-core (409299b5) both APPROVED.
WAVE 4 DISPATCHED: sdd/protocol-state-witnessed (branch OFF 409299b5 — STACKED: must integrate after protocol-state-core; runWitnessedOperation + adapter registry + activity checkpoint/prune/capacity machinery; osi-command-ledger integration + coverage verifier deferred); sdd/deployment-state-guard (branch OFF 5233f6ab — STACKED: guard-bootstrap chain verbs + claim-attempt + abandon + authorize-topology-activation with authorityKind guard-bootstrap + the 3 ledgered reclaim hardenings; image-baseline + staging-GC verbs deferred).
witnessed-ops slice: complete (branch sdd/protocol-state-witnessed, 409299b5..be7bfe77, 6 checkpoints; module 145/145 + CLI 21/21; review spec PASS / quality APPROVED; reviewer traced append discipline/one-ahead recovery/checkpoint cadence/bounded verification/one-use capability line-by-line vs plan; capacity test verified to cross a real checkpoint boundary at million-generation numbers; mirror byte-identical). NON-NEGOTIABLE ACCEPTANCE CRITERION promoted to the future osi-command-ledger slice: durable commandKeySha256-scoped dedup INDEPENDENT of the activity ledger 8193-row window, with an explicit test that a beyond-window replay of the same logical command cannot re-invoke an external-effect or ACK-transport adapter (deterministic operation IDs + window-bounded uniqueness = second effect otherwise). Minors: canonical-row-hash recompute duplicated in 3 places; lazy require inconsistency; 4 defensive verifier guards lack direct triggering tests; wall-clock budget flake risk (intrinsic).
guard-bootstrap slice: review NEEDS FIXES (targeted): I1 guard-94 absence path lacks receipt-existence check + zero absent-state test coverage; I2 test title promises operation-id-mismatch + unclaimed-chain negatives it never asserts; M1 new chain dir never fsynced into parent (power-loss dentry window). Fix dispatched. OPEN DECISION FOR PLAN OWNER #2: advance-guard-bootstrap prose requires five expectations (generation, head SHA, phase, boot epoch, boot ID) but the verbatim argv block has no --expected-boot-epoch/--expected-boot-id flags — reviewer verified the transitive head-SHA binding is cryptographically sound (entry hash covers both fields), so safety holds either way; decide whether to add the two flags to the plan argv (diagnostic specificity) or record the prose as satisfied transitively. Concretizations (a) claimed-terminal, (c) tombstone armed-detection, (d) zero-sentinel adjudicated CONSISTENT by reviewer with plan-text grounding; G5(a) EEXIST restore verified strictly better than round-1, no new lost-update path.
guard-bootstrap slice: complete (branch sdd/deployment-state-guard, 5233f6ab..d08748a4; 282/282; re-review spec PASS / quality APPROVED, no remaining findings). Recovery now rejects guard-aware-94 absence as guard-94-consumption-unverifiable because this slice has no typed, addressable consumption-receipt authority. Generation-1 durability is parent-of-root fsync -> guard-root fsync -> link -> chain-directory fsync; empty and temp-debris-only retries repeat the higher-level fsync proof, while later generations do not. The future deploy-integration slice may accept absent guard-aware-94 only after it creates and verifies a typed generation-bound consumption receipt in the same terminal generation.
A0 ratchet hardening: complete (branch sdd/a0-ratchet-absolute, final head b174be03). The plan-owner decision is exact coverage, not aggregate-only coverage: both profiles have 240 unique function IDs, the allowance set contains the same 240 IDs, every max_chars and max_total value equals the measured baseline, and missing or extra IDs fail closed. The original 32 detailed reasons remain unchanged; 208 inherited entries carry the bounded baseline reason. Ratchet 29/29, identity-ownership 6/6, identity/profile/sync-flow gates green; re-review spec PASS / quality APPROVED, no remaining findings. The committed 101/99 compensation test isolates per-node growth with unchanged total, and zero-length functions are explicitly covered.
PLAN-OWNER DECISIONS RESOLVED: (1) require exact per-node allowance coverage for every current function node; a new node needs a reviewed explicit ceiling. (2) do not add duplicate boot-epoch or boot-ID flags to advance-guard-bootstrap: the expected full entry-head SHA transitively binds bootEpoch and bootId, while generation and phase remain explicit. The repair-program plan records both decisions and the first-generation durability/receipt prerequisites.
WAVE 4 COMPLETE: sdd/protocol-state-witnessed (be7bfe77) and sdd/deployment-state-guard (d08748a4) are review-approved. The interrupted external review campaign is closed for all 11 implemented scratch slices recorded above. This does not mean the seven-plan repair program is implemented or integrated: shared-file replay, deferred slices, CI/workflow union, live rehearsal, Train A assembly, and later Train B work remain.
HANDOVER 2026-07-19: Train A finalization (wave-5 slices + integration through A3) handed to Codex orchestrator — docs/superpowers/prompts/refactor-repair-finalize-codex/prompt.md. Reports/briefs archived to .superpowers/sdd/handoff/. Stop boundary: end of A3 (no A3.5/A4, no push/PR, no live hosts). All 9 binding integration corrections + build order + resolved decisions embedded in the prompt.
FINALIZATION PREFLIGHT 2026-07-19: complete on integration/refactor-repair-train-a-a3-20260719 at pinned base f50950b1767a1aa6302ef2553d68a4e379b5b142; scratch refs/ancestry and archived evidence verified; base sync/profile/absolute-ratchet gates green; ordering conflict adjudicated A0-before-shared-activation; source branches classified as reconstruction inputs; A2 gate omissions and environment prerequisites recorded; A3 scope expanded to A0/A1/A2, five edge plans, PR146 preservation, hotspot ledger, and Train B non-foreclosure. No live access, push, PR, or main-checkout mutation.
A0 COMMIT 3 FINALIZATION: approved proposal head 27241bff167b76ea2382062a452150d97e2f98c6 (`fix: migrate the flow-size ratchet to absolute ceilings`), one commit/eight allocated files; orchestrator gates green; three independent-review correction loops closed unsafe/lossy numeric tokens, duplicate-key tokenizer escapes, and broken/intermediate/final symlink escape/cycle/special-target cases; final re-review spec PASS / quality APPROVED, no Critical/Important. Held unintegrated until A0 commits 1 and 2 precede it.
A0 COMMIT 1 FINALIZATION: candidate 81494bf8fcbe777f4d59a5d7d47fb6643a534f1b passed the full orchestrator gate union but independent review 3 returned spec FAIL / quality NEEDS FIXES. Confirmed blockers cover terminal recovery topology authority, complete inhibitor quarantine, crash-safe immutable publication, live-root/no-follow compatibility, operation-ID confinement, removal of legacy backup/restore authority, exact staged-npm confinement, deterministic backup/restore resume, persistent OpenWrt overlay authority, production-disabled test adapters, and complete factory-zero root/link/process proof. Third corrective TDD loop dispatched; candidate remains unapproved and unintegrated.
TRAIN A HOTSPOT LEDGER: extracted against f50950b1. Controlling order is A0 -> sync activation -> sync readiness -> writer -> ChirpStack -> Device API -> edge contract -> orchestrator-only AGENTS checkpoint. A0 is the first owner of node-red.init, pipeline files, osi-bootstrap, and verify-communication-contract.js even where the umbrella hotspot table understated it. Every shared resolution retains the PR146 four-role/six-link lifecycle and the complete named owner-test union; Train B-only provider/evidence/rehearsal modules remain untouched.
A0 COMMIT 1 REVIEW 4: candidate 0c4a62460f0320c736414d3236b4288c7886d242 passed the full orchestrator union but re-review remained spec FAIL / quality NEEDS FIXES. Literal production checks found a rejected /var/lock path, unreleasable recovered lock, arm crash dead-end, wrong pinned-ujail grammar, no post-restore topology proof, inconsistent persistent-overlay/path-mode authority, an invented database-restore contract, nobody-writable staging evidence, stale stopped-role evidence, and fragmented in-place immutable JSON publication. Fourth corrective TDD loop dispatched; candidate remains unapproved and unintegrated.
A0 COMMIT 1 REVIEW 5: candidate 6c4f4945e9f5536af427ac77ece9333515d5b6f0 passed the full orchestrator and hermetic gate union but independent review remained spec FAIL / quality CHANGES REQUIRED. Critical gaps cover claim-bound arming, mandatory recovery health/audit/zero-mutation evidence, current-topology startup measurement, active-recovery startup exclusion, compatibility restore after safety installation, stable mount identity, and concurrent immutable publication. Important gaps cover pinned ujail flags, complete live-control binding, attempt-lock races and reuse identity, staged-output ownership/modes, the production role-state adapter, canonical factory-database binding, ChirpStack post-backup validation, snapshot copy binding, and strict boot identity. Fifth corrective TDD loop dispatched; candidate remains unapproved and unintegrated.
A0 COMMIT 1 REVIEW 6: candidate 5eef790b0bfa39cd268a86e1e3112d98959068ea passed the complete post-amend orchestrator union and hermetic audit but independent review remained spec FAIL / quality CHANGES REQUIRED. Critical gaps cover arm-versus-abandon atomicity, no-follow root ownership, mutation-lock exclusion, predecessor authority, fresh terminal topology proof, and reboot-stable mount identity. Important crash/TOCTOU gaps remain in compatibility capture/resume, staged npm resume, factory-database ancestry, ChirpStack source/destination resume checks, and monotonic role-generation evidence. Sixth corrective TDD loop dispatched; candidate remains unapproved and unintegrated. Future re-review uses a separate immutable worktree.
A0 COMMIT 1 REVIEW 7: candidate 15bc6cc0f8d84515770f0b2b2b4f602277a8f71f passed the complete orchestrator union, including 313 deployment-state tests, 393 combined Node tests, shell gates, parity, and hermetic checks. Independent immutable-worktree review remained spec FAIL / quality CHANGES REQUIRED. Critical gaps cover full-root bind shadows, factory mount/publication ancestry, launcher CLI/runtime binding, pre-validation staged-root mutation, pinned-ujail identity resolution, early recovery-health permits, and exact-existing publication durability. Important gaps cover complete topology stabilization, jailed-process liveness, mutation-ticket PID reuse, second-pass link auditing, factory-directory durability, malformed device-tree properties, and ambient backup-helper shadowing. Seventh corrective TDD loop split across three isolated workers; candidate remains unapproved and unintegrated.
A0 COMMIT 1 REVIEW 8: candidate 55e59a8c474f6aec81241549e6a69ab99c5b83fc passed the complete orchestrator union, explicit POSIX and BusyBox gates, and the hermetic audit. Independent immutable-worktree review remained spec FAIL / quality CHANGES REQUIRED. Critical gaps cover the factory link-before-authority crash window, terminal S01 safety revalidation, and privileged ambient-tool resolution. Important gaps cover attempt-lock release/PID reuse, stable shared topology collection, compatibility inventory completeness, staged authority-root ownership, overlay full-root aliases, persistent permit/claim confinement, strict device-tree entries, and the old pipeline wrapper interface that commit 4 must update. Eighth corrective TDD loop split across three isolated workers; candidate remains unapproved and unintegrated.
A0 COMMIT 1 REVIEW 9: candidate 4b5b25a77b579a5268c4bc6f34453d2d4daa6c55 passed every plan-listed direct command separately, POSIX and BusyBox gates, repository verifiers, and the hermetic audit. Independent immutable-worktree review remained spec FAIL / quality CHANGES REQUIRED. Critical gaps cover factory pre-link-intent retry, stale guard claim/abandon evidence, and terminal startup's dependency on a released volatile lock. Important gaps cover complete guard role-generation facts, staged-npm supervisor journal recovery, and preservation of prefix-bound ChirpStack partials. Ninth corrective TDD loop split across three isolated workers; candidate remains unapproved and unintegrated.
A0 COMMIT 1 REVIEW 10: candidate ce242acf33f011c20f65d00ffd46544343150927 passed the full separate-command union, POSIX and BusyBox suites, repository verifiers, and the hermetic audit. Independent immutable-worktree review remained spec FAIL / quality CHANGES REQUIRED. Critical gaps cover unverified inhibitor CLI execution, safety residents incorrectly restored as predecessor topology, recovery lock-owner handoff, and the permit-consumed-before-launch crash window. Important gaps cover pre-mutation role restoration, six-link prior-role evidence, pre-terminal durable release intent, factory temporary-alias cleanup, and the fixed ChirpStack watchdog. Tenth corrective TDD loop split across primary state and isolated I/O workers; candidate remains unapproved and unintegrated.
A0 COMMIT 1 REVIEW 11: candidate cdb663e2dff59ff348bf94eba4d00ca1c124e165 passed the affected state and I/O unions, remaining repository commands, parity, and the corrected audit/current-role contract. Independent immutable-worktree review remained spec FAIL / quality CHANGES REQUIRED. Critical gaps cover quarantine-versus-predecessor terminal authority, mutation evidence lost across epochs or before the phase append, and child execution before durable supervisor authority. Important gaps cover independent readiness, terminal-to-new-recovery release state, and lifecycle publication debris. Eleventh corrective TDD loop split across state, role, and launcher workers; candidate remains unapproved and unintegrated.
A0 COMMIT 1 REVIEW 12: frozen candidate cce1945ae27d03b4644e1f3f31c8f357256452d1 passed the affected state and I/O unions, remaining repository commands, parity, and the corrected audit/current-role contract. Independent spec review remained FAIL and found Critical gaps in cross-epoch mutation-snapshot rebinding, fresh role/link evidence at each epoch-start, an untracked token-bearing launcher spawner after supervisor death, and pre-link role-state publication debris. Independent quality review remained CHANGES REQUIRED and found an additional Critical gap: repeat recovery accepts caller-supplied receipt hashes from mutable state without verifying the immutable deployment/acceptance and recovery/topology receipt files. Candidate remains unapproved and unintegrated; a twelfth corrective TDD loop is required.
A0 COMMIT 1 REVIEW 13: temporary candidate 1826941e (cce1945a plus role/state/launcher corrections) passed bounded role 15/15 and launcher 12/12 suites, but selected state/recovery checks were 31/32 because the existing repeat-recovery positive fixture still lacked immutable receipt files. Independent quality review remained CHANGES REQUIRED with Critical spawner pre-publication retry and spawner-phase receipt-schema gaps, plus an Important terminal receipt operationId cross-bind gap. Independent specification review found a Critical cross-epoch abandon-to-authorize snapshot authority mismatch and was still finalizing. Candidate remains unapproved and unintegrated; the thirteenth corrective loop is active.
A0 COMMIT 1 REVIEW 13 FINAL: frozen candidate 1826941e825dba6546c7a39550ca6877b599e81d failed specification review and quality review. Critical residuals are (1) cross-epoch abandon pins first-unresolved authority A while authorize-topology-activation reselects latest snapshot B, deadlocking legal recovery; and (2) guarded launch can leave a token-bearing process tree untracked between detached spawn, durable spawner publication, and watchdog installation. Quality also requires typed spawner-phase abort handling and terminal receipt operationId cross-binding. Candidate remains unapproved and unintegrated; corrective loop 13 continues.
A0 COMMIT 1 REVIEW 14: candidate 9d4c17b5 passed all authority and launcher checks except one G4 diagnostic-contract test (52/53; full CLI 377/378). Both independent reviewers returned FAIL/CHANGES REQUIRED solely because no-quarantined-topology was emitted after guard-fact-binding for an unmutated abandonment fixture. Correction 9a3faa45 reorders the fail-closed diagnostic path and passes the failing test plus G4 16/16; Review 15 is required.
A0 COMMIT 1 REVIEW 15 FINAL: candidate c9a48a44521af9f926683b0d33025bcaa8458232 passed independent specification and quality review. Gates: deployment-state CLI 378/378, current-role 15/15, guarded launcher 13/13, resident parity 6/6, G1-G4 focused union 110/110, launch/process authority and crash-resume selection 19/19, profile parity, source/resident byte comparison, JS/shell syntax, and diff checks. No Critical or Important findings remain. Candidate is approved for single-commit reconstruction but remains unintegrated pending the orchestrator amendment and final A0 commit-1 gate bundle.
A0 COMMIT 1 APPROVED: amended source commit 50f2c7ed6bcb810418616bf6bc13d798a83aca6b is one clean commit over 66111630 with the reviewed 62-file boundary. Final source gates are green: deployment-state CLI 378/378, current-role 15/15, launcher 13/13, residents 6/6, profile parity, syntax/diff, compatibility 19/19, backup 37/37, detector, staged-npm, inhibitor, factory seed 22/22, factory-zero audit 6/6, protocol 2/2, and role-start 2/2. Independent Review 15 specification PASS and quality APPROVED. Ready for the required integration checkpoint and cherry-pick.
A0 COMMIT 2 APPROVED: exact source candidate `3e741e746ad130c2b2134f15d8e4dbbfe6d01f49` (`fix: align factory bootstrap contracts`) passed independent specification and quality review after corrective loops for path/argv binding, image identity, mode enforcement, deterministic baseline IDs, one-use verification-result handling, generator refresh semantics, and resident authority no-follow checks. Final gates: serial provenance CLI 7/7, generator 4/4, built-rootfs verifier 6/6, source verifier 4/4, factory-baseline envelope 3/3, factory seed 22/22, factory-zero audit 6/6, resident guard 6/6; bootstrap shell PASS; sync-flow verifier, generator check, source verifier, profile parity, and diff check exit 0; deployment-state CLI 378/378. The default parallel runner remains disallowed for the anchor-mutating subset because of a tracked-file test race. Commit 2 is intentionally non-deployable pending the later image-baseline state-verb checkpoint.
