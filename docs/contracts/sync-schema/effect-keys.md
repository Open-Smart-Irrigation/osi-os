# Effect Keys

`effect_key` deduplicates the *physical effect* of a command across replays. While `command_id` deduplicates exact command re-delivery, `effect_key` deduplicates two distinct commands that would cause the same physical outcome (e.g., a scheduler trigger re-fired across reconciliation paths).

## Authority

Effect keys are generated by the **originator** of the command:

- Cloud generates effect keys for UI/API/cloud-scheduler commands.
- Edge generates effect keys for edge-scheduler and edge-originated actions.

Both originators emit the **same format string for the same logical effect**. The formats below are normative.

## Formats

### `irrigation:scheduler:{zone_id}:{schedule_id}:{scheduled_for_iso}`

Scheduler-triggered irrigation (cloud or edge scheduler). `scheduled_for_iso` is ISO-8601 UTC with millisecond precision (`YYYY-MM-DDTHH:MM:SS.sssZ`). Naturally idempotent across both schedulers and across force-sync replay.

### `irrigation:manual:{device_eui}:{originator}:{command_uuid}`

Ad-hoc operator irrigation. `originator` is `cloud` or `edge`. `command_uuid` is the command's UUID — making each intentional manual command unique, so repeated manual operator clicks are NOT silently coalesced. Deliberate retries by an operator are a new command with a new UUID.

### `config:{device_eui}:{config_key}:{config_version}`

Device configuration commands. `config_version` is a monotonically increasing integer per `(device_eui, config_key)` pair.

## Normalization

- `device_eui` is uppercase EUI-64 with no separators.
- `zone_id` is the canonical integer zone id.
- `command_uuid` is lowercase, hyphenated 8-4-4-4-12.
- `scheduled_for_iso` and any timestamps are in canonical form per `canonicalization.md` once Phase 0 lands it.

## Replay rules

- Force-sync replay must preserve the original `effect_key` from the source command.
- Two commands sharing an `effect_key` are deduplicated to a single applied effect, regardless of `command_id`.
